# D+ Runtime Module Readiness

本文评估 D+ / B27-RWO 被动双边买入 + redeem/salvage 方向在当前项目模块化架构中的落地条件。范围只覆盖策略之外的基础模块：数据、配置、协调器、OMS、Executor、User WS、Recorder、库存/ledger、claim/redeem、风控、测试与上线门槛。

结论：当前项目的基础模块足以支撑 D+ 的 observer 和小额 canary 路线，不需要重写执行框架。生产前主要缺口不是“有没有执行系统”，而是 D+ 专属的 lot ledger、source-of-truth 记录字段、钱包/赎回/资金锁定 reconciliation，以及严格的 stop-on-unknown 门。

## 模块状态总览

| 模块 | 当前状态 | 对 D+ 的判断 | 必须补的点 |
| --- | --- | --- | --- |
| 公共数据 / shared ingress | 可用 | 可直接支撑 observer/shadow 的行情输入 | 不能当作 user/order/fill truth |
| 策略注册 / 配置 | 已接入安全骨架 | `xuan_b27_dplus` 已注册并默认 disabled | 后续仍需 D+ lot ledger 和真实 order controller |
| Coordinator / 风控 | 部分可用 | 可复用统一报价治理，但 D+ 需要专属风险语义 | max open cost、max live orders、stop-on-unknown |
| OMS / OrderManager | 基本可用 | 四槽位、post-only 生命周期适配 D+ 双边 BUY | 初期只 observer，避免复用路径误下单 |
| Executor / CLOB | 基本可用 | 已有 post-only GTC、cancel、reconcile、User WS fill 路径 | 补 D+ candidate/order/fill 关联和 fee/role 字段 |
| User WS | 基本可用 | 是 live fill 的唯一真相入口 | 结构化保留 trade_id、maker/taker、fee、venue ts |
| Recorder | 已接入 observer scaffold | 已有 market/user/order/inventory/redeem streams；D+ observer tick 写入 events stream | 后续补 candidate/order/fill/redeem/cashflow 全链路 schema |
| Inventory / Pair ledger | D+ projection 已起步 | PGT ledger 不等于 D+ ledger；已新增 D+ FIFO lot ledger projection | 后续接 source-of-truth fills / redeem residual / scan_best |
| Claim / Redeem / Cashflow | 部分可用 | claim 工具链存在 | D+ 需要 wallet/cashflow/capital-lock reconciliation |
| 测试 / runbook | 部分可用 | staged dry-run/live 方法已存在 | 增加 D+ observer、recorder、ledger、canary tests |

## 关键模块判断

### 1. 公共数据平面

`SHARED_INGRESS_MULTIPROCESS_ZH.md` 的边界是清楚的：shared ingress 只共享 public market WS、Chainlink 和 local price feeds，不处理钱包、下单 authority、用户仓位/余额真相和 User WS。

这对 D+ 是正确边界。D+ observer 可以复用 public data plane；生产验证不能用 shared ingress 替代 authenticated order/fill/wallet truth。

### 2. 策略插件边界

当前新增策略指南规定：策略层只回答“当前 tick 想挂哪些单”，不能直接访问 OMS、Executor、网络、库存或 claim。D+ 符合“应该新建策略”的条件，因为它改变了入场逻辑、目标函数和常态库存语义。

D+ 应按 `UnifiedBuys` 接入，初始只输出 YES_BUY / NO_BUY 的候选语义。FAK salvage 不应在第一版策略层随意直连执行，而应走显式安全门和 one-shot taker repair 路径。

### 3. 消息与执行语义

现有 `TradeUrgency::{MakerPostOnly,TakerFak}`、`TradePurpose`、四个 `OrderSlot` 和 `TradeIntent` 足够表达 D+ 的主要动作：

- 被动 YES buy
- 被动 NO buy
- 可选 FAK salvage
- cancel/reprice/reconcile

但 `FillEvent` 目前只保留 `order_id / side / direction / filled_size / price / status / ts`。这对普通库存足够，对 D+ source-of-truth 不够。D+ canary 需要结构化记录：

- client candidate id
- client order id
- venue order id
- trade/fill id
- maker/taker role
- fee / rebate / fee asset
- venue event timestamp
- raw User WS correlation key

否则无法把回测 candidate、真实订单、真实 fill、redeem/cashflow 串成一条可审计链。

### 4. Executor 和 User WS

Executor 的 live 设计是正确的：post-only 下单成功只进入 live order 状态，不合成 fill；真实 fill 只来自 authenticated User WS。dry-run 模式有 synthetic/touch fill simulator，但文档已明确它只能验证生命周期和机制，不验证真实 fill edge。

User WS 解析已经具备 maker-first 路径，能处理 `maker_orders[]`，并以 User WS 作为 inventory truth。这是 D+ 小额 canary 的基础。

缺口是字段丰富度和审计关联，而不是执行路径本身。

### 5. Recorder

Recorder 已有 market、market_md、user_ws、events、meta streams，也能记录 order events、inventory events 和 redeem result。它是 D+ observer/canary 的自然落点。

D+ 需要增加一个 versioned event taxonomy，例如：

- `xuan_b27_dplus_observer_tick`
- `xuan_b27_dplus_canary_blocked_not_implemented`
- `xuan_b27_dplus_candidate`
- `xuan_b27_dplus_virtual_pair`
- `xuan_b27_dplus_order_intent`
- `xuan_b27_dplus_order_ack`
- `xuan_b27_dplus_fill_linked`
- `xuan_b27_dplus_pair_locked`
- `xuan_b27_dplus_salvage_intent`
- `xuan_b27_dplus_redeem_attempt`
- `xuan_b27_dplus_cashflow_snapshot`
- `xuan_b27_dplus_stop_unknown`

生产门槛里 `critical_drop_count` 必须为 0。任何 recorder drop 都应直接让 D+ verdict 进入 UNKNOWN/STOP，而不是继续统计收益。

当前已落地的最小安全钩子：

- `RecorderHandle::emit_strategy_event(...)`
- `src/polymarket/coordinator_xuan_b27_dplus.rs`
- `xuan_b27_dplus_observer_tick`
- `xuan_b27_dplus_canary_blocked_not_implemented`
- `src/polymarket/xuan_b27_dplus_correlation.rs`

这一步只记录 book、候选价格、库存、pair ledger projection 和安全配置。主循环在 `StrategyKind::XuanB27Dplus` 下仍会强制 `StrategyQuotes::default()`，所以该模块目前没有下单能力。

`xuan_b27_dplus_correlation` 把候选、订单、fill、lot、pair、redeem、cashflow 的 id 链显式化。candidate-only 的状态会返回 UNKNOWN，只有 order/fill 或 redeem/cashflow 的必需字段齐全时才 PASS。

### 6. Inventory / Pair ledger

现有 inventory 能区分 pending、settled、working，并处理 fill status lifecycle。现有 `pair_ledger` 提供 tranche、surplus、repair budget、capital state 和 merge 语义。

这些概念对 D+ 很有价值，但不能直接把 PGT ledger 当作 D+ ledger。原因：

- PGT 是 seed/hedge/tranche/merge 语义。
- D+ 是双边被动 BUY、内部 pair priority、redeem/settlement residual、可选 FAK salvage。
- D+ 的 scan_best/FIFO salvage 和 B27/RWO 式 passive/passive inventory discipline 需要 lot-level ledger。

已新增 `src/polymarket/xuan_b27_dplus_ledger.rs` 作为 D+ 专属 ledger projection 起点。当前覆盖：

- D+ fill lot
- FIFO YES/NO pair
- gross / net pair cost
- fee prorating
- residual qty / residual cost snapshot

它暂时不驱动下单、不读钱包、不做 redeem，只是把 D+ 的账本语义从 PGT tranche ledger 里分离出来。后续再接真实 User WS fill、redeem/cashflow 和 scan_best salvage。

### 7. Claim / Redeem / Cashflow

项目已有 Data API positions scan、claim/redeem、relayer/Safe/EIP712、round-claim runner 和 recorder redeem_result。说明基础 claim 能力存在。

D+ 缺的是 source-of-truth reconciliation：

- 哪些 D+ fills 已形成 full set
- 哪些 full set 已 redeem/claim
- redeem tx/hash/amount/status
- USDC balance before/after
- wallet capital lock / free balance
- unresolved residual 的真实成本

这部分不能靠 public P2 或其他交易员数据证明，只能用我们自己的 authenticated account/wallet/cashflow 数据证明。

## 生产前分阶段落地顺序

### G1a: Rust observer skeleton

目标：策略逻辑进入 Rust，但不能下单。

要求：

- `xuan_b27_dplus` strategy kind / registry / config 已新增。
- `PM_XUAN_B27_DPLUS_MODE=observer` 时只产出 recorder events，不创建 `SetTarget` 或 `ExecuteIntent`。
- 输出 candidate、价格、side、target_qty、imbalance state、pair opportunity、risk block reason。
- 不接触私钥、不发订单、不 cancel。

### G1b: authenticated dry-run observer

目标：接入我们自己的 account/user truth，但仍不下单。

要求：

- 记录 account positions / open orders / User WS availability / balances。
- 校验 recorder drop 为 0。
- 验证 candidate id 到真实市场状态、wallet/capital snapshot 的链路。
- 任意 UNKNOWN 直接 stop，不进入 canary。

### G2: $50-$100 smoke canary

目标：只验证 maker-only 订单机制，不验证盈利。

硬限制：

- 单市场。
- post-only maker-only。
- max open cost <= $50-$100。
- max live orders <= 2。
- no passive taker。
- stop on unknown。
- no automated scale-up。

通过标准：

- 下单/撤单/ack/fill/redeem/cashflow 全链路可审计。
- 所有 fills 都能和 order/candidate 对齐。
- fee/maker role 清楚。
- recorder drop 为 0。
- 触发 stop 时能及时撤单并归档。

### G3: $200-$500 initial canary

目标：开始观察真实 fill_rate、pair_share、residual、capital lock 是否接近 D+ 回测/影子区间。

### G4: production candidate

只有 G2/G3 多日通过后，才能讨论 production profile。当前 D+ 还不在此阶段。

## 近期 patch order

1. 已加 D+ 配置结构和 env 解析，默认 disabled。
2. 已加 `StrategyKind::XuanB27Dplus` 和 registry 测试。
3. 已加 observer-only skeleton：运行时强制空 quotes，不发单。
4. 已加第一版 D+ recorder observer event schema。
5. 已加 D+ lot ledger projection 和 FIFO/fee 单元测试。
6. 已加 source-of-truth correlation id 骨架和 UNKNOWN/PASS 判定。
7. 已把 observer payload 接入 source-of-truth UNKNOWN gate：candidate-only 会显式列出缺失的 order/fill 与 redeem/cashflow 字段，不能被提升为 PASS。
8. 已新增本地安全 smoke：`scripts/xuan_b27_dplus_observer_safety_smoke.sh`，验证 Rust 测试、强制空 quotes、无 order-intent surface、无正向下单 flag。
9. 已在 authenticated User WS 解析路径增加通用结构化 recorder event：`user_ws_fill_parsed`，记录 `order_id`、`trade_id`、maker/taker role、side、direction、qty、price、status、fee_rate_bps 可用性和 truth field flags。
10. 已补 executor `order_accepted` recorder 字段：dry-run、live、tick-size retry、small-size fallback 均写入 `order_id` 和 `venue_order_id`，便于后续 candidate -> order -> fill 串联。
11. 已新增通用 trace plumbing：`OrderAttemptTrace`、`OrderManagerCmd::SetTargetWithTrace`、`TradeIntent.trace`、OMS trace propagation、executor `order_accepted.data.correlation` payload。
12. 普通 `SetTarget` 保持 `trace=None`；D+ observer 仍强制 no-order，不会自动发 traced target。
13. 已加 D+ no-order linkage preview：observer payload 为每个 candidate 生成 deterministic `order_attempt_trace_preview`，字段包括 `candidate_id`、`order_attempt_id`、`run_id`、`market_session_id`，并显式标记 `preview_only=true`、`submitted=false`、`venue_order_id=null`。
14. 最新本地 smoke `xuan_b27_dplus_observer_safety_smoke_20260515T094110Z` PASS，确认 trace preview 没有打开任何 order-intent surface。
15. 已新增 auth observer dry-run runbook：`docs/research/xuan/DPLUS_AUTH_OBSERVER_DRY_RUN_RUNBOOK_ZH.md`。它规定 `PM_DRY_RUN=true`、`PM_XUAN_B27_DPLUS_MODE=auth_observer`、单市场、`MAX_LIVE_ORDERS=0`、禁止 order/cancel/redeem。
16. 已新增 no-order linkage preview artifact：`xuan_research_artifacts/xuan_b27_dplus_no_order_linkage_preview_20260515T0943Z.json`，记录 candidate -> order_attempt preview -> future order/fill join fields。
17. 已新增 auth observer readiness checker：`scripts/xuan_b27_dplus_auth_observer_readiness_check.sh`。最新 artifact `xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260515T094929Z/manifest.json` 为 `READY_FOR_APPROVAL`，全程不启动网络、不下单。
18. 已补 mode invariant 单元测试：`AuthObserver` 不允许 order submission，`Canary` 会强制 `post_only=true`、`allow_passive_taker=false`、`stop_on_unknown=true`。
19. 已补 source-of-truth schema 文档：`docs/research/xuan/DPLUS_SOURCE_OF_TRUTH_SCHEMA_ZH.md`，明确 candidate、order attempt、fill、lot/pair、redeem/cashflow 字段和 UNKNOWN/PASS 规则。
20. 已补 redeem/cashflow correlation 单元测试：pair-only evidence 仍为 `UNKNOWN`，只有 `redeem_attempt_id`、`redeem_tx_hash`、`cashflow_snapshot_id` 等字段齐全才 `PASS`。
21. safety smoke `xuan_b27_dplus_observer_safety_smoke_20260515T104939Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260515T104954Z` 为 `READY_FOR_APPROVAL`。
22. 主线程已授权第一轮本地 30 分钟 `auth_observer` dry-run，输出：
    `xuan_research_artifacts/xuan_b27_dplus_auth_observer_20260515T105826Z/`。
23. 第一轮 dry-run 结论：no-order 安全链路通过，但不是候选有效性验证。`run_manifest.json` 显示 `PM_DRY_RUN=true`、order/cancel/redeem 均 forbidden，当前二进制在 dry-run 下禁用 User WS，因此该轮只验证 market observer / recorder / no-order 行为。
24. 第一轮 dry-run 的 `observer_summary.json` 显示 6 个 BTC 5m 市场目录、290917 个 `xuan_b27_dplus_observer_tick`、0 个 no-order violation、0 个 submitted preview、0 个 `would_place`，source-of-truth verdict 全为 `UNKNOWN`。
25. 第一轮 dry-run 暴露出一个本地配置/实现问题：`PM_XUAN_B27_DPLUS_MARKET_SLUG=btc-updown-5m` 被当作完整 slug 精确匹配，实际市场 slug 为 `btc-updown-5m-<timestamp>`，导致所有候选被 `market_slug_filter` 阻塞。
26. 已修复 market filter：`btc-updown-5m` 现在可匹配 `btc-updown-5m-*` 系列 slug；新增单测 `xuan_b27_dplus_market_filter_accepts_5m_slug_prefix`。
27. 已新增 observer artifact summarizer：
    `scripts/xuan_b27_dplus_summarize_observer_run.py`，用于审计 no-order violation、candidate block reason、preview trace、source-of-truth verdict。
28. 修复后 `cargo test xuan_b27_dplus --lib`、`scripts/xuan_b27_dplus_observer_safety_smoke.sh`、`scripts/xuan_b27_dplus_auth_observer_readiness_check.sh` 均通过；最新 artifacts 包括 `xuan_b27_dplus_observer_safety_smoke_20260515T110724Z` 和 `xuan_b27_dplus_auth_observer_readiness_20260515T110439Z`。
29. summarizer 已增强：timeout/SIGTERM 造成的 JSONL 最后一行半写入会归类为 `truncated_final_line`，不再计为真实 `decode_error`。第一轮 observer summary 复跑后 `decode_error_count=0`、`truncated_final_line_count=1`。
30. summarizer 现在支持本地 gate：`--check-no-order` 用于 no-order 安全验收；`--require-tracked-candidates` 用于下一轮 observer 的 candidate tracking 验收。第一轮已知 blocked run 上，`--check-no-order` 通过，`--require-tracked-candidates` 返回 3，这是预期行为。
31. 已新增 `scripts/xuan_b27_dplus_observer_summary_gate_smoke.sh`。它用本地 fixture 验证三类 gate 行为：tracked/no-order artifact 通过双 gate；blocked/no-order artifact 只通过 no-order、candidate gate 返回 3；order violation artifact 的 no-order gate 返回 2。
32. readiness checker 现在要求 summary gate smoke PASS。最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260515T130304Z` 为 `READY_FOR_APPROVAL`；`cargo test xuan_b27_dplus --lib` 仍为 12 passed。
33. auth observer runbook 已补第二轮验收命令：下一轮结束后必须运行 `scripts/xuan_b27_dplus_summarize_observer_run.py "$RUN_DIR" --output "$RUN_DIR/observer_summary.json" --check-no-order --require-tracked-candidates`。exit 0 才表示 no-order 安全且候选 tracking 生效；exit 2 是安全违规；exit 3 是仍无 tracked candidate。
34. 最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260515T140215Z` 为 `READY_FOR_APPROVAL`。
35. readiness checker 现在也验证 runbook 中存在第二轮 summary gate 参数和 exit-code 说明。最新 summary gate smoke `xuan_b27_dplus_observer_summary_gate_smoke_20260515T150201Z` PASS；最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260515T150201Z` 为 `READY_FOR_APPROVAL`。
36. 已新增第二轮 no-order observer wrapper：`scripts/xuan_b27_dplus_auth_observer_dry_run.py`。它不带 `--approved-no-order-observer` 会拒绝启动（已测试返回 64），带授权参数时强制 `PM_DRY_RUN=true`、`MAX_LIVE_ORDERS=0`、standalone ingress、禁用 claim/redeem，并在结束后自动运行 summary gates。
37. readiness checker 现在静态检查 wrapper 约束。最新 summary gate smoke `xuan_b27_dplus_observer_summary_gate_smoke_20260515T153336Z` PASS；最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260515T153336Z` 为 `READY_FOR_APPROVAL`。本轮未启动 observer/network。
38. 下一步需要第二轮明确授权的 no-order observer：使用修复后二进制确认 candidate `would_track` 能产生，再考虑 User WS dry-run 兼容性设计或 $50-$100 G2。不得把第一轮提升为 fill / auth / 生产验证。
39. 主线程已明确授权第二轮 no-order observer。该轮输出 `xuan_research_artifacts/xuan_b27_dplus_auth_observer_20260516T002351Z/`，运行 1800.023s，`PM_DRY_RUN=true`、order/cancel/redeem 均 forbidden，`no_order_violation_count=0`。
40. 第二轮 summary gate 返回 3：`would_track=0`，所有 418540 个 preview 仍被 `market_slug_filter` 阻断。定位为 wrapper 直接运行旧 `target/debug/polymarket_v2` 的 stale-binary 风险，而不是 no-order 安全失败。已修复 wrapper：启动前执行 `cargo build --bin polymarket_v2`，记录 build 输出和二进制 mtime/size；readiness checker 现在要求该约束。下一步是在 readiness 重新 PASS 后复跑同一授权范围的 no-order observer。
41. 修复 wrapper 后复跑同一授权范围，输出 `xuan_research_artifacts/xuan_b27_dplus_auth_observer_20260516T010001Z/`。该轮运行 1800.016s，summary gate 返回 0，`status=PASS_NO_ORDER_OBSERVER`。
42. 复跑 summary：5 个 BTC 5m 市场、145759 个 observer tick、291518 个 trace preview、`would_track=290091`、`would_place=0`、submitted preview 0、no-order violation 0。`market_enabled=true` 覆盖 291518 个 preview，说明 prefix filter 已在真实 observer 路径生效。source-of-truth verdict 仍全为 `UNKNOWN`，因为当前 dry-run 会禁用 User WS，尚未产生自有 order/fill/redeem/cashflow truth。
43. 已新增只读 User WS dry-run 支持，但默认关闭。`PM_XUAN_B27_DPLUS_USER_WS_IN_DRY_RUN=true` 仅在 `xuan_b27_dplus` + `auth_observer` + `PM_DRY_RUN=true` 下生效，使用 env 提供的 `POLYMARKET_API_*`，不初始化 live order mode。wrapper 需要额外 `--approved-readonly-user-ws` 才会开启该路径。
44. 最新本地验证：`cargo build --bin polymarket_v2` 通过，`cargo test xuan_b27_dplus --lib` 12 passed，safety smoke `xuan_b27_dplus_observer_safety_smoke_20260516T014810Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T014826Z` 为 `READY_FOR_APPROVAL`。下一次带 User WS 的 observer 仍需要主线程明确授权，不能由 automation/heartbeat 自行启动。
45. 主线程已授权只读 User WS observer，但当前 xuan-research worktree 不存在 `.env` / `.env.local`，且 `.env.example` / `.env.test` 没有 `POLYMARKET_API_KEY`、`POLYMARKET_API_SECRET`、`POLYMARKET_API_PASSPHRASE`。第一次尝试输出 `xuan_b27_dplus_auth_observer_20260516T015918Z`，binary 因缺少三项 API credential 在 1.42s 安全退出，无 order/cancel/redeem。
46. wrapper 已加 credential preflight：若 `--approved-readonly-user-ws` 但三项 `POLYMARKET_API_*` 不齐，会在启动 binary 前写出 `READONLY_USER_WS_CREDENTIALS_MISSING` manifest 并返回 67。验证 artifact `xuan_b27_dplus_auth_observer_20260516T020312Z`；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T020320Z` 仍为 `READY_FOR_APPROVAL`。
47. `.env` 已在本 worktree 中软链接到 `/Users/hot/web3Scientist/pm_as_ofi/.env`，且 `.env` 被 git ignore。按 `.env` 注释语义修正：`POLYMARKET_API_*` 全空时，read-only User WS 会从 `POLYMARKET_PRIVATE_KEY` 派生 CLOB/User WS credentials；若三项只填一部分则拒绝启动。
48. 授权的 read-only User WS observer 曾启动并确认 User WS status=101，但随后发现 wrapper 使用 standalone public market WS，不符合共享 ingress 架构。该轮 `xuan_b27_dplus_auth_observer_20260516T023047Z` 已手动 SIGTERM 停止；摘要显示 BTC-only、no-order violation 0、user_ws 记录 53，但不能作为最终 shared-ingress 验收。
49. wrapper 已改为默认 `PM_SHARED_INGRESS_ROLE=client`，`PM_SHARED_INGRESS_ROOT=/Users/hot/web3Scientist/pm_as_ofi/run/shared-ingress-main`，且启动前要求已有健康 broker；若没有 broker，写出 `SHARED_INGRESS_BROKER_UNAVAILABLE` 并退出，不自动拉起 broker，也不回退 standalone。验证 artifact `xuan_b27_dplus_auth_observer_20260516T023941Z` 返回 69，未启动 binary。最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260516T023929Z` 为 PASS。
50. 已新增更严格的独立只读 User WS observer 二进制：`src/bin/xuan_b27_dplus_user_ws_observer.rs` / `cargo build --bin xuan_b27_dplus_user_ws_observer`。它不构造 `Executor`、`OrderManager`、`InventoryManager`，只检查 shared-ingress broker、解析/派生 User WS credentials、启动 `UserWsListener`、写 recorder 和本地 fill sink summary。无健康 broker 时 fail-closed 为 `SHARED_INGRESS_BROKER_UNAVAILABLE`，不回退 standalone。
51. 已新增本地无网络 static smoke：`scripts/xuan_b27_dplus_readonly_user_ws_static_smoke.sh`，验证独立入口必须显式 `--approved-readonly-user-ws`、必须检查 shared-ingress broker、无 standalone 逃生口、无执行 actor import、无 order/redeem/claim 调用。最新 artifact `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T033612Z` PASS；`cargo test xuan_b27_dplus --lib` 12 passed。
52. readiness checker 已纳入独立 User WS observer static smoke 和静态约束。下一次 read-only User WS 验收优先使用独立入口；旧 `polymarket_v2` wrapper 只保留为 no-order market observer / 兼容路径。仍不能在没有健康 shared-ingress broker 与主线程明确授权时启动网络进程。
53. 已新增独立入口专用 wrapper：`scripts/xuan_b27_dplus_readonly_user_ws_observer.py`。它要求 `--approved-readonly-user-ws`，启动前执行 `cargo build --bin xuan_b27_dplus_user_ws_observer`，强制 `PM_SHARED_INGRESS_ROLE=client`、`PM_DRY_RUN=true`、`PM_XUAN_B27_DPLUS_MAX_LIVE_ORDERS=0`，记录 build/binary/log manifest；若底层 manifest 为 `SHARED_INGRESS_BROKER_UNAVAILABLE`，wrapper 返回 69。最新 static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T033954Z` PASS，最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260516T034018Z` 为 READY_FOR_APPROVAL。
54. 已新增独立 User WS run summarizer：`scripts/xuan_b27_dplus_summarize_readonly_user_ws_run.py`。wrapper 结束后会自动生成 `readonly_user_ws_summary.json` 并执行 `--check-readonly`；如果底层 run PASS，还会要求 `--require-user-ws-records`。exit 2 表示只读违规，exit 3 表示没有 User WS 记录。独立 observer 会在 session_end 后等待 500ms 给 recorder flush。最新 static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T034347Z` PASS，最新 readiness `xuan_b27_dplus_auth_observer_readiness_20260516T034359Z` 为 READY_FOR_APPROVAL。
55. heartbeat 已重新持久化：`/Users/hot/.codex/automations/xuan-research-dplus-implementation-heartbeat/automation.toml` 为 `kind=heartbeat`、`status=ACTIVE`、`FREQ=MINUTELY;INTERVAL=30`。本轮无网络复验通过：`cargo test xuan_b27_dplus --lib` 12 passed，static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T044145Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T044155Z` 为 `READY_FOR_APPROVAL`。未启动 observer、broker、canary、order/cancel/redeem 或 live 进程；下一 gate 仍是健康 shared-ingress broker 加主线程明确授权。
56. 已新增只读 shared-ingress preflight：`scripts/xuan_b27_dplus_shared_ingress_preflight.py`。它只读取 `/Users/hot/web3Scientist/pm_as_ofi/run/shared-ingress-main/broker_manifest.json` 与 manifest 中 socket 路径，检查协议/schema、heartbeat age、socket 类型；不启动、不停止、不连接、不修复 broker。readiness checker 会用 `--soft` 写出最新 broker 前置状态，但不会把外部 broker 不健康误判为 xuan_b27_dplus 本地代码未准备好。
57. 已新增本地状态 bundle：`scripts/xuan_b27_dplus_local_status_bundle.py`。它汇总 heartbeat 持久化、readiness、read-only User WS static smoke、shared-ingress preflight、observer safety/summary gate，输出单个 no-network manifest，并给出 `verdict`。当前若本地 gate 通过但 broker manifest 缺失，verdict 应为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，用于区分外部 broker gate 与本地实现 gate。
58. 已新增状态 bundle smoke：`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh`。它静态检查 bundle 脚本没有网络/进程控制导入，运行 bundle，并验证 manifest 的 no-network/无副作用字段、heartbeat id、readiness 引用与 verdict 枚举。最新 smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T061256Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T061303Z` 仍为 `READY_FOR_APPROVAL`，并已纳入该 smoke，避免 bundle 变成未验证的旁路诊断。
59. 已新增只读 User WS auth-source preflight：`scripts/xuan_b27_dplus_auth_source_preflight.py`。它只读取当前环境和 `.env`，不派生 credentials、不连接网络、不打印 secret value；输出仅包含三项 `POLYMARKET_API_*` 是否完整、`POLYMARKET_PRIVATE_KEY` 是否存在、`.env` symlink 状态和 `auth_source` 分类。最新 preflight `xuan_b27_dplus_auth_source_preflight_20260516T071647Z` 为 `OK`，`auth_source=derive_from_private_key`，确认 `.env` 软链存在且可从 `POLYMARKET_PRIVATE_KEY` 派生；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T071655Z` 仍为 `READY_FOR_APPROVAL`。
60. 已新增 shared-ingress broker handoff 文档：`docs/research/xuan/DPLUS_SHARED_INGRESS_BROKER_HANDOFF_ZH.md`。该文档明确 xuan-research 不启动/修复 broker、不回退 standalone public market WS，只用只读 preflight 验收 broker manifest/socket 条件。即使 broker 变健康，read-only User WS observer 仍必须由主线程明确授权 exact run。
61. 已新增 auth-source fixture smoke：`scripts/xuan_b27_dplus_auth_source_preflight_smoke.sh`。它用临时 `.env` 覆盖四种本地场景：完整 API 三项通过、仅 private key 可派生通过、partial API 即使带 private key 也阻断、完全缺失阻断；同时检查 manifest 不包含 fixture secret value、不派生 credentials、不启动网络。最新 smoke `xuan_b27_dplus_auth_source_preflight_smoke_20260516T084619Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T084642Z` 仍为 `READY_FOR_APPROVAL`，且已纳入该 smoke，防止 auth-source gate 被后续改坏。
62. status bundle 已纳入 auth-source fixture smoke 状态，并由 `scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 验证该 smoke 存在且为 PASS。最新 bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T091544Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T091614Z` 仍为 `READY_FOR_APPROVAL`。这样 bundle 同时报告当前 auth-source 可用性和 auth-source 判定逻辑自身是否被 fixture 覆盖。
63. 已新增 shared-ingress preflight fixture smoke：`scripts/xuan_b27_dplus_shared_ingress_preflight_smoke.sh`。它用本地短路径 Unix socket fixtures 覆盖 missing manifest、healthy manifest/socket、stale heartbeat、missing socket 四种场景，并验证 preflight 没有 broker/network/order 副作用。readiness 现在要求该 smoke PASS；status bundle 也显式区分真实 `xuan_b27_dplus_shared_ingress_preflight` artifact 与 `xuan_b27_dplus_shared_ingress_preflight_smoke` artifact，避免 glob 把 smoke 误当成真实 broker 状态。最新 smoke `xuan_b27_dplus_shared_ingress_preflight_smoke_20260516T102004Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T102004Z` 仍为 `READY_FOR_APPROVAL`；最新 status bundle `xuan_b27_dplus_local_status_bundle_20260516T102022Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
64. 已新增 source-of-truth schema smoke：`scripts/xuan_b27_dplus_source_of_truth_schema_smoke.sh`。它把 `DPLUS_SOURCE_OF_TRUTH_SCHEMA_ZH.md`、Rust correlation spine、executor `order_accepted` correlation、authenticated `user_ws_fill_parsed`、redeem result event、observer candidate-only UNKNOWN gate 绑成一个本地契约检查。readiness 和 status bundle 现在都会引用该 smoke，防止后续只保留 observer 启动能力却丢掉 order/fill/redeem/cashflow 证据链。最新 smoke `xuan_b27_dplus_source_of_truth_schema_smoke_20260516T104819Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T104820Z` 仍为 `READY_FOR_APPROVAL`；最新 status bundle `xuan_b27_dplus_local_status_bundle_20260516T104821Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
65. 已新增只读 User WS summary gate fixture smoke：`scripts/xuan_b27_dplus_readonly_user_ws_summary_gate_smoke.sh`。它构造本地 run/recorder fixtures，验证 `scripts/xuan_b27_dplus_summarize_readonly_user_ws_run.py` 对 PASS+records 返回 0、PASS 但无 User WS 记录返回 3、broker unavailable 不计只读违规、forbidden order event 和 `orders_possible=true` 返回 2。readiness 和 status bundle 现在都会引用该 smoke，避免真实只读 User WS run 后的 summary gate 被误改。最新 smoke `xuan_b27_dplus_readonly_user_ws_summary_gate_smoke_20260516T111729Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T111730Z` 仍为 `READY_FOR_APPROVAL`；最新 status bundle `xuan_b27_dplus_local_status_bundle_20260516T111730Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
66. 已新增只读 User WS wrapper refusal smoke：`scripts/xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke.sh`。它只测试 wrapper 前置拒绝，不启动 observer/broker/network：缺少 `--approved-readonly-user-ws` 返回 64 且不 build；带授权但非法 duration 也在 build 前返回 64。readiness 和 status bundle 现在都会引用该 smoke，确保启动器在进入 build/binary/observer 前先 fail-closed。最新 smoke `xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke_20260516T114742Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T114742Z` 仍为 `READY_FOR_APPROVAL`；最新 status bundle `xuan_b27_dplus_local_status_bundle_20260516T114743Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
67. 已收紧独立只读 User WS observer 的 shared-ingress broker 检查：`src/bin/xuan_b27_dplus_user_ws_observer.rs` 现在用 `FileTypeExt::is_socket()` 验证 manifest 中的 chainlink/local_price/market 三个路径必须是 Unix socket，而不只是路径存在。`scripts/xuan_b27_dplus_readonly_user_ws_static_smoke.sh` 已纳入该静态约束。最新 `cargo build --bin xuan_b27_dplus_user_ws_observer` 通过，static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T121607Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T121608Z` 仍为 `READY_FOR_APPROVAL`。
68. 只读 User WS wrapper 现在会在 `cargo build` / binary 启动前先调用 `scripts/xuan_b27_dplus_shared_ingress_preflight.py`。若 shared-ingress broker 不健康，wrapper 直接写 `SHARED_INGRESS_BROKER_UNAVAILABLE` 并返回 69，不进入 build、不写 run manifest、不启动 observer。`scripts/xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke.sh` 已覆盖该路径：latest smoke `xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke_20260516T124619Z` PASS，missing-broker fixture 确认 `broker_preflight_status=UNAVAILABLE` 且无 build log。readiness `xuan_b27_dplus_auth_observer_readiness_20260516T124620Z` 仍为 `READY_FOR_APPROVAL`。
69. 只读 User WS wrapper refusal smoke 已补 secret-value redaction 覆盖。新增 missing-broker fixture 会带入哨兵 `POLYMARKET_API_*`、`POLYMARKET_PRIVATE_KEY` 和 `PM_*SECRET*` 环境值，验证 wrapper 在 `SHARED_INGRESS_BROKER_UNAVAILABLE`/69 早停路径中不写 build log、不写 run manifest、不启动 observer，并且 artifact/log 不包含这些敏感值。latest smoke `xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke_20260516T131545Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T131617Z` 为 `READY_FOR_APPROVAL`；status bundle `xuan_b27_dplus_local_status_bundle_20260516T131638Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
70. shared-ingress preflight smoke 已补“路径存在但不是 Unix socket”的 fixture。新增 regular-file socket case 会让 manifest 中 `market_socket` 指向普通文件，预期 preflight 返回 `UNAVAILABLE` / `broker_socket_unavailable`，并在 `socket_checks` 里记录 `exists=true`、`is_socket=false`、`reason=not_socket`。latest smoke `xuan_b27_dplus_shared_ingress_preflight_smoke_20260516T134002Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T134017Z` 为 `READY_FOR_APPROVAL`；status bundle `xuan_b27_dplus_local_status_bundle_20260516T134023Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
71. 只读 User WS run summarizer 现在把 recorder JSONL decode error 作为 gate 失败。`scripts/xuan_b27_dplus_summarize_readonly_user_ws_run.py` 若看到 `user_ws.jsonl` 或 `events.jsonl` decode error，会输出 `FAIL_RECORDER_DECODE_ERROR`，且 `--check-readonly` 返回 2。`scripts/xuan_b27_dplus_readonly_user_ws_summary_gate_smoke.sh` 已新增 malformed `events.jsonl` fixture；latest smoke `xuan_b27_dplus_readonly_user_ws_summary_gate_smoke_20260516T141205Z` PASS，fixture summary 确认 `event_decode_error_count=1`。readiness `xuan_b27_dplus_auth_observer_readiness_20260516T141233Z` 为 `READY_FOR_APPROVAL`；status bundle `xuan_b27_dplus_local_status_bundle_20260516T141238Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
72. 只读 User WS static smoke 现在检查 Rust `RunManifest` 不包含 secret material 字段。`scripts/xuan_b27_dplus_readonly_user_ws_static_smoke.sh` 会抽取 `src/bin/xuan_b27_dplus_user_ws_observer.rs` 的 `RunManifest` struct，并拒绝 `api_key`、`api_secret`、`api_passphrase`、`private_key`、`credential`、`token`、`secret` 等字段；允许的只是 `user_ws_auth_source` 分类。latest static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T141520Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T141602Z` 为 `READY_FOR_APPROVAL`；status bundle `xuan_b27_dplus_local_status_bundle_20260516T141609Z` verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
73. 主线程授权后，已按当前规则尝试一次独立 read-only User WS observer：`scripts/xuan_b27_dplus_readonly_user_ws_observer.py --approved-readonly-user-ws --duration-seconds 1800 --market-slug btc-updown-5m`。wrapper 在 build/observer 前运行 shared-ingress preflight，因 `/Users/hot/web3Scientist/pm_as_ofi/run/shared-ingress-main/broker_manifest.json` 缺失返回 69，`wrapper_manifest.json` 状态为 `SHARED_INGRESS_BROKER_UNAVAILABLE`，`broker_preflight_reason=broker_manifest_missing`。该路径没有写 cargo build log，没有写 `run_manifest.json`，因此没有启动 observer、User WS、broker 或任何网络路径。artifact: `xuan_research_artifacts/xuan_b27_dplus_user_ws_observer_20260516T141956Z/`。
74. local status bundle 现在会把最近一次 read-only User WS observer wrapper 尝试作为安全摘要暴露出来。`scripts/xuan_b27_dplus_local_status_bundle.py` 新增 `latest_readonly_user_ws_attempt` 字段，只包含 wrapper 状态、broker preflight 状态/原因/返回码、shared-ingress role/root、order/cancel/redeem booleans 等安全字段，不带 `.env` key 列表或潜在 secret value。`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 已要求该摘要存在且 order/cancel/redeem 均为 false。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T144521Z` PASS；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T144535Z` 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`。
75. read-only User WS wrapper manifest 已去掉原始 env key 名称输出。`scripts/xuan_b27_dplus_readonly_user_ws_observer.py` 不再写 `dotenv_loaded_keys` 或 `safe_env`，改写 `dotenv_loaded` / `env_snapshot` 摘要，只记录加载数量、敏感类 key 数量和 `key_names_recorded=false`、`secret_values_recorded=false`。`scripts/xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke.sh` 现在验证 missing-broker artifacts 不含哨兵 secret value，也不含 `POLYMARKET_API_KEY`、`POLYMARKET_PRIVATE_KEY`、`PM_TEST_SECRET_TOKEN` 等 key 名称。latest wrapper refusal smoke `xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke_20260516T151705Z` PASS；latest static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260516T151705Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T151736Z` 为 `READY_FOR_APPROVAL`。
76. EC2 shared-ingress broker 已通过 bounded read-only SSH discovery 只读核对健康。SSH preflight 验证目标为 `ip-172-31-41-70`，远端 `/srv/pm_as_ofi/shared-ingress-main/broker_manifest.json` 存在，`protocol_version=1`、`schema_version=1`，probe 时 heartbeat age 约 490ms，`chainlink.sock`、`local_price.sock`、`market.sock` 均存在且为 Unix socket。artifact: `xuan_research_artifacts/xuan_b27_dplus_remote_shared_ingress_probe_20260516T153221Z/manifest.json`。该核对没有调用 systemd/service control，没有启动/修复 broker，没有启动 observer/User WS，也没有 order/cancel/redeem、deploy、tunnel、scp、rsync 或 remote write。解释：EC2 broker healthy 只解锁“在 EC2 上作为 shared-ingress client 运行 observer”的可能性；本机 wrapper 不能通过 `--shared-ingress-root=/srv/pm_as_ofi/shared-ingress-main` 消费远端 Unix socket。本机状态仍受本地 broker manifest 缺失阻断，远端 30m read-only User WS observer 需要当前对话里单独明确 exact EC2 run 授权。
77. local status bundle 已纳入 EC2 broker probe 的本地安全摘要，但不改变本地 broker gate。`scripts/xuan_b27_dplus_local_status_bundle.py` 新增 `remote_shared_ingress_probe` 字段，只从本地 artifact 读取远端 root/manifest、protocol/schema、probe 时 heartbeat age、socket check 数量与 all-ok、side-effect all-false、`local_process_can_use_remote_unix_sockets=false`、`requires_same_host_observer=true`。`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 现在要求该摘要存在、状态 OK、socket 全部 OK、副作用全 false，并明确本地进程不能消费远端 Unix socket。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T154559Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T154609Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T154614Z` 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，本地 broker preflight 仍是 `UNAVAILABLE` / `broker_manifest_missing`。
78. 主线程授权 EC2 上以 shared-ingress client 接 `/srv/pm_as_ofi/shared-ingress-main` 的 exact run 后，已执行 SSH/broker/entrypoint 前置检查并 fail closed。SSH 身份通过，远端 broker 仍健康，probe 时 heartbeat age 约 2ms，三条 socket 均为 Unix socket；但远端现有 Cargo roots `/srv/pm_as_ofi/repo`、`/home/ubuntu/pm_as_ofi-xuan-frontier`、`/home/ubuntu/xuan_research_worktrees/pm_as_ofi_xuan_shadow_freeze` 均没有独立只读 User WS observer 入口：缺 `scripts/xuan_b27_dplus_readonly_user_ws_observer.py`、`scripts/xuan_b27_dplus_shared_ingress_preflight.py`、`scripts/xuan_b27_dplus_summarize_readonly_user_ws_run.py`、`src/bin/xuan_b27_dplus_user_ws_observer.rs` 和 `target/debug/xuan_b27_dplus_user_ws_observer`。artifact: `xuan_research_artifacts/xuan_b27_dplus_ec2_readonly_user_ws_attempt_20260516T161334Z/manifest.json`。未启动 observer/User WS/network，未进行 broker/service control/deploy/sync/tunnel/order/cancel/redeem。`scripts/xuan_b27_dplus_local_status_bundle.py` 已新增 `latest_ec2_readonly_user_ws_attempt` 摘要；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T161432Z` PASS，readiness `xuan_b27_dplus_auth_observer_readiness_20260516T161438Z` 为 `READY_FOR_APPROVAL`，latest bundle `xuan_b27_dplus_local_status_bundle_20260516T161438Z` 记录 `FAILED_CLOSED_REMOTE_OBSERVER_ENTRYPOINT_MISSING`。下一 gate 是提供已有 EC2 xuan-research worktree/binary，或单独明确授权部署/同步方案。
79. local status bundle 现在显式暴露 EC2 next gate。`scripts/xuan_b27_dplus_local_status_bundle.py` 新增顶层 `ec2_readonly_user_ws_status` 与 `ec2_next_gate`，当前分别为 `WAITING_FOR_EC2_OBSERVER_ENTRYPOINT` 和 `provide existing EC2 observer worktree/binary or explicitly approve deployment/sync plan`。`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 现在验证 EC2 状态枚举、EC2 next gate 非空、EC2 broker socket checks OK、EC2 attempt side effects all false，以及 `required_remote_entrypoint_found=false`。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T161903Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T161917Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T161918Z` 保持本机 verdict `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，同时把 EC2 gate 单独显示出来。
80. EC2 read-only attempt manifest 现在有独立 no-network smoke gate。新增 `scripts/xuan_b27_dplus_ec2_readonly_attempt_smoke.sh`，只读本地 `xuan_b27_dplus_ec2_readonly_user_ws_attempt_*` manifest，验证授权 scope、order/cancel/redeem 禁止、SSH OK、EC2 broker protocol/schema/socket checks OK、所有 side effects 为 false、entrypoint-missing fail-closed 语义和 deployment/sync next gate。`scripts/xuan_b27_dplus_auth_observer_readiness_check.sh` 现在要求该 smoke PASS，`scripts/xuan_b27_dplus_local_status_bundle.py` / `scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 也纳入 `ec2_readonly_attempt_smoke`。latest EC2 attempt smoke `xuan_b27_dplus_ec2_readonly_attempt_smoke_20260516T165520Z` PASS；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T165527Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T165528Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T165537Z` 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，EC2 status 仍为 `WAITING_FOR_EC2_OBSERVER_ENTRYPOINT`。
81. EC2 observer entrypoint 现在有本地 requirement manifest gate，作为任何部署/同步授权前的 review checklist。新增 `scripts/xuan_b27_dplus_ec2_entrypoint_manifest.py`，只在本地记录 required entrypoint files、supporting gate files、Rust module hints、SHA256、推荐远端 root 和 required shared-ingress root；不打包、不同步、不部署、不 SSH、不连接网络。新增 `scripts/xuan_b27_dplus_ec2_entrypoint_manifest_smoke.sh`，验证 manifest 不包含 `.env`、`xuan_research_artifacts`、raw/replay、target 或 secret-adjacent runtime paths，required files 全部存在且有 SHA256，所有 side effects 为 false，并要求 separate deployment/sync approval。readiness 已纳入该 smoke，同时把 local status bundle smoke 的定位改为直接按 `manifest.json` 文件选最新，避免并发 smoke 创建空目录时误判。latest entrypoint manifest smoke `xuan_b27_dplus_ec2_entrypoint_manifest_smoke_20260516T172628Z` PASS；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T172640Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T172716Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T172721Z` 显示 `ec2_entrypoint_manifest_smoke=PASS`，EC2 status 仍为 `WAITING_FOR_EC2_OBSERVER_ENTRYPOINT`。
82. EC2 read-only attempt gate 现在区分“entrypoint 缺失而 fail-closed”和未来真正 observer PASS/FAIL run 的 side-effect 语义。`scripts/xuan_b27_dplus_ec2_readonly_attempt_smoke.sh` 不再把所有 side effects false 作为所有状态的公共条件；`FAILED_CLOSED_REMOTE_OBSERVER_ENTRYPOINT_MISSING` 仍要求 observer/User WS 未启动、remote write/deploy/sync/tunnel/order/cancel/redeem 全 false、`required_remote_entrypoint_found=false`；但未来 `PASS_READONLY_USER_WS_OBSERVER` / `FAIL_READONLY_USER_WS_OBSERVER` 可以记录 observer/User WS 曾启动，同时必须保持 broker/service/deploy/sync/tunnel/order/cancel/redeem 等 forbidden side effects false。`scripts/xuan_b27_dplus_local_status_bundle.py` 现在暴露 `forbidden_side_effects_absent`、`observer_started`、`user_ws_started`、`orders_sent`、`cancels_sent`、`redeems_sent` 等摘要字段；`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 按 EC2 attempt status 条件化验证这些字段，并收窄静态 grep，避免把 `scp_or_rsync_used` 这类禁止字段名误判为实际进程调用。latest EC2 attempt smoke `xuan_b27_dplus_ec2_readonly_attempt_smoke_20260516T202553Z` PASS；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T202554Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T202600Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T202600Z` 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，EC2 status 仍为 `WAITING_FOR_EC2_OBSERVER_ENTRYPOINT`；`cargo test xuan_b27_dplus --lib` 12 passed。
83. EC2 entrypoint requirement manifest 现在记录脚本 executable/shebang 合约，避免未来同步到 EC2 后才发现入口脚本不可直接执行。`scripts/xuan_b27_dplus_ec2_entrypoint_manifest.py` 对 required/supporting script files 记录 `expected_executable`、`mode_octal`、`is_executable`、`has_shebang`、`shebang`，并输出 `expected_executable_files` 与 `missing_executable_files`；`scripts/xuan_b27_dplus_ec2_entrypoint_manifest_smoke.sh` 要求所有 expected executable 都有 executable bit 和 `#!/usr/bin/env ...` shebang。latest entrypoint manifest smoke `xuan_b27_dplus_ec2_entrypoint_manifest_smoke_20260516T205446Z` PASS，内部 manifest 报告 11 个 expected executables 且 `missing_executable_files=[]`；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T205452Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T205452Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T205452Z` 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，EC2 status 仍为 `WAITING_FOR_EC2_OBSERVER_ENTRYPOINT`；`cargo test xuan_b27_dplus --lib` 12 passed。
84. local status bundle 现在直接暴露 EC2 entrypoint executable/shebang 合约摘要，不需要人工打开二级 entrypoint manifest 才能看出入口是否可执行。`scripts/xuan_b27_dplus_local_status_bundle.py` 新增 `summarize_ec2_entrypoint_manifest_smoke`，读取 smoke 中的 `entrypoint_manifest`，并在 `ec2_entrypoint_manifest_smoke` 摘要里写出 `entrypoint_manifest_exists`、`entrypoint_manifest_status`、`expected_executable_count`、`missing_executable_files`、`executable_contract_ok`、`required_entrypoint_file_count`、`supporting_gate_file_count`。`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 要求二级 manifest 存在且 PASS、expected executable 覆盖非空、`missing_executable_files=[]`、`executable_contract_ok=true`。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T233255Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T233303Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T233303Z` 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，EC2 status 仍为 `WAITING_FOR_EC2_OBSERVER_ENTRYPOINT`；`cargo test xuan_b27_dplus --lib` 12 passed。
85. 经主线程明确授权部署/同步后，EC2 上已出现隔离的 xuan-research read-only User WS observer worktree 和 binary。同步目标为 `/home/ubuntu/xuan_research_runs/xuan_research_dplus_entrypoint_sync_20260516T233733Z/worktree`，只通过 SSH tar 写入隔离 `xuan_research_runs` 目录，不修改 `/srv/pm_as_ofi`、shared-ingress、systemd/service、`.env` 或 live/trading 路径。同步 payload 排除了 `.env`、`xuan_research_artifacts`、raw/replay、target 和生产路径；发现一个本地编辑器临时文件被带入后，已仅从隔离远端 worktree 删除并记录 cleanup。远端 `py_compile`、`bash -n`、entrypoint manifest 和 `/srv/pm_as_ofi/shared-ingress-main` broker preflight 均通过；远端 `cargo build --locked --bin xuan_b27_dplus_user_ws_observer` 在依赖拉取后通过，生成 binary SHA256 `a4b5430348880e7f4aba6a15d086c2ec9b631f1648f4da927f7d1f2a0562e82b`。artifact: `xuan_research_artifacts/xuan_b27_dplus_ec2_entrypoint_sync_20260516T233733Z/manifest.json`。`scripts/xuan_b27_dplus_local_status_bundle.py` 现在纳入 `latest_ec2_entrypoint_sync`，`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 验证 remote worktree、binary、broker preflight、entrypoint manifest、forbidden side effects。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260516T234756Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260516T234805Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260516T234756Z` 的 EC2 status 为 `READY_FOR_EXPLICIT_EC2_READONLY_USER_WS_APPROVAL`。没有启动 observer、User WS、broker、service/systemd、order/cancel/redeem 或 live 路径；下一 gate 是显式授权 exact EC2 read-only User WS observer run，使用该 synced remote worktree。
86. 主线程明确授权 exact EC2 read-only User WS observer run 后，已在同步 worktree 上以 shared-ingress client 接 `/srv/pm_as_ofi/shared-ingress-main` 执行 30m 只读观测。EC2 没有可用落盘 auth-source `.env`，因此本轮使用本地 `/Users/hot/web3Scientist/pm_as_ofi/.env` 中 allowlisted auth vars 经 SSH stdin 临时注入远端进程环境；未写远端 `.env`，未把 secret value 放入命令行或 artifact。远端 broker preflight `OK`，二进制 `run_manifest.json` 最终为 `PASS_READONLY_USER_WS_OBSERVER`，`duration_elapsed`，`orders_possible=false`，`fills_seen=0`，`recorder_critical_drop_count=0`；但 wrapper summary gate 使用 `--require-user-ws-records` 返回 3，因为 `user_ws_raw_count=0` 且 `user_ws_files=[]`。本地验收 artifact `xuan_research_artifacts/xuan_b27_dplus_ec2_readonly_user_ws_observer_20260517T000054Z/local_acceptance_review.json` 将真实状态修正为 `FAIL_NO_USER_WS_RECORDS`，同时记录 `forbidden_event_count=0`、event/user_ws decode error 均 0、broker/service/order/cancel/redeem side effects 均 false。远端 run artifacts 已通过 SSH `cat` 拉回同目录 `remote/` 下；该轮不能作为 read-only User WS 验收通过证据，下一步应先解释为什么 authenticated User WS 没有 raw records。
87. 本轮真实 EC2 run 暴露出 wrapper 返回码 bug：`wrapper_manifest.json` 已写 `FAIL_NO_USER_WS_RECORDS` 且 `summary_return_code=3`，但 wrapper 仍返回底层二进制 `proc.returncode=0`，导致一次性外层 driver 误标 `PASS_READONLY_USER_WS_OBSERVER`。已修复 `scripts/xuan_b27_dplus_readonly_user_ws_observer.py`：新增 `wrapper_exit_code()`，把 `FAIL_READONLY_VIOLATION` 映射为 2、`FAIL_NO_USER_WS_RECORDS` 映射为 3、`FAIL_SUMMARY_GATE` 映射为 summary rc/70、非零进程退出映射为非零。`scripts/xuan_b27_dplus_readonly_user_ws_static_smoke.sh` 现在直接导入该 helper 覆盖返回码映射。`scripts/xuan_b27_dplus_local_status_bundle.py` / smoke 也已纳入 `latest_ec2_readonly_user_ws_observer`，当前 EC2 status 为 `BLOCKED_EC2_READONLY_USER_WS_OBSERVER`，next gate 为检查 EC2 read-only User WS artifacts 后再决定是否复跑。最新验证：static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260517T003534Z` PASS，wrapper refusal smoke `xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke_20260517T003535Z` PASS，summary gate smoke `xuan_b27_dplus_readonly_user_ws_summary_gate_smoke_20260517T003535Z` PASS，`cargo test xuan_b27_dplus --lib` 12 passed，readiness `xuan_b27_dplus_auth_observer_readiness_20260517T003536Z` 为 `READY_FOR_APPROVAL`，bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260517T003707Z` PASS，latest bundle `xuan_b27_dplus_local_status_bundle_20260517T003708Z` 本地 verdict 仍为 `READY_FOR_APPROVAL_WAITING_FOR_SHARED_INGRESS_BROKER`，EC2 status 为 `BLOCKED_EC2_READONLY_USER_WS_OBSERVER`。
88. 已完成对 `FAIL_NO_USER_WS_RECORDS` 的本地 artifact postmortem，结论从“没有账号事件”收窄为“旧 EC2 binary 的 User WS 子任务在 TLS 路径 panic，主 observer 未感知”。拉回的 `logs__readonly_user_ws_stderr.log` 包含 rustls 0.23 `CryptoProvider` panic；对应 recorder 只有 observer start/end，没有任何 User WS lifecycle/raw 文件。新增本地 postmortem artifact `xuan_research_artifacts/xuan_b27_dplus_readonly_user_ws_postmortem_20260517T014511Z/manifest.json`，用新连接 gate 重跑旧 artifact summary，`summary_return_code=4`、`gate_status=FAIL_NO_USER_WS_CONNECTION`、`user_ws_connected_count=0`、`user_ws_subscribe_sent_count=0`、`user_ws_raw_count=0`。该 postmortem 只读已有本地 artifact，没有启动 observer/User WS/network/order/cancel/redeem。
89. 已修复 read-only User WS observer 的两个真实缺口：`src/bin/xuan_b27_dplus_user_ws_observer.rs` 在任何 reqwest/tungstenite TLS 路径前显式安装 `rustls::crypto::ring::default_provider()`，并用 `tokio::select!` 监控 `UserWsListener` 子任务；若子任务提前返回或 panic，run manifest 写 `FAIL_USER_WS_TASK_EXITED` 并返回失败，不再睡满 duration 后伪装 PASS。`src/polymarket/user_ws.rs` 增加无 secret lifecycle recorder events：listener started、connect attempt、connected、subscribe sent、text received、server error/closed/read error 等；同时去掉日志中的 API key 前缀输出。`scripts/xuan_b27_dplus_summarize_readonly_user_ws_run.py` 新增 `--require-user-ws-connection`、`gate_status/gate_failures/gate_requirements`、lifecycle/error counters；wrapper 改为要求连接+订阅 gate，连接缺失返回 4 (`FAIL_NO_USER_WS_CONNECTION`)，而不再要求 raw records 非空作为唯一验收条件。
90. 本轮修复后的本地 gate 已通过：`cargo fmt --check`、`cargo build --bin xuan_b27_dplus_user_ws_observer`、`cargo test xuan_b27_dplus --lib` 12 passed；summary gate smoke `xuan_b27_dplus_readonly_user_ws_summary_gate_smoke_20260517T014457Z` PASS，覆盖有 records、有连接但无 records、无连接、broker unavailable、forbidden order event、`orders_possible=true`、decode error；static smoke `xuan_b27_dplus_readonly_user_ws_static_smoke_20260517T015148Z` PASS，覆盖 rustls provider 安装、User WS task early-exit failure、lifecycle event recorder、wrapper exit code；wrapper refusal smoke `xuan_b27_dplus_readonly_user_ws_wrapper_refusal_smoke_20260517T015020Z` PASS；bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260517T015353Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260517T015403Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T015424Z` 记录 `latest_readonly_user_ws_postmortem.gate_status=FAIL_NO_USER_WS_CONNECTION`。远端 synced worktree 仍是旧 binary/code；任何新的 EC2 diagnostic/run 都需要先在隔离 xuan_research_runs worktree 重新同步/构建这些修复，并在当前对话里获得 exact run 授权。
91. 已新增下一次 EC2 read-only diagnostic 的本地 no-network 计划 artifact 和 smoke gate，避免授权后临场拼接 SSH/sync/run 步骤。`scripts/xuan_b27_dplus_ec2_resync_diagnostic_plan.py` 只读取本地 gate/旧 EC2 artifact/postmortem，不 SSH、不打包、不同步、不启动网络，输出 `READY_FOR_EXPLICIT_SYNC_AND_DIAGNOSTIC_APPROVAL` 计划：先把修复后的 entrypoint 重新同步/构建到新的隔离 `/home/ubuntu/xuan_research_runs/xuan_research_dplus_entrypoint_resync_<ts>/worktree`，禁止写 `/srv/pm_as_ofi`、shared-ingress、raw/replay、远端 `.env`，再在明确 exact approval 下以 `PM_SHARED_INGRESS_ROLE=client` 接 `/srv/pm_as_ofi/shared-ingress-main` 做 300s read-only diagnostic，验收 gate 为 `--check-readonly --require-user-ws-connection`。`scripts/xuan_b27_dplus_ec2_resync_diagnostic_plan_smoke.sh` 验证该计划不含网络/进程控制导入，side effects 全 false，且本地 gates、stale sync、postmortem rc=4 均成立。最新 plan smoke `xuan_b27_dplus_ec2_resync_diagnostic_plan_smoke_20260517T021434Z` PASS；readiness `xuan_b27_dplus_auth_observer_readiness_20260517T021441Z` 已纳入该 smoke；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T021441Z` 报告 `ec2_resync_diagnostic_plan_smoke.plan_status=READY_FOR_EXPLICIT_SYNC_AND_DIAGNOSTIC_APPROVAL`。该计划不是授权，也未启动 SSH/network/observer/User WS。
92. 主线程随后明确授权“重同步/重建修复后的 EC2 isolated worktree + exact EC2 read-only diagnostic run”。按计划重新同步到新的隔离 worktree `/home/ubuntu/xuan_research_runs/xuan_research_dplus_entrypoint_resync_20260517T022008Z/worktree`，只写 `/home/ubuntu/xuan_research_runs/xuan_research_*`，不修改 `/srv/pm_as_ofi`、shared-ingress、systemd/service、远端 `.env`、raw/replay 或 live/trading 路径。远端 `/srv/pm_as_ofi/shared-ingress-main` broker preflight 为 OK，entrypoint manifest 为 PASS，`cargo build --locked --bin xuan_b27_dplus_user_ws_observer` 用显式 cargo path 构建成功，修复后 observer binary SHA256 为 `26d9debb54d65ac9b9b9ffba9692563f50494b3c3070215c5fe5cad072031323`。本地摘要 artifact: `xuan_research_artifacts/xuan_b27_dplus_ec2_resync_rebuild_20260517T022008Z/manifest.json`，status=`PASS_RESYNC_REBUILD_READY`。该步骤未启动 observer/User WS，也未触碰 broker/service/order/cancel/redeem/env。
93. exact EC2 300s read-only diagnostic 已在上述修复后 worktree 执行，作为 shared-ingress client 接 `/srv/pm_as_ofi/shared-ingress-main`，market prefix 为 `btc-updown-5m`。本地 allowlist auth vars 仍只通过 SSH stdin 作为远端进程临时环境注入，未写远端 `.env`，未把 secret 放入命令行或 artifact。诊断 artifact: `xuan_research_artifacts/xuan_b27_dplus_ec2_readonly_user_ws_diagnostic_20260517T023152Z/local_acceptance_review.json`，status=`PASS_READONLY_USER_WS_DIAGNOSTIC`、`acceptance_passed=true`、wrapper/run/summary 均为 PASS、`gate_status=PASS`、`user_ws_connected_count=1`、`user_ws_subscribe_sent_count=1`、`user_ws_raw_count=60`、`user_ws_lifecycle_event_count=64`、`user_ws_error_event_count=0`、event/user_ws decode error 均 0、`orders_possible=false`、order/cancel/redeem/broker/service/systemd/shared-ingress/env write side effects 均 false。这个结果修复了旧 30m run 的 rustls provider / User WS task 监控缺口，但它是 300s diagnostic PASS，不等价于 30m acceptance 或 canary 授权。
94. 已新增 `scripts/xuan_b27_dplus_ec2_readonly_diagnostic_smoke.sh`，作为本地 no-network gate 验证最新 diagnostic acceptance review：必须 PASS、连接与订阅计数大于 0、有 raw User WS 记录、无 User WS error、无 decode error、无 forbidden event、无 order/cancel/redeem/broker/service/env side effects。latest smoke `xuan_b27_dplus_ec2_readonly_diagnostic_smoke_20260517T024940Z` PASS。`scripts/xuan_b27_dplus_local_status_bundle.py` 现在报告 `latest_ec2_resync_rebuild`、`latest_ec2_readonly_user_ws_diagnostic` 和 `ec2_readonly_diagnostic_smoke`，顶层 `ec2_readonly_user_ws_status=PASS_READONLY_USER_WS_DIAGNOSTIC`，`ec2_next_gate=review 300s EC2 readonly diagnostic artifacts before any 30m acceptance or canary discussion`。`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 和 `scripts/xuan_b27_dplus_auth_observer_readiness_check.sh` 已纳入该 smoke；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260517T024949Z` PASS，latest readiness `xuan_b27_dplus_auth_observer_readiness_20260517T024954Z` 为 `READY_FOR_APPROVAL`，latest bundle `xuan_b27_dplus_local_status_bundle_20260517T024959Z` 同时保留本机 broker gate verdict 与 EC2 diagnostic PASS 状态。`cargo test xuan_b27_dplus --lib` 复验仍为 12 passed。
95. 已新增 30m read-only acceptance 的本地 no-network 计划和 smoke gate，避免把 300s diagnostic PASS 直接误升为长时验收。`scripts/xuan_b27_dplus_ec2_30m_acceptance_plan.py` 只读取本地 PASS artifacts，不 SSH、不打包、不同步、不启动网络，要求输入包括 `PASS_READONLY_USER_WS_DIAGNOSTIC`、`PASS_RESYNC_REBUILD_READY`、diagnostic smoke PASS、readiness READY；输出 `READY_FOR_EXPLICIT_EC2_30M_ACCEPTANCE_APPROVAL`。计划固定 proposed run：远端 worktree `/home/ubuntu/xuan_research_runs/xuan_research_dplus_entrypoint_resync_20260517T022008Z/worktree`，输出目录在同一隔离 `xuan_research_runs` root 下，`duration_seconds=1800`，`market_slug=btc-updown-5m`，`PM_SHARED_INGRESS_ROLE=client` 接 `/srv/pm_as_ofi/shared-ingress-main`，wrapper 参数必须包含 `--approved-readonly-user-ws`，验收 gate 为 read-only 安全、`--require-user-ws-connection`、连接/订阅计数大于 0、User WS/error/decode/forbidden/order/cancel/redeem/critical-drop 全为 0。该计划仍需要主线程对 exact EC2 30m read-only acceptance run 的明确授权。
96. `scripts/xuan_b27_dplus_ec2_30m_acceptance_plan_smoke.sh` 已验证上述计划的本地安全边界：脚本无网络/进程控制/归档导入，所有 side effects 为 false，remote worktree/run dir 只在 `/home/ubuntu/xuan_research_runs/xuan_research_*` 下，shared-ingress root 固定为 `/srv/pm_as_ofi/shared-ingress-main`，且 plan status 为 `READY_FOR_EXPLICIT_EC2_30M_ACCEPTANCE_APPROVAL`。latest plan smoke `xuan_b27_dplus_ec2_30m_acceptance_plan_smoke_20260517T030049Z` PASS。`scripts/xuan_b27_dplus_local_status_bundle.py` 现在报告 `ec2_30m_acceptance_plan_smoke`；`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 与 `scripts/xuan_b27_dplus_auth_observer_readiness_check.sh` 已纳入该 gate，并将 readiness 的 30m plan smoke 选择改为按 `manifest.json` 文件选最新，避免并发目录竞态。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260517T030202Z` PASS，latest readiness `xuan_b27_dplus_auth_observer_readiness_20260517T030202Z` 为 `READY_FOR_APPROVAL`，latest bundle `xuan_b27_dplus_local_status_bundle_20260517T030203Z` 记录 30m plan status 与 EC2 diagnostic PASS。
97. 主线程明确授权 exact EC2 30m read-only acceptance run 后，已使用固定 remote worktree `/home/ubuntu/xuan_research_runs/xuan_research_dplus_entrypoint_resync_20260517T022008Z/worktree`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main`、duration 1800s、market_slug `btc-updown-5m` 执行。第一次启动在 observer 前 fail-closed：非 login shell 找不到 `cargo`，artifact `xuan_research_artifacts/xuan_b27_dplus_ec2_readonly_user_ws_acceptance_20260517T031331Z/manifest.json` 记录 `FAILED_CLOSED_REMOTE_CARGO_NOT_IN_PATH`，未启动 observer/User WS/order/cancel/redeem/broker/service/env。随后同一 exact 授权范围内补 `PATH=/home/ubuntu/.cargo/bin:$PATH` 重试；SSH transport 中途被 remote host 关闭，但远端 wrapper/observer 继续运行。只读远端检查确认单个 authorized process 仍在运行，未启动第二个 observer。
98. 30m acceptance 最终通过。远端 run dir `/home/ubuntu/xuan_research_runs/xuan_research_dplus_entrypoint_resync_20260517T022008Z/acceptance_run_20260517T031459Z`，本地 artifacts 已拉回 `xuan_research_artifacts/xuan_b27_dplus_ec2_readonly_user_ws_acceptance_20260517T031459Z/remote/`。本地验收 `local_acceptance_review.json` 报告 `status=PASS_READONLY_USER_WS_ACCEPTANCE`、`acceptance_passed=true`、wrapper/run/summary 均 PASS、`gate_status=PASS`、`duration_secs=1800`、`elapsed_ms=1800608`、`shared_ingress_status=OK`、`broker_preflight_status=OK`、`user_ws_connected_count=1`、`user_ws_subscribe_sent_count=1`、`user_ws_raw_count=360`、`user_ws_lifecycle_event_count=364`、User WS error/decode error/event decode error/forbidden event 均 0、`orders_possible=false`、`recorder_critical_drop_count=0`、fills 0。secret 扫描未发现本地 allowlist secret 值写入拉回 artifacts；order/cancel/redeem/broker/service/systemd/shared-ingress/env write side effects 全 false。
99. 已新增 `scripts/xuan_b27_dplus_ec2_30m_acceptance_smoke.sh`，作为本地 no-network gate 验证最新 30m acceptance review。该 smoke 要求 PASS、1800s、连接/订阅/raw 记录存在、无 User WS error、无 decode error、无 forbidden event、无 order/cancel/redeem/broker/service/env side effects。latest smoke `xuan_b27_dplus_ec2_30m_acceptance_smoke_20260517T035104Z` PASS。`scripts/xuan_b27_dplus_local_status_bundle.py` 现在报告 `latest_ec2_readonly_user_ws_acceptance` 和 `ec2_30m_acceptance_smoke`，顶层 `ec2_readonly_user_ws_status=PASS_READONLY_USER_WS_ACCEPTANCE`，`ec2_next_gate=review 30m EC2 readonly acceptance artifacts before any canary discussion`。latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260517T035121Z` PASS，latest readiness `xuan_b27_dplus_auth_observer_readiness_20260517T035121Z` 为 `READY_FOR_APPROVAL`，latest bundle `xuan_b27_dplus_local_status_bundle_20260517T035123Z` 记录 30m acceptance PASS。`cargo test xuan_b27_dplus --lib` 复验仍为 12 passed。
100. 已新增 canary readiness 的本地 no-network 判定 artifact，防止把 30m read-only acceptance 误升为 canary 许可。`scripts/xuan_b27_dplus_canary_readiness_plan.py` 只读取本地 artifacts 和代码静态信号，不 SSH、不启动 observer/User WS、不触碰 broker、不下单；它确认 read-only acceptance、source-of-truth schema smoke、observer safety smoke、本地 readiness 均通过，但当前 `src/polymarket/strategy/xuan_b27_dplus.rs` 仍返回 `StrategyQuotes::default()`，observer payload 仍写 `quotes_forced_empty=true`、`order_controller_implemented=false`，Canary 模式事件仍为 `xuan_b27_dplus_canary_blocked_not_implemented`。因此计划状态为 `CANARY_NOT_READY_ORDER_CONTROLLER_NOT_IMPLEMENTED`，next gate 是先实现并本地 gate 一个 canary-safe order-intent/controller plan，再谈 exact G2 canary approval。latest plan smoke `xuan_b27_dplus_canary_readiness_plan_smoke_20260517T041500Z` PASS；latest bundle smoke `xuan_b27_dplus_local_status_bundle_smoke_20260517T041512Z` PASS；latest readiness `xuan_b27_dplus_auth_observer_readiness_20260517T041512Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T041512Z` 同时报告 `ec2_readonly_user_ws_status=PASS_READONLY_USER_WS_ACCEPTANCE` 和 `canary_readiness_status=CANARY_NOT_READY_ORDER_CONTROLLER_NOT_IMPLEMENTED`。
101. 已把 G2 canary 前的 order-intent surface 推进到本地 preview-only planner，但仍未接执行 controller。新增 `src/polymarket/xuan_b27_dplus_order_plan.rs`：在 `XuanB27DplusMode::Canary`、显式 canary approval、account truth ready、单市场、`max_live_orders<=2`、`target_qty<=5`、`max_open_cost_usdc<=100`、`post_only=true`、`allow_passive_taker=false` 等条件同时满足时，生成 YES/NO passive BUY `XuanB27DplusOrderIntentPreview`，价格为 ask-edge 向 tick 下取整，带 `OrderAttemptTrace`，且强制 `preview_only=true`、`submitted=false`。该模块不导入 `OrderManagerCmd`/`Executor`，不产生 `SetTarget`，不调用 order/cancel/redeem。新增 `scripts/xuan_b27_dplus_canary_order_plan_smoke.sh` 验证静态安全和 4 个 Rust 单测：preview-only 双边 passive buys、缺显式 approval 阻断、account truth UNKNOWN 阻断、G2 cap 阻断。latest order-plan smoke `xuan_b27_dplus_canary_order_plan_smoke_20260517T042932Z` PASS；latest canary-readiness smoke `xuan_b27_dplus_canary_readiness_plan_smoke_20260517T042948Z` PASS；latest readiness `xuan_b27_dplus_auth_observer_readiness_20260517T042958Z` 为 `READY_FOR_APPROVAL`；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T043011Z` 报告 `canary_readiness_status=CANARY_NOT_READY_CONTROLLER_NOT_WIRED`，next gate 为把 preview-only planner 接到 exact-approval-gated execution controller。`cargo test xuan_b27_dplus --lib` 现在 16 passed。
102. 已新增 D+ execution-controller decision 层，但仍没有 OMS adapter。`src/polymarket/xuan_b27_dplus_execution_controller.rs` 调用 preview-only planner，产出 `XuanB27DplusControllerDecision`：当 planner ready 但 controller disabled / OMS adapter missing 时，保留 preview intents，状态为 `PREVIEW_HELD_CONTROLLER_DISABLED`，并始终记录 `oms_command_count=0`、`orders_submitted=false`。缺显式 approval、account truth UNKNOWN、G2 risk cap 违规时直接 `BLOCKED` 且 preview intents 为空。该模块不导入 `OrderManagerCmd`/`Executor`，不产生 `SetTarget`，不调用 order/cancel/redeem。新增 `scripts/xuan_b27_dplus_execution_controller_smoke.sh` 验证模块注册、调用 planner、disabled 状态、OMS adapter missing block、无 OMS/executor 调用，并运行 controller 单测。latest controller smoke `xuan_b27_dplus_execution_controller_smoke_20260517T043845Z` PASS；latest canary-readiness smoke `xuan_b27_dplus_canary_readiness_plan_smoke_20260517T043859Z` PASS；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T043914Z` 报告 `canary_readiness_status=CANARY_NOT_READY_OMS_ADAPTER_NOT_IMPLEMENTED`，next gate 为实现 exact-approval-gated OMS adapter，并加本地 gate 证明无显式 approval / account truth / stop-on-unknown / risk cap 时不能发命令。`cargo test xuan_b27_dplus --lib` 现在 20 passed。
103. 已新增 D+ OMS adapter surface，但仍未接入 runtime sender。`src/polymarket/xuan_b27_dplus_oms_adapter.rs` 只在 controller 状态为 `PREVIEW_READY_FOR_OMS_ADAPTER`、显式 canary approval、account truth ready、stop-on-unknown 未触发、risk caps ready、adapter enabled 且 preview intents 仍为 post-only/no-taker/未提交时，把 preview intent 转成本地 `OrderManagerCmd::SetTargetWithTrace` 值；它不持有 `mpsc::Sender`、不调用 executor、不发送 order/cancel/redeem。缺 approval、account truth UNKNOWN、risk cap 不 ready 或 adapter disabled 时 command count 为 0，且 `orders_submitted=false`。新增 `scripts/xuan_b27_dplus_oms_adapter_smoke.sh` 验证静态边界和 5 个 adapter 单测；readiness、canary readiness plan 和 local status bundle 已纳入该 smoke。latest adapter smoke `xuan_b27_dplus_oms_adapter_smoke_20260517T050754Z` PASS；latest canary-readiness smoke `xuan_b27_dplus_canary_readiness_plan_smoke_20260517T050809Z` PASS；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T050819Z` 报告 `canary_readiness_status=CANARY_NOT_READY_RUNTIME_WIRING_NOT_IMPLEMENTED`，next gate 为把 exact-approval-gated controller/OMS adapter 接入 runtime，并加 no-command emission 集成 gate。未启动网络、observer、User WS、canary、broker、order/cancel/redeem。
104. 已新增 D+ runtime wiring gate，但 account-truth runtime gate 仍未接真实 source-of-truth。`StrategyCoordinator::maybe_dispatch_xuan_b27_dplus_canary_commands` 已在 D+ tick loop 中接入：它调用 planner/controller/OMS adapter，只有 `PM_XUAN_B27_DPLUS_EXPLICIT_CANARY_APPROVAL=true`、`PM_XUAN_B27_DPLUS_RUNTIME_WIRING_ENABLED=true`、`PM_XUAN_B27_DPLUS_OMS_ADAPTER_ENABLED=true`、代码内 `account_truth_ready=true`、stop-on-unknown 未触发、G2 caps ready 时，才会向本地 OMS channel 发送 `SetTargetWithTrace`。默认配置全部关闭，`account_truth_ready` 当前不能通过 env 覆盖；缺 approval、缺 account truth、risk cap 失败时新增 coordinator tests 均验证 receiver 收不到任何命令。新增 `scripts/xuan_b27_dplus_runtime_wiring_smoke.sh` 验证 runtime method、tick-loop 调用、controller/adapter 接入、无网络/broker/executor 调用、account truth 不可 env 覆盖和 4 个 no-command/emission 单测。latest runtime wiring smoke `xuan_b27_dplus_runtime_wiring_smoke_20260517T053855Z` PASS；latest canary-readiness smoke `xuan_b27_dplus_canary_readiness_plan_smoke_20260517T053908Z` PASS；latest bundle `xuan_b27_dplus_local_status_bundle_20260517T053915Z` 报告 `canary_readiness_status=CANARY_NOT_READY_SOURCE_TRUTH_RUNTIME_NOT_READY`，next gate 为把自有 order/fill/redeem/cashflow truth 接入 runtime account-truth gate。未启动网络、observer、User WS、canary、broker、order/cancel/redeem。
105. 已把 D+ runtime account-truth gate 改成 typed source-truth 状态门。新增 `XuanB27DplusRuntimeSourceTruth` / `XuanB27DplusRuntimeTruthStatus`，默认 `order_truth / fill_truth / wallet_truth / redeem_truth / cashflow_truth` 全为 `UNKNOWN`；只有这五项全部 `PASS`，且代码内 `account_truth_ready=true`、显式 canary approval、runtime wiring、OMS adapter、stop-on-unknown、G2 risk caps 全部通过时，D+ runtime 才会把 adapter command 发到本地 OMS channel。runtime recorder event `xuan_b27_dplus_canary_runtime_gate` 现在记录无 secret 的 source-truth status、missing components 和 failed components。新增 coordinator tests 覆盖默认 UNKNOWN、全 UNKNOWN、wallet UNKNOWN、cashflow FAIL 均不发送 OMS；all-gates-open 仍只用本地 in-memory channel，不提交订单。
106. 已新增 `scripts/xuan_b27_dplus_source_truth_runtime_gate_smoke.sh`，验证 source-truth runtime gate 类型、默认 UNKNOWN、all-pass 构造器、coordinator 持有状态、runtime dispatch 使用 `cfg.account_truth_ready && runtime_source_truth_ready`、runtime event 输出 missing/failed components、source-truth 状态不可 env 覆盖，并运行 `cargo test xuan_b27_dplus_runtime --lib`。canary readiness plan、local status bundle 和 readiness checker 已纳入该 smoke。当前状态应从 `CANARY_NOT_READY_SOURCE_TRUTH_RUNTIME_NOT_READY` 推进为 `CANARY_NOT_READY_SOURCE_TRUTH_FEEDS_NOT_WIRED`：source-truth gate 已接入 runtime，但真实自有 order/fill/wallet/redeem/cashflow event feeds 仍未把状态升级为 PASS。下一 gate 是接入这些 feeds，并证明任一 UNKNOWN/FAIL 继续 stop-on-unknown 且不发命令。
107. 已把 D+ order/fill source-truth event feed 接到 runtime gate，但 wallet/redeem/cashflow producers 仍故意保持未接入。新增 `XuanB27DplusSourceTruthEvent`，`Executor` 在 venue order accepted 后通过 no-secret channel 发送 `OrderAccepted`，authenticated `UserWsListener` 在 `user_ws_fill_parsed` 时发送 `UserWsFillParsed`；`polymarket_v2` 只在 `StrategyKind::XuanB27Dplus` 下把该 channel 接到 executor、User WS 和 `StrategyCoordinator`。coordinator handler 只对 D+ 生效：完整 order/fill 事件可把 `order_truth/fill_truth` 推到 `PASS`，缺 trade id 仍为 `UNKNOWN`，非 D+ 策略忽略该 feed。最新 smoke `xuan_research_artifacts/xuan_b27_dplus_source_truth_runtime_gate_smoke_20260517T064504Z/manifest.json` PASS，明确记录 `event_feeds_partially_wired=true` 且 `wallet_redeem_cashflow_runtime_producers_wired=false`；canary readiness plan `xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T064514Z/plan_run/manifest.json` 记录 `order_fill_source_truth_event_feeds_ready=true`、`wallet_redeem_cashflow_event_feeds_ready=false`，状态仍为 `CANARY_NOT_READY_SOURCE_TRUTH_FEEDS_NOT_WIRED`。本轮验证 `cargo test xuan_b27_dplus --lib` 37 passed、`cargo fmt --check`、`git diff --check`、local status bundle smoke 和 readiness 均通过。未启动 SSH、observer、User WS、canary、broker、order/cancel/redeem 或任何网络进程。
108. 已把 wallet/cashflow/redeem runtime producer 接到真实路径，但没有把不完整 redeem 误提升为 PASS。`Executor::cached_free_balance_usdc` 现在在真实 CLOB collateral balance 预检成功/失败时发送 no-secret `WalletSnapshot`；`polymarket_v2` 的 capital recycler 在真实 collateral status / post-merge balance refresh 路径发送 `WalletSnapshot` 和 `CashflowSnapshot`；round-claim recorder 路径发送 `RedeemResult` attempt feed，但当前 `AutoClaimRunResult` 没有 tx hash 字段，所以该 producer 只能把 `redeem_truth` 保持 `UNKNOWN`，不能 PASS。新增 coordinator tests 覆盖 wallet invalid => FAIL、redeem missing tx hash => UNKNOWN、cashflow missing id => UNKNOWN，均不会发送 OMS 命令。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_source_truth_runtime_gate_smoke_20260517T071001Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T071002Z/plan_run/manifest.json` 仍为 `CANARY_NOT_READY_SOURCE_TRUTH_FEEDS_NOT_WIRED`，其中 `wallet_runtime_producer_wired=true`、`redeem_runtime_producer_wired=true`、`cashflow_runtime_producer_wired=true`、`redeem_truth_runtime_confirmation_ready=false`、`wallet_redeem_cashflow_event_feeds_ready=false`。最新 bundle/readiness：`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T071031Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T071030Z/manifest.json` READY_FOR_APPROVAL，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260517T071036Z/manifest.json`。`cargo test xuan_b27_dplus --lib` 40 passed；`cargo fmt --check` 和 `git diff --check` 通过。未启动 SSH、observer、User WS、canary、broker、order/cancel/redeem 或任何网络进程。下一 gate 是让 redeem/claim 执行结果暴露 tx hash 或等价确认 id，再要求 stable all-PASS source truth。
109. 已把 redeem/claim 执行结果升级为结构化 confirmation evidence，并接入 runtime source-truth。`AutoClaimRunResult` 现在保留 `redeem_evidence: Vec<RedeemExecutionEvidence>`；EOA onchain claim 成功会记录 `tx_hash` 且 `confirmed=true`，SAFE relayer claim 会记录 `relayer_tx_id`，只有 wait-confirm 成功才 `confirmed=true`；未确认的 SAFE submit 仍不能把 `redeem_truth` 推到 PASS。`XuanB27DplusSourceTruthEvent::RedeemResult` 新增 `has_redeem_confirmation`，coordinator 现在接受 `redeem_attempt_id && (tx_hash || confirmed relayer id)` 作为 redeem PASS。round-claim recorder payload 也写入 no-secret `redeem_evidence`、`has_redeem_tx_hash` 和 `has_redeem_confirmation`。新增测试覆盖 EOA tx hash、SAFE confirmed tx id、SAFE unconfirmed submit，以及 confirmed SAFE id 无 tx hash 也可打开 runtime gate。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_source_truth_runtime_gate_smoke_20260517T073533Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T073533Z/plan_run/manifest.json` 报告 `CANARY_NOT_READY_G2_CANARY_RUNBOOK_NOT_READY`，其中 `wallet_redeem_cashflow_event_feeds_ready=true`、`redeem_truth_runtime_confirmation_ready=true`、`requires_source_truth_event_feeds=false`、`requires_exact_g2_canary_runbook=true`。latest bundle/readiness：`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T073636Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T073649Z/manifest.json` READY_FOR_APPROVAL，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260517T073641Z/manifest.json`。`cargo test xuan_b27_dplus --lib` 44 passed；`cargo fmt --check` 和 `git diff --check` 通过。未启动 SSH、observer、User WS、canary、broker、order/cancel/redeem 或任何网络进程。下一 gate 是 exact G2 canary runbook 和本地 canary acceptance smoke，而不是直接网络 canary。
110. 已新增 exact G2 canary runbook 和本地 no-network canary acceptance fixture smoke，并把 canary readiness 从 `CANARY_NOT_READY_G2_CANARY_RUNBOOK_NOT_READY` 推进为 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL`。新增 `docs/research/xuan/DPLUS_G2_CANARY_RUNBOOK_ZH.md` 明确：文档不是授权；G2 只能在 exact approval 中指定 `xuan_b27_dplus`、`btc-updown-5m`、`PM_SHARED_INGRESS_ROLE=client`、`/srv/pm_as_ofi/shared-ingress-main`、`max_live_orders=2`、`target_qty=5`、`max_open_cost_usdc=50`、`max_strategy_exposure_usdc=100`、post-only/no-taker/stop-on-unknown，以及只允许 capped post-only order/cancel side effects。新增 `scripts/xuan_b27_dplus_g2_canary_runbook_smoke.sh` 验证 runbook、30m read-only acceptance、runtime/source-truth/OMS gates；新增 `scripts/xuan_b27_dplus_g2_canary_acceptance_smoke.sh` 用本地 fixtures 覆盖 PASS、missing approval、risk cap 超限、source truth fail、unexpected taker fill、recorder critical drop、shared-ingress 错误和 secret 记录失败。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_runbook_smoke_20260517T075030Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_acceptance_smoke_20260517T075030Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T075030Z/plan_run/manifest.json` 报告 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL` 且 `canary_run_authorized=false`，`g2_canary_runbook_ready=true`，`g2_canary_acceptance_smoke_ready=true`。latest bundle/readiness：`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T075030Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T075031Z/manifest.json` READY_FOR_APPROVAL，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260517T075032Z/manifest.json` 记录 `canary_readiness_status=READY_FOR_EXPLICIT_G2_CANARY_APPROVAL`。`cargo fmt --check`、`git diff --check`、`cargo test xuan_b27_dplus --lib` 44 passed。未启动 SSH、observer、User WS、canary、broker、order/cancel/redeem 或任何网络进程；下一 gate 是主线程明确授权 exact G2 canary run。
111. 已新增 exact G2 canary launch/review plan artifact 和 smoke gate，把“下一次真跑”拆成明确的 sync/rebuild + run 审批包。新增 `scripts/xuan_b27_dplus_g2_canary_launch_plan.py` 只读取本地 artifacts，不 SSH、不打包、不同步、不构建、不启动网络；输出 `READY_FOR_EXPLICIT_G2_CANARY_SYNC_AND_RUN_APPROVAL`，要求当前 canary readiness、G2 runbook、G2 acceptance fixture、30m read-only acceptance、source-truth runtime gate 全部 PASS，并明确 `canary_run_authorized=false`。计划建议先把当前本地代码同步/构建到新的隔离 `/home/ubuntu/xuan_research_runs/xuan_research_dplus_g2_canary_<ts>/worktree`，再在 exact approval 下运行 `G2_READ_WRITE_CANARY_SMOKE`：`duration_seconds=1800`、`max_rounds=6`、`market_slug=btc-updown-5m`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main`、`max_live_orders=2`、`max_open_cost_usdc=50`、`max_strategy_exposure_usdc=100`、post-only/no-taker/stop-on-unknown。新增 `scripts/xuan_b27_dplus_g2_canary_launch_plan_smoke.sh` 验证该计划无网络/进程控制/归档导入，side effects 全 false，allowed side effects 仅限 exact approval 后 capped post-only orders 与 bounded own cancels，redeem/claim 仍为 false。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_launch_plan_smoke_20260517T081547Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T081548Z/plan_run/manifest.json` 报告 `g2_canary_launch_plan_ready=true`、`requires_g2_canary_launch_plan=false`，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260517T081550Z/manifest.json` 仍记录 `canary_readiness_status=READY_FOR_EXPLICIT_G2_CANARY_APPROVAL`。本轮仍未启动 SSH、observer、User WS、canary、broker、order/cancel/redeem 或任何网络进程。
112. 已新增 G2 canary post-run review summarizer 和本地 fixture smoke，把未来 canary 验收从“人工看 artifact”推进为机器 gate。新增 `scripts/xuan_b27_dplus_summarize_g2_canary_run.py`，只读取本地 run dir 中的 `g2_canary_acceptance_review.json` / `local_acceptance_review.json`、run manifest 和 recorder `events.jsonl`；它要求 exact approval scope、D+ strategy、BTC 5m market、shared-ingress client/root、source-truth all-PASS、order ack/trace/venue id linkage、User WS connected/subscribed、decode error 0、recorder critical drop 0、unexpected taker fill 0、G2 risk caps、broker/service/shared-ingress/env/secret side effects 全 false，并可用 caller-provided sentinel 做 secret scan。新增 `scripts/xuan_b27_dplus_g2_canary_review_smoke.sh` 用本地 fixtures 覆盖 PASS、missing approval、cap violation、source-truth fail、unexpected taker fill、forbidden redeem event、secret sentinel leak 和 decode error。canary readiness plan、local status bundle、readiness checker 均已纳入该 smoke；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_review_smoke_20260517T084842Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T084851Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T084852Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T084859Z/manifest.json` READY_FOR_APPROVAL。该 gate 不 SSH、不启动 observer/User WS/canary/broker，不发送 order/cancel/redeem；下一 gate 仍是主线程明确授权 exact G2 canary sync/rebuild + run。
113. 已新增 G2 canary structured approval-envelope verifier 和 fixture smoke，避免 launcher 把泛授权当成 exact canary 授权。新增 `scripts/xuan_b27_dplus_g2_canary_approval_envelope.py`，只验证本地 JSON envelope：必须是 `exact_g2_canary_sync_rebuild_and_run`、`explicit_current_conversation_approval=true`、`heartbeat_or_generic_approval=false`、`G2_READ_WRITE_CANARY_SMOKE`、`xuan_b27_dplus`、`btc-updown-5m`、shared-ingress client `/srv/pm_as_ofi/shared-ingress-main`、远端路径限制在 `/home/ubuntu/xuan_research_runs/xuan_research_*`、duration 1800、max rounds 6、target qty 5、live orders 2、open cost 50、strategy exposure 100、post-only/no-taker/stop-on-unknown，并明确 redeem/claim、broker/service/shared-ingress/env write 仍禁止。新增 `scripts/xuan_b27_dplus_g2_canary_approval_envelope_smoke.sh` 覆盖 PASS、generic approval、wrong market、cap violation、bad remote path、redeem allowed、env write allowed、missing post-run secret scan 等 fixtures。canary readiness plan、local status bundle、readiness checker 已纳入该 smoke；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_approval_envelope_smoke_20260517T091349Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T091357Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T091357Z/manifest.json` PASS。该 gate 不解析聊天、不 SSH、不启动 canary、不发送 order/cancel/redeem；未来 exact run 仍需要主线程明确授权并生成可验证 envelope。
114. 已新增 G2 canary refusal-only launcher preflight 和 smoke，确保真实 sync/run 路径未实现前不会被 heartbeat 或 envelope 误触发。新增 `scripts/xuan_b27_dplus_g2_canary_launcher_refusal.py`：缺 `--approved-g2-canary-sync-and-run` 返回 64，缺 `--approval-envelope` 返回 64，invalid envelope 返回 65；即使 envelope 通过，也只写 `REFUSED_EXECUTION_PATH_NOT_IMPLEMENTED` manifest 并返回 75，且 side effects 全 false。新增 `scripts/xuan_b27_dplus_g2_canary_launcher_refusal_smoke.sh` 覆盖 missing flag、missing envelope、invalid market envelope、valid envelope still refuses execution，并验证 manifest 中 SSH/network/sync/build/canary/order/cancel/redeem/broker/service/shared-ingress/env side effects 全 false。canary readiness plan、local status bundle、readiness checker 已纳入该 smoke；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_launcher_refusal_smoke_20260517T094334Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T094344Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T094344Z/manifest.json` PASS。下一 gate 若要进入真实 G2 canary，必须新增单独 reviewed exact-run executor，并继续保留该 fail-closed 合约。
115. 已新增 G2 canary exact-run executor contract artifact 和 smoke，把下一步从 refusal-only launcher 明确推进为“实现并审查符合合同的真实 executor”，仍不启动任何网络或订单路径。新增 `scripts/xuan_b27_dplus_g2_canary_executor_contract.py`，只读取本地 readiness/status/runbook/approval/review/launcher smokes，输出 `READY_FOR_REVIEWED_G2_CANARY_EXECUTOR_IMPLEMENTATION`；合同列出未来 executor 必须执行的阶段：加载结构化 approval envelope、验证当前本地 readiness、创建隔离 `/home/ubuntu/xuan_research_runs/xuan_research_*` 远端目录、只同步 allowlisted source、构建声明目标、先跑 read-only shared-ingress preflight、仅通过临时环境注入 auth vars、启动单个 bounded G2 canary、按 duration/round limit 停止、拉回 artifacts、运行本地 post-run review 与 secret sentinel scan、写本地 acceptance review。合同同时规定 mandatory abort：approval envelope 无效、本地 readiness 非 ready、远端路径越界、broker preflight 非 OK、source truth UNKNOWN/FAIL、risk cap/stop-on-unknown 违规、unexpected taker fill、recorder decode/critical drop、secret leak、任意 forbidden side effect。新增 `scripts/xuan_b27_dplus_g2_canary_executor_contract_smoke.sh` 验证该合同本身无网络/进程/归档导入、side effects 全 false，并已接入 canary readiness plan、local status bundle、auth readiness。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_contract_smoke_20260517T101935Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T101935Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T101935Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T101936Z/manifest.json` READY_FOR_APPROVAL。真实 G2 canary executor 仍未实现；heartbeat/泛授权仍不能启动 canary/order/cancel/redeem。
116. 已新增 G2 canary executor dry-run skeleton 和 smoke，把 contract 转成更接近真实 executor 的阶段化本地计划，但仍在任何 SSH/sync/build/run/order 路径前拒绝。新增 `scripts/xuan_b27_dplus_g2_canary_executor_dry_run.py`：缺 `--approved-g2-canary-sync-and-run` 返回 64，缺 `--approval-envelope` 返回 64，invalid envelope 返回 65；valid envelope + 最新 executor contract ready 时生成 `phase_plan`，每个 effectful phase（创建远端 root、同步、构建、shared-ingress preflight、auth env 注入、启动 bounded canary、等待/停止、拉回 artifacts）均标记 `REFUSED_BEFORE_SIDE_EFFECT`，最终返回 75 `REFUSED_DRY_RUN_ONLY_EXECUTOR`。新增 `scripts/xuan_b27_dplus_g2_canary_executor_dry_run_smoke.sh` 覆盖 missing flag、missing envelope、bad remote path envelope、valid envelope dry-run refusal，并验证 contract/status/envelope、remote path isolation、phase plan、mandatory aborts、allowed/forbidden side effects 和所有当前 side effects false。该 smoke 已纳入 canary readiness plan、local status bundle、auth readiness；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_dry_run_smoke_20260517T104239Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T104435Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T104436Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T104436Z/manifest.json` READY_FOR_APPROVAL。真实 executor 仍未实现；这个 skeleton 是实现前的 no-network stage model，不是 canary launcher。
117. 已新增 G2 canary executor payload allowlist manifest 和 smoke，把 future executor 的 `sync_allowlisted_source_files_only` 阶段变成机器可审计输入。新增 `scripts/xuan_b27_dplus_g2_canary_executor_payload_manifest.py`，只读取本地文件并记录核心 Rust source、executor gate scripts 和 review docs 的 size/hash/exec/shebang 信息；明确排除 `.env`、`xuan_research_artifacts/`、`target/`、`.git/`、raw/replay、`/mnt/poly-replay`、`/srv/pm_as_ofi` 等路径，不打包、不同步、不部署、不 SSH、不构建、不启动网络。新增 `scripts/xuan_b27_dplus_g2_canary_executor_payload_manifest_smoke.sh` 验证 payload 文件数、必需文件集合、sha256、脚本 executable/shebang、无 forbidden path、无 duplicate path、side effects 全 false，并已接入 canary readiness plan、local status bundle、auth readiness。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_payload_manifest_smoke_20260517T111159Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T111351Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T111352Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_auth_observer_readiness_20260517T111352Z/manifest.json` READY_FOR_APPROVAL。真实 executor 仍未实现；下一步是让 executor implementation 只使用这个 allowlist。
118. 已把 payload allowlist 从旁路 smoke 升级为 executor contract 与 dry-run skeleton 的强依赖。`scripts/xuan_b27_dplus_g2_canary_executor_payload_manifest.py` 现在把自身和 smoke 纳入 executor gate payload，使未来隔离 worktree 能携带同一套 allowlist 审查工具；最新 payload manifest 文件数为 52。`scripts/xuan_b27_dplus_g2_canary_executor_contract.py` 现在要求 latest payload manifest smoke PASS、payload manifest PASS、missing/forbidden/duplicate/executable 问题全空，并在 required phases 中加入 `load_reviewed_payload_allowlist_manifest`，mandatory abort 中加入 `payload_allowlist_missing_or_invalid`。`scripts/xuan_b27_dplus_g2_canary_executor_dry_run.py` 现在 valid envelope + contract ready 后还必须验证 payload allowlist；若 allowlist 不安全则返回 67 `REFUSED_PAYLOAD_ALLOWLIST_NOT_READY`，否则继续返回 75 `REFUSED_DRY_RUN_ONLY_EXECUTOR`，且 manifest 记录 payload file count、forbidden hits、source smoke 等字段。新增 smoke 断言 contract/dry-run 均真正消费 payload allowlist。初次验证 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_payload_manifest_smoke_20260517T114152Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_contract_smoke_20260517T114158Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_dry_run_smoke_20260517T114158Z/manifest.json` PASS。仍未启动 SSH、sync、build、broker、canary、order/cancel/redeem 或任何网络路径。
119. 已新增 executor payload allowlist drift gate，避免未来 executor 使用过期 hash 清单同步已变化的代码。`scripts/xuan_b27_dplus_g2_canary_executor_contract.py` 和 `scripts/xuan_b27_dplus_g2_canary_executor_dry_run.py` 现在会重新计算 payload records 的 `size_bytes`、`sha256`、`mode_octal`、`is_executable`、`has_shebang`，并写入 `payload_current_check`；若发现漂移，contract 输入不 ready，dry-run 返回 68 `REFUSED_PAYLOAD_ALLOWLIST_DRIFT`。`scripts/xuan_b27_dplus_g2_canary_executor_dry_run.py` 还支持 `--payload-manifest`，用于 fixture/审查指定清单。`scripts/xuan_b27_dplus_g2_canary_executor_dry_run_smoke.sh` 新增 stale payload fixture，把 `Cargo.toml` hash 改成全 0，验证 dry-run 在任何 side effect 前拒绝且记录 `sha256_mismatch`。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_payload_manifest_smoke_20260517T121338Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_contract_smoke_20260517T121344Z/manifest.json` PASS 且 `payload_current_check.status=PASS`，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_dry_run_smoke_20260517T121344Z/manifest.json` PASS，stale fixture manifest 状态为 `REFUSED_PAYLOAD_ALLOWLIST_DRIFT`。仍未启动 SSH、sync、build、broker、canary、order/cancel/redeem 或任何网络路径。
120. 已新增 refusal-first G2 canary executor preflight entrypoint，把“真实 executor 未实现”收窄为单一受测入口。新增 `scripts/xuan_b27_dplus_g2_canary_executor.py`：缺 `--approved-g2-canary-sync-and-run` 返回 64，缺 `--approval-envelope` 返回 64，invalid envelope 返回 65，contract not ready 返回 66，payload invalid 返回 67，payload drift 返回 68；valid envelope + contract + current payload allowlist 时返回 75 `REFUSED_EFFECTFUL_EXECUTOR_NOT_IMPLEMENTED`，并把第一个未实现 effectful phase 记录为 `create_isolated_remote_run_root_under_xuan_research_runs`。新增 `scripts/xuan_b27_dplus_g2_canary_executor_smoke.sh` 覆盖 missing flag、missing envelope、invalid remote path envelope、stale payload drift 和 valid preflight refusal，并验证 side effects 全 false。该 executor smoke 已接入 canary readiness plan、local status bundle、auth readiness，payload manifest 也纳入 executor.py 与 executor_smoke.sh。初次 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T124243Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T124536Z/manifest.json` PASS。仍未启动 SSH、sync、build、broker、canary、order/cancel/redeem 或任何网络路径。
121. 已把 G2 canary executor 的第一段 effectful phase 收窄成 no-network remote run-root 规划验证。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在在 exact envelope、contract、payload allowlist 和 payload drift 全部通过后，解析 `run.remote_worktree` 与 `run.remote_run_dir`：要求二者为绝对路径、无 `..`、同属 `/home/ubuntu/xuan_research_runs/xuan_research_*` root，`remote_worktree` 叶子为 `worktree`，`remote_run_dir` 叶子为 `g2_canary_run_*`；结构不合法时返回 69 `REFUSED_REMOTE_RUN_ROOT_PLAN_INVALID`，且 side effects 全 false。合法计划仅把 `create_isolated_remote_run_root_under_xuan_research_runs` 标记为 `PLANNED_NO_NETWORK_REMOTE_ROOT_CREATION`，不 mkdir、不 SSH、不 sync，并把第一个未实现 effectful phase 推进到 `sync_allowlisted_source_files_only`。更新后的 `scripts/xuan_b27_dplus_g2_canary_executor_smoke.sh` 覆盖 bad run-root fixture 和 valid preflight phase-plan；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_payload_manifest_smoke_20260517T131305Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_contract_smoke_20260517T131305Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_dry_run_smoke_20260517T131305Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T131306Z/manifest.json` PASS。仍未启动 SSH、sync、build、broker、canary、order/cancel/redeem 或任何网络路径。
122. 已修正 canary readiness 的语义：链路/执行器 gate 全绿不再等价于“可申请 G2 小额实盘”。`scripts/xuan_b27_dplus_canary_readiness_plan.py` 现在要求额外存在 `xuan_b27_dplus_rust_shadow_strategy_acceptance_*` artifact，且其状态为 `PASS_RUST_SHADOW_STRATEGY_ACCEPTANCE`、scope 为 `rust_no_order_shadow_or_dry_run_strategy_acceptance`、orders/cancels/redeems/auth network/canary side effects 全 false、`strategy_acceptance_passed=true`。当前该 artifact 不存在，因此 plan 状态降级为 `CANARY_NOT_READY_SHADOW_DRY_RUN_STRATEGY_ACCEPTANCE_MISSING`，`pre_canary_plumbing_ready=true` 但 `rust_shadow_strategy_acceptance_ready=false`、`ready_for_explicit_g2_canary_approval=false`。更新后的 `scripts/xuan_b27_dplus_canary_readiness_plan_smoke.sh` 和 `scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 已验证该降级；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260517T132549Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260517T132550Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260517T132550Z/manifest.json` 报告 `canary_readiness_status=CANARY_NOT_READY_SHADOW_DRY_RUN_STRATEGY_ACCEPTANCE_MISSING`。这纠正了此前把 read-only User WS acceptance 误读成策略表现验收的风险。
123. 已新增 Rust no-order shadow/dry-run 策略验收器和本地 fixture smoke。`scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance.py` 只读取本地 `xuan_b27_dplus_observer_summary` 与独立 shadow/replay performance evidence，不启动网络、observer、User WS、canary 或订单路径；真实 PASS 必须同时满足 no-order safety、recorder completeness、candidate quality、source-truth UNKNOWN handling 和 `PASS_STRATEGY_PERFORMANCE_EVIDENCE`。`scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance_smoke.sh` 用本地 fixtures 覆盖 PASS、缺 performance evidence、no-order 违规、无候选、UNKNOWN handling 缺失、decode error 和 performance evidence 失败，并且不会发布真实 acceptance artifact。canary readiness/status bundle 现在要求该 smoke PASS，但仍保持 `CANARY_NOT_READY_SHADOW_DRY_RUN_STRATEGY_ACCEPTANCE_MISSING`，直到未来出现真实 `PASS_RUST_SHADOW_STRATEGY_ACCEPTANCE` artifact。
124. 已新增 shadow/replay performance evidence 生成器和 fixture smoke。`scripts/xuan_b27_dplus_shadow_performance_evidence.py` 只读取本地 observer summary 与 edge-sample JSONL，验证 observer summary no-order safe、BTC 5m market scope、edge sample decode、样本量、正 edge/expected value、以及样本中没有 order/cancel/redeem/auth-network/canary side effect；输出 `xuan_b27_dplus_shadow_strategy_performance_evidence`，作为 `scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance.py` 的输入。`scripts/xuan_b27_dplus_shadow_performance_evidence_smoke.sh` 覆盖 PASS、unsafe observer summary、edge JSONL decode error、样本量不足、负 edge、side-effect sample、wrong market 等 fixtures，并设置 `real_performance_evidence_published=false`。canary readiness/status/payload allowlist 已接入该 smoke，但真实 canary gate 仍要求未来真实 `PASS_RUST_SHADOW_STRATEGY_ACCEPTANCE` artifact。
125. 已新增本地 no-network shadow edge-sample extractor，并把 edge-only 样本和真实 performance evidence 明确拆开。`scripts/xuan_b27_dplus_extract_shadow_edge_samples.py` 只读取 `xuan_research_artifacts` 下的本地 observer recorder events，拒绝 raw/replay、`/mnt/poly-replay`、`replay_published` 路径，不启动网络/observer/canary/order/cancel/redeem。它从 `xuan_b27_dplus_auth_observer_20260516T010001Z` 抽出 5000 条正 edge 候选样本，artifact 为 `xuan_research_artifacts/xuan_b27_dplus_shadow_edge_samples_20260517T144509Z/manifest.json`，但所有样本均标记 `performance_basis=observer_edge_only_not_realized`，因此 `scripts/xuan_b27_dplus_shadow_performance_evidence.py` 对该真实 edge-only 样本集正确返回 `FAIL_EDGE_ONLY_NOT_PERFORMANCE`，artifact 为 `xuan_research_artifacts/xuan_b27_dplus_shadow_performance_evidence_20260517T144516Z/manifest.json`。新增 `scripts/xuan_b27_dplus_extract_shadow_edge_samples_smoke.sh` 验证 extractor PASS 且 edge-only 样本会被 performance gate 拒绝。结论：当前 Rust observer 已能产生候选边际证据，但还没有可进入 canary 的 realized shadow/dry-run strategy performance evidence。
126. 已新增 no-network outcome-label bridge，把“已有 edge 样本”到“可验收 performance evidence”的缺口收窄为独立 realized labels。`scripts/xuan_b27_dplus_outcome_label_bridge.py` 只读取 `xuan_research_artifacts` 下的本地 edge-sample JSONL 与 outcome-label JSONL，拒绝 raw/replay、`/mnt/poly-replay`、`replay_published`，并要求 labels 的 `performance_basis` 属于 `dry_run_outcome`、`independent_shadow_outcome` 或 `shadow_replay_outcome`；若 label 仍是 `observer_edge_only_not_realized`，或出现 order/cancel/redeem/auth-network/canary side effect，都会 fail closed。`scripts/xuan_b27_dplus_outcome_label_bridge_smoke.sh` 用 fixture 证明 PASS bridge 输出可让 `scripts/xuan_b27_dplus_shadow_performance_evidence.py` 和 `scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance.py` 串联通过，同时覆盖 decode error、wrong market、side-effect label、low join、edge-only label、negative realized outcome 等失败。该 smoke 设置 `real_outcome_bridge_published=false`、`acceptance_artifact_published=false`，所以真实 canary gate 仍等独立 no-order outcome labels 和真实 `PASS_RUST_SHADOW_STRATEGY_ACCEPTANCE` artifact。
127. 已新增 realized outcome-label producer，把“未来 Rust no-order shadow/dry-run run 应该怎样产出 labels”变成可测本地契约。`scripts/xuan_b27_dplus_realized_outcome_labels.py` 只读取 `xuan_research_artifacts` 下的本地 run manifest 和显式 `xuan_b27_dplus_shadow_outcome_label` / `xuan_b27_dplus_dry_run_outcome_label` / `xuan_b27_dplus_realized_outcome_label` events，拒绝 raw/replay、`/mnt/poly-replay`、`replay_published`，并要求 run manifest 明确 no-order、orders/cancels/redeems/auth-network/canary side effects 全 false。`scripts/xuan_b27_dplus_realized_outcome_labels_smoke.sh` 覆盖 PASS、unsafe manifest、decode error、wrong market、side-effect event、edge-only basis、低样本、负 realized outcome，并证明 producer -> outcome-label bridge -> performance evidence -> Rust strategy acceptance 的 fixture 链路可 PASS；但 smoke 设置 `real_outcome_labels_published=false`、`acceptance_artifact_published=false`，因此不发布真实 `PASS_RUST_SHADOW_STRATEGY_ACCEPTANCE`。当前 canary 仍 blocked，下一步是真实 Rust no-order shadow/dry-run artifact 产生这些 explicit outcome events。
128. 已新增 no-order shadow/dry-run run artifact harness，把“真实 Rust shadow/dry-run artifact 应该长什么样”收敛为本地可测契约。`scripts/xuan_b27_dplus_no_order_shadow_run_artifact.py` 只读取 `xuan_research_artifacts` 下的 observer summary、edge samples 和独立 realized outcome labels，拒绝 raw/replay、`/mnt/poly-replay`、`replay_published`，并要求 observer summary no-order safe、labels 非 edge-only、market 为 BTC 5m、无 order/cancel/redeem/auth-network/canary side effect、join 样本足够且 realized outcome 质量非负。输出 artifact 是 `PASS_NO_ORDER_SHADOW_DRY_RUN` run manifest + `outcome_events.jsonl`，可被 `scripts/xuan_b27_dplus_realized_outcome_labels.py` 消费。`scripts/xuan_b27_dplus_no_order_shadow_run_artifact_smoke.sh` 覆盖 PASS、unsafe observer、decode error、wrong market、side-effect label、edge-only basis、duplicate key、低样本、负 realized outcome，并证明 harness -> realized labels -> bridge -> performance evidence -> Rust strategy acceptance 的 fixture 链路可 PASS；但 smoke 设置 `real_shadow_run_artifact_published=false`、`acceptance_artifact_published=false`，因此真实 canary gate 仍等待真实 Rust no-order shadow/dry-run outcome events。
129. 已新增 Rust shadow/dry-run strategy acceptance runner，把已有本地 gates 串成一个 fail-closed 入口。`scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance_runner.py` 只接受 `xuan_research_artifacts` 下安全 no-order shadow/dry-run run artifact，并在同一进程内依次调用 realized labels producer、outcome-label bridge、shadow performance evidence 和 Rust strategy acceptance；不启动 observer、User WS、canary、网络或 order/cancel/redeem。`scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance_runner_smoke.sh` 证明 fixture 链路能完整 PASS，但 `--publish-real-acceptance` 遇到 fixture/smoke input 必须返回 `FAIL_REFUSED_FIXTURE_PUBLISH`，不会创建真实 `xuan_b27_dplus_rust_shadow_strategy_acceptance_*` artifact。该 runner smoke 已接入 canary readiness/status/payload gates；当前 `pre_canary_plumbing_ready=true`，但 `rust_shadow_strategy_acceptance_ready=false`，所以 canary 仍 blocked。
130. 已新增 shadow acceptance input discovery gate，把“是否已有真实 no-order shadow/dry-run 策略验收输入”从人工判断变成可审计 artifact。`scripts/xuan_b27_dplus_shadow_acceptance_input_discovery.py` 只扫描本地 `xuan_research_artifacts` 的 manifest/JSONL，主动排除 fixture/smoke、raw/replay、`/mnt/poly-replay` 和 `replay_published` 路径，检查 non-fixture run manifest、no-order 权限、BTC 5m market、显式 realized outcome label events、performance basis 和 forbidden side effects。真实当前扫描 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_acceptance_input_discovery_20260517T173216Z/manifest.json` 报告 `BLOCKED_NO_REAL_SHADOW_ACCEPTANCE_INPUTS`：扫描 1857 个本地 manifest，fixture/smoke candidate 102 个，non-fixture real candidate 0 个。`scripts/xuan_b27_dplus_shadow_acceptance_input_discovery_smoke.sh` 覆盖 fixture ignored、valid fixture root、side-effect labels、edge-only labels 和 raw scan-root refusal，并已接入 canary readiness/status/payload gates；当前 blocker 因此明确为缺少真实 Rust no-order shadow/dry-run outcome artifact，不是授权、User WS 或 canary executor。
131. 已新增本地 L1 dry-run outcome labeler，把 Rust observer edge-only 候选转成可验收或可拒绝的 no-order dry-run outcome evidence。`scripts/xuan_b27_dplus_l1_dry_run_outcome_labels.py` 只读取 `xuan_research_artifacts` 下的 observer summary、edge samples、recorder events 与 `market_md.jsonl`，拒绝 raw/replay、`/mnt/poly-replay`、`replay_published` 和任何 order/cancel/redeem/auth-network/canary side effect。它按 candidate event 后的同侧 L1 ask 是否在 lookahead 窗口内触到 observer passive price 生成 `xuan_b27_dplus_dry_run_outcome_label`，并记录 duplicate candidate 去重。真实 60s gate 明确失败：`xuan_research_artifacts/xuan_b27_dplus_l1_dry_run_outcome_labels_20260517T181833Z/manifest.json` 为 `FAIL_REALIZED_OUTCOME_QUALITY`，4892 个去重 label 中仅 698 个触价，touch ratio 0.14268，mean realized edge 约 -285.85 bps。真实 300s gate 通过：`xuan_research_artifacts/xuan_b27_dplus_l1_dry_run_outcome_labels_20260517T181834Z/manifest.json` 为 `PASS_L1_DRY_RUN_OUTCOME_LABELS`，4892/4892 触价，mean/median realized edge 约 400 bps；该证据只证明 5m 窗口内 passive L1 touch，不证明最终 PnL 或 live fill quality。
132. 已用真实本地 300s L1 dry-run labels 发布 Rust no-order shadow strategy acceptance，但仍未启动任何网络或交易路径。真实 run artifact `xuan_research_artifacts/xuan_b27_dplus_no_order_shadow_run_artifact_20260517T181858Z/manifest.json` 为 `PASS_NO_ORDER_SHADOW_DRY_RUN`，随后 `scripts/xuan_b27_dplus_rust_shadow_strategy_acceptance_runner.py --publish-real-acceptance` 产出 `xuan_research_artifacts/xuan_b27_dplus_rust_shadow_strategy_acceptance_20260517T181958Z/manifest.json`，状态 `PASS_RUST_SHADOW_STRATEGY_ACCEPTANCE`，`strategy_acceptance_passed=true`，orders/cancels/redeems/auth-network/canary side effects 全 false。`scripts/xuan_b27_dplus_shadow_acceptance_input_discovery.py` 已修正 workspace-relative manifest path 解析并重新发现真实 ready input：`xuan_research_artifacts/xuan_b27_dplus_shadow_acceptance_input_discovery_20260517T182203Z/manifest.json`。`scripts/xuan_b27_dplus_canary_readiness_plan.py` / status bundle 现报告 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL`，下一 gate 是单独明确 exact G2 canary approval；heartbeat 或泛授权仍不能启动 canary。
133. G2 canary launch plan smoke 已从“shadow acceptance 缺失”预期切换到真实 ready 状态。`scripts/xuan_b27_dplus_g2_canary_launch_plan_smoke.sh` 现在要求 plan artifact 为 `READY_FOR_EXPLICIT_G2_CANARY_SYNC_AND_RUN_APPROVAL`、`local_gates_ok=true`、status bundle 报告 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL`，并继续要求 `canary_run_authorized=false` 与 exact approval next gate。最新 launch plan smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_launch_plan_smoke_20260517T182940Z/manifest.json` PASS；payload allowlist、executor contract、dry-run、executor preflight、canary readiness、status bundle、auth readiness 和 `cargo test xuan_b27_dplus --lib` 均已重跑通过。仍未启动 SSH/network/canary/order/cancel/redeem；下一 gate 仍是明确 exact G2 canary sync/rebuild + run approval。
134. G2 canary executor preflight 已推进到 no-network payload sync mapping。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在在 exact approval envelope、contract、payload allowlist、payload drift 与 remote run-root plan 全部通过后，生成 allowlisted payload 到隔离 remote worktree 的映射计划，验证所有 destination 都位于 `/home/ubuntu/xuan_research_runs/xuan_research_*/worktree` 下，并继续保证 `sync_archive_created=false`、`ssh_started=false`、`network_started=false`、`remote_files_written=false`。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T190939Z/manifest.json` PASS；valid preflight 记录 `payload_file_count=72`、`payload_total_size_bytes=2817248`、`first_unimplemented_effectful_phase=build_declared_remote_targets_only`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；下一 executor implementation gate 是远端 build phase 的同类 refusal-first 规划与审查。
135. G2 canary executor preflight 已继续推进到 no-network declared remote build plan。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在验证 allowlisted payload 中包含 `Cargo.toml`、`Cargo.lock` 与 `src/bin/polymarket_v2.rs`，并记录远端 build command `cargo build --locked --bin polymarket_v2`，但 `executed=false`、`cargo_invoked=false`、`ssh_started=false`、`network_started=false`、`remote_files_written=false`、`target_dir_written=false`。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T193851Z/manifest.json` PASS；valid preflight 记录 `remote_build_plan.status=PASS`，并把 `first_unimplemented_effectful_phase` 推进到 `run_readonly_shared_ingress_preflight_before_canary`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；下一 executor implementation gate 是 shared-ingress preflight phase 的 refusal-first 规划与审查。
136. G2 canary executor preflight 已继续推进到 no-network read-only shared-ingress preflight plan。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在要求 G2 envelope 中 `shared_ingress_role=client`、`shared_ingress_root=/srv/pm_as_ofi/shared-ingress-main`，并要求 payload 包含 `scripts/xuan_b27_dplus_shared_ingress_preflight.py`；valid preflight 会记录未来远端命令 `python3 <remote_worktree>/scripts/xuan_b27_dplus_shared_ingress_preflight.py --root /srv/pm_as_ofi/shared-ingress-main --output-dir <remote_run_dir>/shared_ingress_preflight`，但 `preflight_executed=false`、`ssh_started=false`、`network_started=false`、`broker_started=false`、`broker_repaired=false`、`connected_to_broker=false`、`modified_shared_ingress=false`。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T200917Z/manifest.json` PASS；valid preflight 把 `first_unimplemented_effectful_phase` 推进到 `inject_auth_vars_ephemerally_only`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；下一 executor implementation gate 是 ephemeral auth injection phase 的 refusal-first 规划与 secret-sentinel 审查。
137. G2 canary executor preflight 已继续推进到 no-network ephemeral auth injection plan。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在从 approval envelope/risk caps 生成非敏感 canary env contract，并只记录敏感凭据类别 `polymarket_api_credentials` / `polymarket_private_key`，不读取 `.env`、不记录 secret key name、不会派生 credentials、不会生成 stdin secret payload、不会写远端 `.env`。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T203933Z/manifest.json` PASS；valid preflight 记录 `auth_injection_plan.status=PASS`、`non_secret_env_count=16`、`secret_values_recorded=false`、`secret_key_names_recorded=false`、`credential_derivation_performed=false`、`remote_env_file_written=false`，并把 `first_unimplemented_effectful_phase` 推进到 `start_single_bounded_g2_canary_process`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；下一 executor implementation gate 是 bounded canary process phase 的 refusal-first 规划与 kill/stop 条件审查。
138. G2 canary executor preflight 已继续推进到 no-network bounded canary process plan。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在在 exact envelope、contract、payload allowlist/drift、remote run-root、payload sync、remote build、shared-ingress preflight 和 ephemeral auth 计划全部通过后，记录未来单个 bounded canary 进程的命令与停止条件：binary 为 `<remote_worktree>/target/debug/polymarket_v2`，env overlay 显式包含 `POLYMARKET_MARKET_SLUG=btc-updown-5m` 与 `PM_LOG_ROOT=<remote_run_dir>/logs`，duration 为 1800s、max_rounds 为 6、graceful `TERM` 后 30s kill，并要求 stop-on-unknown、source-truth UNKNOWN/FAIL、risk cap violation、unexpected taker fill、recorder decode/critical drop 和 broker preflight failure 都触发停止。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T211148Z/manifest.json` PASS；valid preflight 记录 `bounded_canary_process_plan.status=PASS`，但 `process_started=false`、`orders_sent=false`、`cancels_sent=false`、`redeems_sent=false`、`remote_files_written=false`，并把 `first_unimplemented_effectful_phase` 推进到 `wait_or_stop_at_duration_or_round_limit`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；下一 executor implementation gate 是 wait/stop duration/round-limit phase 的 refusal-first 规划与 artifact pullback 前置审查。
139. G2 canary executor preflight 已继续推进到 no-network wait/stop supervision plan。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在在 bounded process plan 通过后，记录未来监督策略：5s poll interval、duration 1800s、max_rounds 6、process exit/source-truth UNKNOWN/FAIL/risk cap/unexpected taker fill/recorder decode 或 critical drop/broker preflight failure 都是停止触发器；终止序列为 observe stop trigger -> graceful `TERM` -> wait 30s -> kill if still running -> record exit status。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T214139Z/manifest.json` PASS；valid preflight 记录 `wait_stop_supervision_plan.status=PASS`，但 `wait_started=false`、`process_polled=false`、`signal_sent=false`、`kill_sent=false`、`process_reaped=false`、`remote_files_read=false`、`orders_sent=false`，并把 `first_unimplemented_effectful_phase` 推进到 `pull_artifacts_to_xuan_research_artifacts`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；下一 executor implementation gate 是 artifact pullback/local review/secret scan 边界的 refusal-first 规划。
140. G2 canary executor preflight 已覆盖到 artifact pullback/local review/secret-sentinel 边界。`scripts/xuan_b27_dplus_g2_canary_executor.py` 现在在 wait/stop supervision plan 通过后，记录未来 artifact pullback 和本地 review 的目录/文件清单：预期远端 run manifest、acceptance review、stdout/stderr、shared-ingress preflight manifest、`logs/`、`recorder/` 等树；本地落点为 `xuan_research_artifacts/xuan_b27_dplus_g2_canary_artifacts_<ts>`；post-run review 计划调用 `scripts/xuan_b27_dplus_summarize_g2_canary_run.py --check-acceptance`，并要求 caller-provided secret sentinel scan，但不记录 secret value 或 secret key name。最新 executor smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_executor_smoke_20260517T221138Z/manifest.json` PASS；valid preflight 记录 `artifact_pullback_review_plan.status=PASS`，但 `remote_files_read=false`、`local_files_written=false`、`summary_executed=false`、`secret_scan_executed=false`、`acceptance_review_written=false`、orders/cancels/redeems 全 false，且 `first_unimplemented_effectful_phase=null`。仍未启动 SSH/sync/build/broker/canary/order/cancel/redeem；这表示 executor 的所有 effectful phases 已有本地 refusal-first plan，但真实执行实现仍未启用，exact G2 canary 仍需要单独当前对话授权和 reviewed effectful executor。
141. G2 canary launch/readiness artifact 现在显式区分“可申请 exact approval”和“可实际执行”。`scripts/xuan_b27_dplus_g2_canary_launch_plan.py` 直接读取最新 `xuan_b27_dplus_g2_canary_executor_smoke_*` 的 `valid_preflight/manifest.json`，要求 executor smoke PASS、valid preflight 为 `REFUSED_EFFECTFUL_EXECUTOR_NOT_IMPLEMENTED`、`effectful_executor_implemented=false`、`executor_preflight_implemented=true`、`canary_run_authorized=false`、所有 effectful phase plan 存在、所有 plan section PASS、phase/planned/top-level side effects 全 false，才把 launch plan 标为 `READY_FOR_EXPLICIT_G2_CANARY_SYNC_AND_RUN_APPROVAL`。同时 launch plan 顶层新增 `ready_to_execute=false`、`execution_readiness_status=NOT_READY_EFFECTFUL_EXECUTOR_NOT_IMPLEMENTED` 和 `requires_reviewed_effectful_executor_implementation=true`；`scripts/xuan_b27_dplus_canary_readiness_plan.py` 也暴露同一组字段。结论：D+ 仍处于“证据足够进入 exact G2 approval 讨论，但 effectful executor 尚未实现”的状态；本轮未启动 SSH/sync/build/broker/canary/order/cancel/redeem 或任何网络路径。
142. local status bundle 现在把 G2 canary execution readiness 提升为顶层字段，避免只看 `canary_readiness_status=READY_FOR_EXPLICIT_G2_CANARY_APPROVAL` 的消费方误判为可执行。`scripts/xuan_b27_dplus_local_status_bundle.py` 新增 `canary_run_ready=false`、`canary_ready_to_execute=false`、`canary_effectful_executor_implemented=false`、`canary_execution_readiness_status=NOT_READY_EFFECTFUL_EXECUTOR_NOT_IMPLEMENTED` 和 `canary_requires_reviewed_effectful_executor_implementation=true`；`canary_next_gate` 也改为同时要求 explicit exact G2 canary approval 和 reviewed effectful executor implementation。`scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 与 `scripts/xuan_b27_dplus_canary_readiness_plan_smoke.sh` 已同步断言这些字段。结论不变：当前可进入 exact approval 讨论，但 heartbeat/泛授权仍不能启动 SSH/sync/build/canary/order/cancel/redeem，真实执行还需要 reviewed effectful executor。
143. G2 approval envelope verifier 现在把 reviewed effectful executor implementation 变成硬 gate。`scripts/xuan_b27_dplus_g2_canary_approval_envelope.py` 新增 `executor_review` 校验：envelope 必须声明 `reviewed_effectful_executor_implementation=true`、`review_status=PASS_REVIEWED_EFFECTFUL_G2_EXECUTOR_IMPLEMENTATION`、`require_current_payload_allowlist_no_drift=true`、`require_not_heartbeat_or_generic_approval=true`，并指向安全的本地 `xuan_research_artifacts` review artifact。verifier 会读取该 artifact，要求 `artifact=xuan_b27_dplus_g2_canary_effectful_executor_review`、`status=PASS_REVIEWED_EFFECTFUL_G2_EXECUTOR_IMPLEMENTATION`、`review_passed=true`、`effectful_executor_implemented=true`，且 review 阶段所有 side effects 为 false。approval envelope smoke 新增 missing executor review、bad review status、missing review artifact 拒绝用例；launcher refusal、executor dry-run、executor preflight smoke fixtures 也同步带入 review artifact。结论：即使用户给出 exact G2 approval，没有 reviewed effectful executor review artifact 的 envelope 仍不能通过。
144. 已新增独立的 G2 effectful executor review gate，把“当前 executor 尚未实现”从口头说明变成机器可审计 artifact。`scripts/xuan_b27_dplus_g2_canary_effectful_executor_review.py` 只读取本地 executor preflight manifest，不 SSH、不 sync、不 build、不启动 broker/canary，也不读取或写入任何远端 artifact；当前真实 preflight 会产出 `BLOCKED_EFFECTFUL_EXECUTOR_NOT_IMPLEMENTED`、`review_passed=false`、`effectful_executor_implemented=false`，同时验证所有 phase plan 完整、plan sections 为 PASS、review input side effects 全 false。`scripts/xuan_b27_dplus_g2_canary_effectful_executor_review_smoke.sh` 覆盖 missing/unsafe preflight、side-effect preflight、当前 BLOCKED、fixture PASS 仅在 smoke 内部允许，且 `fixture_pass_artifact_published=false`。该 smoke 已接入 payload allowlist、canary readiness、local status bundle 和 auth readiness；最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_g2_canary_effectful_executor_review_smoke_20260518T001821Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260518T001900Z/plan_run/manifest.json` 仍为 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL` 但 `ready_to_execute=false`、`effectful_executor_implemented=false`，`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T001924Z/manifest.json` 仍明确 `NOT_READY_EFFECTFUL_EXECUTOR_NOT_IMPLEMENTED`。本轮未启动 SSH/sync/build/broker/canary/order/cancel/redeem 或任何网络路径。
145. G2 approval envelope 对 effectful executor review artifact 的验真已收紧，防止 smoke/fixture PASS 被误当成真实执行器评审。`scripts/xuan_b27_dplus_g2_canary_approval_envelope.py` 现在读取 review artifact 后必须同时验证 `scope=local_no_network_g2_canary_effectful_executor_review`、`fixture_review=false`、`reviewed_effectful_executor_implementation=true`、`canary_run_authorized=false`、`started_canary=false`、`orders_sent=false`、`cancels_sent=false`、`redeems_sent=false`、`auth_network_started=false`，并继续要求 side effects 全 false。`scripts/xuan_b27_dplus_g2_canary_approval_envelope_smoke.sh` 新增 `fixture_executor_review_artifact_rejects` 用例；launcher refusal、executor dry-run、executor preflight smoke 的有效 envelope fixture 也同步带上新字段。该变更只影响本地 JSON verifier/smoke，没有启动 SSH/sync/build/broker/canary/order/cancel/redeem 或任何网络路径。
146. canary readiness/status artifact 现在明确区分 shadow/dry-run 证据类型，避免把当前 no-order L1 touch dry-run 误读为 live fill/PnL/queue-position 证明。`scripts/xuan_b27_dplus_canary_readiness_plan.py` 新增 `shadow_strategy_evidence`，记录 `kind=NO_ORDER_LOCAL_L1_TOUCH_DRY_RUN`、`basis=dry_run_outcome`、300s L1 label 数量/触价率/realized edge、`no_order=true`，同时显式写出 `live_fill_pnl_queue_position_proof=false` 以及 does-not-prove 列表：live fill quality、real PnL、queue position、fee/rebate economics、authenticated order lifecycle。`scripts/xuan_b27_dplus_local_status_bundle.py` 把同类字段提升为顶层 `canary_shadow_strategy_evidence_*` 摘要，smoke 已断言这些 caveat。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T011339Z/manifest.json` 仍为 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL`，但 `canary_ready_to_execute=false`、`canary_effectful_executor_implemented=false`。本轮未启动 SSH/sync/build/broker/canary/order/cancel/redeem 或任何网络路径。
147. 已按“真实 shadow/dry-run 应该报告模拟交易 PnL/配对/残仓”的口径修正 canary 前置 gate：新增 `scripts/xuan_b27_dplus_shadow_trading_acceptance.py`，只读取已有 `tools/xuan_dplus_passive_passive_shadow_runner.py` 输出目录中的 `manifest.json` 与 `aggregate_report.json`，不启动 runner、不连 shared-ingress、不读 raw/replay。该 gate 要求 D+ passive/passive shadow trading 报告具备 candidates、queue-supported fills、pair actions、pair qty、pair PnL、ROI、pair share、pair cost 分布和 residual qty/cost/material lots，并且 pair PnL/ROI 非负、残仓风险受限、orders/cancels/redeems/auth-network/canary side effects 全 false。`scripts/xuan_b27_dplus_shadow_trading_acceptance_smoke.sh` 用 fixture 覆盖 PASS、missing report、negative PnL、残仓过大、样本不足和 safety side-effect 拒绝。`scripts/xuan_b27_dplus_canary_readiness_plan.py` 现在同时要求旧的 Rust no-order signal/label acceptance 和新的 shadow trading PnL/残仓 acceptance；当前真实状态已从 `READY_FOR_EXPLICIT_G2_CANARY_APPROVAL` 降为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_MISSING`，因为本地没有真实 D+ passive/passive shadow runner 的 aggregate report。最新 artifacts：`xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_smoke_20260518T015500Z/manifest.json` PASS，`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260518T015658Z/plan_run/manifest.json` 报告 `pre_canary_plumbing_ready=true`、`rust_shadow_strategy_acceptance_ready=true`、`shadow_trading_acceptance_ready=false`、`requires_true_shadow_trading_pnl_residual_report=true`，`xuan_research_artifacts/xuan_b27_dplus_g2_canary_launch_plan_smoke_20260518T015657Z/plan_run/manifest.json` 为 `BLOCKED_G2_CANARY_LAUNCH_PLAN_INPUTS`，最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T015728Z/manifest.json` 的 `canary_next_gate` 是先取得 D+ passive/passive shadow trading 的 pair/PnL/residual metrics。验证：`py_compile`、`bash -n`、shadow trading/payload/contract/dry-run/executor/launch/readiness/status/auth readiness smokes、`cargo fmt --check`、`git diff --check`、`cargo test xuan_b27_dplus --lib` 均通过；没有启动 SSH/sync/build/broker/canary/order/cancel/redeem 或网络路径。
148. 已新增真实 shadow trading report discovery gate，把“是否已有可验收的 D+ passive/passive aggregate_report”从人工判断变成机器可见状态。`scripts/xuan_b27_dplus_shadow_trading_report_discovery.py` 只扫描 `xuan_research_artifacts` 下的 `aggregate_report.json` + sibling `manifest.json`，排除 smoke/fixture、raw/replay、`/mnt/poly-replay`、`replay_published`，并校验 runner 为 `tools/xuan_dplus_passive_passive_shadow_runner.py`、BTC 5m scope、必需 trading metrics、orders/cancels/redeems/auth-network/canary side effects 全 false。真实 discovery `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_report_discovery_20260518T022145Z/manifest.json` 报告 `BLOCKED_NO_REAL_SHADOW_TRADING_REPORTS`：`candidate_count=16`、`fixture_or_smoke_candidate_count=16`、`real_candidate_count=0`、`valid_real_candidate_count=0`。该 artifact 同时写出 exact shadow-run plan，但仅为计划：`python3 tools/xuan_dplus_passive_passive_shadow_runner.py --repo /srv/pm_as_ofi/repo --shared-ingress-root /srv/pm_as_ofi/shared-ingress-main --prefix btc-updown-5m --round-offsets 0,1,2,3 --duration-s 1800 ...`，明确 `requires_current_exact_main_thread_approval=true` 且 `will_connect_to_shared_ingress_market_socket_if_executed=true`、`will_send_orders_if_executed=false`。`scripts/xuan_b27_dplus_shadow_trading_report_discovery_smoke.sh` 已覆盖 fixture 默认排除、显式 fixture READY、invalid report 拒绝和 side-effect 全 false；最新 smoke `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_report_discovery_smoke_20260518T022157Z/manifest.json` PASS。readiness/status 已接入该 gate：`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260518T022303Z/plan_run/manifest.json` 仍为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_MISSING`，并暴露 `shadow_trading_report_discovery_status=BLOCKED_NO_REAL_SHADOW_TRADING_REPORTS`、`requires_real_shadow_trading_report=true`；最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T022310Z/manifest.json` 顶层也暴露同一 blocker。没有启动 shadow runner、SSH/sync/build/broker/canary/order/cancel/redeem 或网络路径。
149. 已按主线程明确授权执行 no-order shadow runner，并把真实模拟交易结果接入验收。第一次以 `ubuntu` 连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock` 因 Unix socket 权限 fail-closed；随后在隔离 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_20260518T023803Z` 下以 `pmofi` 只读 shared-ingress client 运行 `tools/xuan_dplus_passive_passive_shadow_runner.py` 1800s，未发送 order/cancel/redeem，未改 broker/shared-ingress/service/env。拉回的 `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T023803Z/remote/output/aggregate_report.json` 记录 4 个 slug、30 candidates、28 queue-supported fills、23 pair actions、pair_qty=23.38、pair_pnl=+1.150475、ROI on filled cost 约 5.00%、residual_qty=4.0225、residual_cost=1.59665、material_residual_lots=0。`scripts/xuan_b27_dplus_shadow_trading_acceptance.py` 对真实报告的验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T031434Z/manifest.json` 为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，`acceptance_passed=false`，失败项为 candidates 30 < 100 且 net_pair_cost_p90 约 1.07 > 1.0。结论：shadow runner 有正模拟 pair PnL，但样本和成本尾部未达标，不能作为 G2 canary 或实盘推进依据。
150. 已复用项目现有 `src/bin/pair_arb_backtest.rs` 做本地 backtest/grid 验证，结果反向支持“先调参/训练，不推进 canary”。在允许的本地 snapshot DB `/Users/hot/web3Scientist/poly_trans_research/data/snapshots/btc5m.db` 上，broad grid artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_grid_broad_20260518T031947Z/manifest.json` 覆盖 1296 组参数，其中 1008 组有非零 fills，但 positive_nonzero_run_count=0；最佳非零 fill 配置仍为 pnl=-36.06、avg=-0.0361、residual_loss_rate=97.5%。随后扩展 aggressive/conservative probe `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_grid_aggressive_probe_20260518T034253Z/manifest.json` 覆盖 2160 组更小 bid、更低 target、disabled/continuous tier 和 conservative/aggressive fill 假设；1344 组有非零 fills，positive_nonzero_run_count 仍为 0，最佳非零配置仅到 pnl=-1.59、avg=-0.0016、residual_loss_rate=83.0%。`scripts/xuan_b27_dplus_canary_readiness_plan.py` 和 `scripts/xuan_b27_dplus_local_status_bundle.py` 现在优先暴露最新 aggressive probe 的 `pair_arb_backtest_*` 字段，并把当前状态置为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`，next gate 是收集更大样本的正 shadow/backtest 证据或 retrain/tune strategy。
151. G2 launch plan 已同步为策略质量失败阻断，而不是继续停留在“缺报告”口径。`scripts/xuan_b27_dplus_g2_canary_launch_plan.py` 在 status bundle 报告 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED` 时，`next_gate` 直接写入 “shadow trading acceptance failed; collect larger positive nonzero backtest/shadow evidence or retrain/tune strategy”，并继续保持 `BLOCKED_G2_CANARY_LAUNCH_PLAN_INPUTS`、`local_gates_ok=false`、`canary_run_authorized=false`、`ready_to_execute=false`。最新 launch smoke `xuan_research_artifacts/xuan_b27_dplus_g2_canary_launch_plan_smoke_20260518T033958Z/manifest.json` PASS。
152. `src/bin/pair_arb_backtest.rs` 已补入默认关闭的 residual completion/salvage 模型，用来对齐真实 shadow runner 中 aged residual 的 FAK 补腿假设。新增参数为 `--salvage-net-cap`、`--salvage-start-remaining` 和 `--taker-fee-rate`；默认 `salvage_net_cap=0`，因此旧 backtest 口径不变。补腿逻辑只在本地 backtest 内按当前 inventory 平均成本和对侧 ask 判断 `held_avg + completion_ask + taker_fee <= salvage_net_cap` 时模拟买入缺腿，记录 `completion_fills/qty/cost/fee`，不触碰任何网络或交易路径。salvage probe `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_salvage_probe_20260518T043838Z/manifest.json` 在 1152 组低仓位/低 target/补腿 cap 参数中，1152 组均有非零 fills，但 positive_nonzero_run_count 仍为 0；最佳非零配置为 pnl=-1.22、avg=-0.0012、residual_loss_rate=97.5%，最低残仓亏损率配置也仍 pnl=-1.39、residual_loss_rate=96.8%。结论：补腿模型缩小了亏损但仍未转正，说明问题不只是旧 backtest 缺少 salvage，而是入场边际/库存路径本身仍不够。
153. `src/bin/pair_arb_backtest.rs` 已重构为一次加载 snapshot windows 后在内存中跑参数网格，并新增 JSONL 输出、`--skip` 切片、以及默认关闭的 `--directional-risk-filter-bps` 风险过滤探针。新增 `scripts/xuan_b27_dplus_pair_arb_backtest_grid.py` 统一产出 `xuan_b27_dplus_pair_arb_backtest_json_grid` artifact，包含用户要求的 trading 指标：filled/paired windows、fills、pair qty、pair PnL、residual qty/cost/PnL、weighted avg pair cost、residual loss rate、positive run counts、best/top configs，并显式记录 validation scope 与 no-order side effects。全样本基础低 target 方向过滤探针 `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_20260518T050453Z/manifest.json` 找到探索性正数：675 configs 中 195 个 positive nonzero，最佳配置 pnl=+0.914、avg=0.000171/window、target=0.55、bid=0.10、net=0.5、cutoff=120s、directional filter=2bps；但只有 147/5331 filled windows、173 fills、paired_qty=1.2、paired_pnl=+0.556、residual_pnl=+0.358、residual_qty=15.2、residual_cost=6.542、residual_loss_rate=54.29%、weighted_avg_pair_cost=0.5367。结论：它说明参数空间里有信号线索，但不是可推广策略证据。
154. 已做同数据集分半 OOS 验证，探索性全样本正数没有稳住。全样本最佳配置在 first half `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_oos_first_20260518T0508Z/manifest.json` 上为 pnl=-0.165、paired_qty=0、residual_pnl=-0.165；second half `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_oos_second_20260518T0508Z/manifest.json` 为 pnl=+1.079、paired_qty=1.2、paired_pnl=+0.556、residual_pnl=+0.523。进一步按 first half 训练选出的最佳参数 `target=0.75,bid=0.10,net=0.5,cutoff=240s,directional_filter=5bps` 在 train artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_is_first_20260518T0510Z/manifest.json` 上为 pnl=+0.179，但在 second-half OOS artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_oos_fail_20260518T0512Z/manifest.json` 上失败：pnl=-2.486、paired_pnl=+2.495、residual_pnl=-4.981、residual_qty=23.3、residual_loss_rate=77.93%、weighted_avg_pair_cost=0.6801。结论：当前正数来自样本内/局部窗口，不足以进入 canary。
155. `scripts/xuan_b27_dplus_canary_readiness_plan.py` 已把 JSONL grid/OOS artifact 纳入 pair_arb_backtest 证据优先级，并要求 positive nonzero run 必须通过 OOS validation 才能解除策略推广 blocker；`scripts/xuan_b27_dplus_canary_readiness_plan_smoke.sh` 与 `scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 同步支持单配置 OOS validation artifact。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T051434Z/manifest.json` 仍为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`，并记录 `canary_pair_arb_backtest_status=FAIL_NO_POSITIVE_BACKTEST_CONFIG`、`canary_pair_arb_backtest_positive_nonzero_run_count=0`、`canary_pair_arb_backtest_best_nonzero_pnl=-2.486`、`canary_requires_strategy_retrain_or_positive_backtest_config=true`。结论：现在的正确推进方向是继续训练/调参并做 OOS/backtest + no-order shadow 验证，不是推进 G2 canary。
156. 已新增 `scripts/xuan_b27_dplus_pair_arb_oos_compare.py`，专门比较两个本地 JSONL backtest grid 的同参数配置，避免把单一 train/OOS artifact 或临时解析误读成最终结论。first/second half 对比 artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_20260518T0526Z/manifest.json` 覆盖 675 个 matched configs：first half 有 63 个 positive nonzero，second half 有 362 个 positive nonzero，双半都 positive 的配置有 27 个；但在阈值 `min_pnl_per_split=0.05`、`min_filled_windows_per_split=50`、`min_paired_qty_per_split=1.0`、`max_residual_loss_rate=0.70` 下，qualified_both_positive_config_count=0，status=`FAIL_OOS_POSITIVE_CONFIGS_BELOW_SAMPLE_OR_SIZE`。最佳双半正数配置为 target=0.60、bid=0.10、net=0.5、cutoff=180s、directional_filter=1bps；first half pnl=+0.046、filled_windows=26、paired_qty=0.3，second half pnl=+0.572、filled_windows=70、paired_qty=1.0。结论更新为：已有 OOS 正数苗头，但样本/配对量太薄，仍不能推广；下一步要围绕这个参数族扩大样本和降低残仓风险。
157. 围绕上述 OOS 苗头做了更密集的 target/margin/cutoff/directional-filter refine，结论仍未达到推广阈值。`xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_refine_*.json` 系列先将 matched configs 扩到 960，best overlap 为 target=0.58、bid=0.20、net=1.0、cutoff=150s、directional_filter=2bps，双半 sum_pnl=+1.726，但 first half 只有 34 filled windows、0.6 paired_qty；`xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_refine_margin_*.json` 系列再扩到 2304 configs，second half 很强（2251 个 positive nonzero，best pnl=+2.45），first half 仍是瓶颈。最新 compare `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_refine_margin_20260518T0545Z/manifest.json` 报告 first_positive_nonzero=359、second_positive_nonzero=2251、both_positive=359，但 qualified_both_positive_config_count 仍为 0。最佳 overlap first half pnl=+0.248、filled_windows=34、paired_qty=0.6；second half pnl=+1.474、filled_windows=95、paired_qty=2.8。另查 first half 中 filled_windows>=50 的配置，最佳 pnl 仍为负（约 -0.0432）。结论：当前信号可赚钱的窗口太稀，放大到足够样本会吃掉 edge；下一步必须改信号/训练逻辑，而不是只在现有参数附近继续微调。
158. `src/bin/pair_arb_backtest.rs` 已加入默认关闭的策略选择探针，用于测试“更干净的入场”而不是继续盲目放宽参数：`--directional-entry-min-bps` 要求 risk-increasing first leg 必须有足够 BTC move 才允许开仓，并只允许顺 move 方向开仓；`--directional-price-source price|signal|binance` 允许选择已有 snapshot 中的 BTC price source；同时新增 `--reject-stale`、`--require-ws-fresh`、`--max-quote-age-sec`、`--min-ask-depth` 等报价质量过滤字段。`scripts/xuan_b27_dplus_pair_arb_backtest_grid.py` 与 `scripts/xuan_b27_dplus_pair_arb_oos_compare.py` 已把这些字段写入 JSONL config/key，避免 OOS compare 混淆不同信号逻辑。entry-min 单点 OOS probe `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_20260518T062613Z/manifest.json` 显示：在原最佳族 target=0.58、bid=0.20、net=1.0、cutoff=150s、margin=0.015 上，entry_min=0.5bps 把 first-half pnl 从 +0.248 提到 +0.308、residual_qty 从 6.2 降到 3.0、residual_loss_rate 从 61.3% 降到 53.3%，second-half pnl 提到 +1.752；但 first-half filled_windows 只有 18，因此仍不能推广。
159. 针对 entry-min 逻辑继续放开 target/bid 与 residual salvage 后，仍没有 OOS-qualified 配置。target/bid probe `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_20260518T062705Z/manifest.json` 发现 best overlap 为 target=0.58、bid=0.35、entry_min=0.5bps：first-half pnl=+0.5355、paired_qty=1.05、residual_loss_rate=53.3%，second-half pnl=+3.2485、paired_qty=3.84，但 first-half filled_windows 仍只有 18；first-half 达到 50 filled windows 的 target=0.66 configs 净 PnL 只有约 +0.008。salvage probe `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_20260518T062839Z/manifest.json` 能把 50 filled windows 配置推到 first-half pnl=+0.245、paired_qty=11.55、second-half pnl=+1.472、paired_qty=39.9，但 residual_loss_rate 分别约 100%/94.5%，说明它是用补腿放大 paired_qty，同时留下系统性尾部残仓亏损；提高 salvage cap 到 1.0 后 first-half 转负。更强方向门槛 + 高 target probe `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_20260518T063048Z/manifest.json` 在 50+ filled windows 下也为负。结论：当前版本确实有稀疏正收益信号和可观 paired PnL，但残仓尾部仍吞噬收益，不能进入 G2 canary；下一步应改训练/信号结构以减少未配对残仓，而不是再做同一族局部微调。
160. 已修正 readiness/status smoke 对探索性 backtest 正收益的误判风险。随着 entry-min/salvage probes 找到若干单 split 或双 split 正 PnL 但不合格的配置，旧 smoke 仍要求 `pair_arb_backtest_positive_nonzero_run_count=0` 和 best pnl < 0，这会把“已发现稀疏正数苗头”误当成 smoke failure。现在 `scripts/xuan_b27_dplus_canary_readiness_plan.py` 明确用 OOS compare 的 `oos_validation_passed=false` 维持 `requires_strategy_retrain_or_positive_backtest_config=true`；`scripts/xuan_b27_dplus_canary_readiness_plan_smoke.sh` 与 `scripts/xuan_b27_dplus_local_status_bundle_smoke.sh` 改为允许单 grid 出现探索性正数，但必须继续断言 `qualified_both_positive_config_count=0`、`pair_arb_oos_compare_validation_passed=false`、`ready_for_explicit_g2_canary_approval=false`。同时 readiness/status/launch plan 的依赖型 smoke 查找现在优先选择最新 PASS artifact，避免刚产生的 FAIL smoke 调试产物污染下一次 readiness 判定。最新恢复通过的 gates：`xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260518T063843Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260518T063854Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_g2_canary_launch_plan_smoke_20260518T063854Z/manifest.json`。
161. 已把“有残仓时只补对侧”的 residual-control 逻辑加入本地 backtest 和 no-order shadow runner。`src/bin/pair_arb_backtest.rs` 新增默认关闭参数 `--pairing-only-when-residual`、`--entry-pair-max-ask-sum`、`--require-two-sided-entry`；`scripts/xuan_b27_dplus_pair_arb_backtest_grid.py` 与 `scripts/xuan_b27_dplus_pair_arb_oos_compare.py` 已把这些字段写入 config/key。`tools/xuan_dplus_passive_passive_shadow_runner.py` 也新增 `--pairing-only-when-residual`，当同侧 exposure 已大于对侧 exposure+dust 时拒绝继续 seed 同侧，仅允许后续 public flow 触发对侧补配；该变更仍只影响 no-order 虚拟 shadow，不发送 order/cancel/redeem。
162. residual-control backtest 首次给出 OOS-qualified positive 配置，但还需要真实 no-order shadow 复验。first split artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_20260518T064511Z/manifest.json` 与 second split artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_backtest_json_grid_20260518T064557Z/manifest.json` 在 target 0.58/0.62/0.66/0.70、bid 0.20/0.35、entry_min 0/0.5/1.0、pairing-only residual 等 576 configs 上比较；严格 OOS compare `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_20260518T064746Z/manifest.json` 为 `PASS_OOS_QUALIFIED_POSITIVE_CONFIGS`，qualified configs=14，阈值包含 min pnl/split 0.05、min filled windows/split 50、min paired qty/split 1.0、max residual loss rate 0.70、max residual window rate 0.08、max weighted avg pair cost 0.70、max avg residual cost/window 0.025。best qualified config 为 target=0.66、bid=0.35、cutoff=120s、margin=0.015、directional_entry_min=0.5bps、pairing_only_when_residual=true、无 salvage：first split pnl=+0.434、80 filled windows、paired_qty=2.8、weighted pair cost=0.5825、residual_window_rate=2.70%；second split pnl=+3.237、204 filled windows、paired_qty=11.89、weighted pair cost=0.6321、residual_window_rate=6.58%。这是目前第一条符合本地 OOS 阈值的 backtest 证据。
163. 尽管 OOS backtest 已出现合格 residual-control 配置，canary 仍被真实 shadow trading acceptance 阻断。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T065323Z/manifest.json` 报告 `canary_pair_arb_oos_compare_status=PASS_OOS_QUALIFIED_POSITIVE_CONFIGS`、`canary_pair_arb_oos_compare_qualified_both_positive_config_count=14`、`canary_requires_strategy_retrain_or_positive_backtest_config=false`，但顶层仍为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`，next gate 已改成“用同一 residual-control 逻辑跑更大的 no-order shadow trading 并通过验收；若 shadow 失败再 retrain/tune”。本机 shared-ingress preflight `xuan_research_artifacts/xuan_b27_dplus_shared_ingress_preflight_local_shadow_probe_20260518T065048Z/manifest.json` fail-closed 为 `UNAVAILABLE` / `broker_manifest_missing`，因此本轮没有启动 local shadow runner、没有连接 broker、没有写 shared-ingress、没有 order/cancel/redeem。
164. 已新增四段 walk-forward backtest compare，并把该证据接入 readiness/status。新增 `scripts/xuan_b27_dplus_pair_arb_walkforward_compare.py`，只读取本地 JSONL backtest grids，不启动网络、不读 raw/replay、不触碰 order/cancel/redeem；它要求同一配置在 3+ 时间切片中全部正 PnL，并同时满足每段最小 PnL、filled windows、paired qty、最大 residual loss rate、residual window rate、weighted pair cost 和平均 residual cost/window。四段 focused grid 分别为 `xuan_b27_dplus_pair_arb_backtest_json_grid_walk_q1_20260518T0713Z` 到 `..._q4_20260518T0713Z`，walk-forward compare artifact `xuan_research_artifacts/xuan_b27_dplus_pair_arb_walkforward_compare_20260518T0713Z/manifest.json` 报告 `PASS_WALKFORWARD_QUALIFIED_POSITIVE_CONFIGS`：72 个 matched configs 中 4 个四段全正、2 个合格。最佳合格配置为 target=0.64、bid=0.35、cutoff=90s、margin=0.02、directional_entry_min=0.75bps、pairing_only_when_residual=true、无 salvage；四段 PnL 分别约 +0.0245/+0.0595/+0.823/+1.512，sum_pnl=+2.419，min_filled_windows=38，min_paired_qty=1.05，max residual_loss_rate=54.35%，max residual_window_rate=8.18%，max weighted pair cost=0.6202。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T072503Z/manifest.json` 已暴露 `canary_pair_arb_walkforward_compare_status=PASS_WALKFORWARD_QUALIFIED_POSITIVE_CONFIGS`、`qualified_all_positive_config_count=2`、`validation_passed=true`，但仍保持 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`，因为下一关仍是更大 no-order shadow trading acceptance。
165. 已按 backtest 数据使用规则修正上述 OOS/walk-forward 证据口径：本轮 positive 数字来自 `/Users/hot/web3Scientist/poly_trans_research/data/snapshots/btc5m.db` 的 `local_sqlite_snapshot_btc5m`，不是当前声明可推广的 strict/cache 或 completion/account-truth 数据集。最新重新生成的报告均在 manifest 顶层声明 `data_root`、`dataset_type`、`labels`、`days`、`market_prefix/assets`、`row_count`、`excluded_20260514_20260515`、`contains_20260518=false`、`includes_public_account_execution_truth_v1=false` 和 `can_support_strategy_promotion=false`。OOS compare `xuan_research_artifacts/xuan_b27_dplus_pair_arb_oos_compare_datascope2_20260518T0744Z/manifest.json` 为 `PASS_OOS_QUALIFIED_POSITIVE_CONFIGS_DATASET_SCOPE_LIMITED`：数值 OOS 仍为正，但 `oos_validation_passed=false`、`requires_compliant_backtest_dataset_for_promotion=true`。walk-forward compare `xuan_research_artifacts/xuan_b27_dplus_pair_arb_walkforward_compare_datascope2_20260518T0744Z/manifest.json` 同理为 `PASS_WALKFORWARD_QUALIFIED_POSITIVE_CONFIGS_DATASET_SCOPE_LIMITED`，`walkforward_validation_passed=false`。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T074339Z/manifest.json` 因此重新报告 `canary_requires_strategy_retrain_or_positive_backtest_config=true`，next gate 是在合规声明的 strict/cache/completion 数据上复跑，或通过更大 no-order shadow trading acceptance；不能把本地 BTC SQLite 数字写成全量、多资产、account truth、可部署或 canary-ready 结论。
166. 已新增合规 backtest 输入 preflight，避免在当前机器未挂载 strict/cache、completion store 或 public-account truth 时继续误跑本地 SQLite。`scripts/xuan_b27_dplus_compliant_backtest_input_preflight.py` 只检查允许路径的 manifest/duckdb 文件存在性，不读取 DuckDB 表、不扫描 raw/replay、不启动网络或交易路径；`scripts/xuan_b27_dplus_compliant_backtest_input_preflight_smoke.sh` 覆盖 fixture PASS、缺 root 阻断、20260514 forbidden label 阻断。真实 preflight `xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_input_preflight_20260518T0802Z/manifest.json` 为 `BLOCKED_COMPLIANT_BACKTEST_INPUTS_UNAVAILABLE`：本机缺少 `/mnt/poly-cache/taker_buy_signal_core_v2_strict_l1`、`/mnt/poly-verification-store/completion_unwind_event_store_v2`、`/mnt/poly-verification-store/public_account_execution_truth_v1/20260502_20260513`，strict/completion ready label count 均为 0，public truth 不可用。`scripts/xuan_b27_dplus_local_status_bundle.py` 现在把这些字段提升到顶层；最新 bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T080412Z/manifest.json` 报告 `compliant_backtest_input_status=BLOCKED_COMPLIANT_BACKTEST_INPUTS_UNAVAILABLE`、`compliant_backtest_can_run_locally=false`。结论：下一步若要做可晋级 backtest，先让合规 store 在本机可见，或走明确批准的只读 metadata discovery；否则只能继续写 scope-limited research。
167. 已新增合规 backtest run-plan，使“数据没挂载”和“现有 runner 输入类型不合规”成为两个独立 blocker。`scripts/xuan_b27_dplus_compliant_backtest_run_plan.py` 只读取本地 compliant input preflight，不启动网络、不读 DuckDB 表、不扫 raw/replay、不运行 backtest；当前真实 run-plan `xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_run_plan_20260518T084204Z/manifest.json` 为 `BLOCKED_COMPLIANT_BACKTEST_INPUTS_UNAVAILABLE`。即使后续 strict/cache、completion store 和 public truth root 可见，该 plan 也会先要求实现 compliant store adapter，因为现有 `src/bin/pair_arb_backtest.rs` 的输入是 `local_sqlite_snapshot_btc5m_market_ticks`，而可晋级证据要求 `declared_strict_cache_plus_completion_store`。`scripts/xuan_b27_dplus_local_status_bundle.py` 已把 `compliant_backtest_run_plan_status`、`compliant_backtest_ready_to_run`、`compliant_backtest_requires_store_adapter`、`compliant_backtest_store_adapter_ready`、`compliant_backtest_existing_runner_input_type` 和 `compliant_backtest_required_dataset_type` 提升到顶层；最新 bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T084204Z/manifest.json` 报告 `compliant_backtest_ready_to_run=false`、`compliant_backtest_requires_store_adapter=true`、`compliant_backtest_store_adapter_ready=false`。最新 smokes `xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_input_preflight_smoke_20260518T084143Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_run_plan_smoke_20260518T084143Z/manifest.json` 和 `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_smoke_20260518T084143Z/manifest.json` 均 PASS；`cargo test xuan_b27_dplus --lib` 44 passed（仅既有 warning）。结论：下一步本地开发重点应是 compliant store adapter 或让合规 store 可见，而不是继续把本地 BTC SQLite scope-limited 数字包装成晋级证据。
168. 已新增 backtest report data-scope audit，防止后续 OOS/walk-forward 报告漏声明数据范围或把 scope-limited 结果误标为可晋级。`scripts/xuan_b27_dplus_backtest_report_scope_audit.py` 默认读取最新 OOS compare、walk-forward compare 及其引用的 split grid manifest，只检查 manifest 元数据，不读 DuckDB、不扫 raw/replay、不启动网络或交易路径；它要求每个报告声明 `data_root`、`dataset_type`、`labels`、`days`、`market_prefix/assets`、`row_count`、是否排除 20260514/20260515、是否包含 20260518、是否包含 `public_account_execution_truth_v1`。scope-limited compare 若数值通过，必须仍写 `*_DATASET_SCOPE_LIMITED`、`*_validation_passed=false`、`requires_compliant_backtest_dataset_for_promotion=true`。真实 audit `xuan_research_artifacts/xuan_b27_dplus_backtest_report_scope_audit_20260518T084747Z/manifest.json` 为 `PASS_BACKTEST_REPORT_SCOPE_AUDIT`：审计 8 个 manifest，8 个均为 scope-limited，promotion-supported report 为 0。smoke `xuan_research_artifacts/xuan_b27_dplus_backtest_report_scope_audit_smoke_20260518T084803Z/manifest.json` 覆盖 PASS、缺声明失败和本地 SQLite 误晋级失败；最新 bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T084949Z/manifest.json` 暴露 `backtest_report_scope_audit_passed=true`、`backtest_report_scope_requires_compliant_dataset=true`。结论不变：D+ 不能进入 G2 canary；下一步是 compliant store adapter / 合规 store 可见 / 更大 no-order shadow acceptance。
169. 合规输入 preflight 现在同时识别 `/tmp/xuan_frontier_data/completion_unwind_event_store_v2/20260509..13` 这种 scope-limited completion store，但明确禁止把它当晋级证据。更新后的 `scripts/xuan_b27_dplus_compliant_backtest_input_preflight.py` 仍以 `/mnt/poly-cache/taker_buy_signal_core_v2_strict_l1`、`/mnt/poly-verification-store/completion_unwind_event_store_v2` 和 `/mnt/poly-verification-store/public_account_execution_truth_v1/20260502_20260513` 作为 promotion-grade 输入要求；真实 preflight `xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_input_preflight_20260518T085240Z/manifest.json` 继续 `BLOCKED_COMPLIANT_BACKTEST_INPUTS_UNAVAILABLE`，但同时报告 `scope_limited_research_completion_available=true`、ready label count=5，结论范围只能是 `BTC strict-V2 20260509..20260513 research conclusion only`。最新 run-plan `xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_run_plan_20260518T085255Z/manifest.json` 仍为 `BLOCKED_COMPLIANT_BACKTEST_INPUTS_UNAVAILABLE`，最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T085255Z/manifest.json` 暴露这些字段。结论：可以继续用该 `/tmp` completion store 做研究/adapter fixture，但不能解除 canary 或 promotion blocker。
170. 已新增 `/tmp` completion store schema/coverage probe，为后续 adapter 研究提供实际字段确认，但继续保持 scope-limited。`scripts/xuan_b27_dplus_completion_store_schema_probe.py` 只以 read-only DuckDB 读取 `/tmp/xuan_frontier_data/completion_unwind_event_store_v2/20260509..13/event_store.duckdb` 的 `completion_unwind_events` 表，记录列集合、row_count、market/day 覆盖、`l1_price_change` 与 `public_trade` 行数，不触碰 raw/replay、网络、shared-ingress、broker 或交易路径。真实 probe `xuan_research_artifacts/xuan_b27_dplus_completion_store_schema_probe_20260518T090143Z/manifest.json` PASS：5 天合计 8,626,226 rows、按日 market_count 合计 1,440、每个 store 92 列，核心 adapter 字段齐全；但 `dataset_type=scope_limited_completion_unwind_event_store_v2`、`can_support_strategy_promotion=false`、`requires_compliant_backtest_dataset_for_promotion=true`。smoke `xuan_research_artifacts/xuan_b27_dplus_completion_store_schema_probe_smoke_20260518T090143Z/manifest.json` PASS；最新 bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T090248Z/manifest.json` 暴露该结果。下一步可用这个 scope-limited store 做 adapter 原型/研究跑分，但最终晋级仍需合规 strict/cache + completion + public truth。
171. 已用 `/tmp` scope-limited completion store 跑 passive/passive research probe，并把结果接入 status bundle，但仍不解除 canary blocker。单日 20260509 probe `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_20260518T091519Z/runner` 8 个配置中 4 个名义 net PnL 为正，best net PnL `+22.638525`，但 `stress100_actual_pnl=-1.23038`、`worst_residual_net_pnl=-143.200975`。五天 20260509..13 基线 probe `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_multiday_20260518T092117Z/runner` 扩大到 8,626,226 rows 后，best net PnL `+112.147885`，但 `stress100_actual_pnl=-8.63026`、`worst_residual_net_pnl=-743.637615`。随后 edge sweep `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_edge_sweep_cap102_20260518T092530Z/runner` 在 `edge=0.025`、`seed_l1_pair_cap=1.02` 下让 12/12 配置名义 net PnL 和 stress100 都转正；最新 summary `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary_20260518T092729Z/manifest.json` 记录 best net PnL `+311.375141`、best stress100 `+178.556971`、pair_qty `5756.097`、residual_qty `1769.623`、qty residual rate `13.32%`，但 `best_worst_residual_net_pnl=-544.410359` 且 `can_support_strategy_promotion=false`。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T093104Z/manifest.json` 暴露 `scope_limited_passive_probe_positive_net_pnl_run_count=12`、`scope_limited_passive_probe_positive_stress100_run_count=12`，同时保持 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：更强 passive discount 是有效研究方向，但当前证据仍只是 BTC strict-V2 20260509..13 scope-limited completion-store research；没有 public account execution truth、没有 promotion-grade strict/cache+completion dataset、没有更大 no-order shadow acceptance，且 worst-residual tail 仍需继续压缩。
172. 已完成 residual-tail / break-even scope-limited probe，结果说明当前改善主要来自更厚 passive discount，而不是残仓量下降。`xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_residual_tail_sweep_20260518T100416Z/runner` 扫 `edge=0.03/0.04/0.05`、target 1/2、imbalance cap 1/2、residual cooldown cap 1/2；target=1 与 imbalance cap=1 过严，36/48 configs 为 zero-seed。非零配置中 edge=0.05 把 best net PnL 推到 `+642.907525`、stress100 `+510.089355`，但 residual_qty 仍为 `1769.623`、qty residual rate 仍 `13.32%`，best worst-residual 仍 `-212.877975`。随后 break-even probe `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_edge_break_even_20260518T101115Z/runner` 扫 edge=0.06/0.07；最新 summary `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary_20260518T101509Z/manifest.json` 报告 edge=0.07 时 best net PnL `+905.888115`、stress100 `+773.069945`、best worst-residual `+50.102615`，但 `stress100_worst_pnl=-82.715555`，positive_worst_residual_run_count=4、positive_stress100_worst_run_count=0，且 residual_qty 仍未下降。`scripts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary.py` 与 local status bundle 已改为忽略 zero-seed configs 的 best/top 排名，并暴露 positive_worst_residual / positive_stress100_worst 字段；最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T101524Z/manifest.json` 仍为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：约 7% 被动成交折价才刚覆盖 worst-residual，且 stress-worst 仍不过；这更像可行性压力阈值，不是可部署成交假设。下一步应改残仓机制/对冲或等待更大 no-order shadow 验证，而不是把高折价 scope-limited 结果当 canary 依据。
173. 已新增 D+ autoresearch loop runbook，明确后续不再以补 gate 为主要进展，而以可证伪策略假设、统一 scoreboard、并行 subagent lane 和 kill criteria 推进。新文档 `docs/research/xuan/DPLUS_AUTORESEARCH_LOOP_ZH.md` 固定要求 backtest/shadow/probe 报告声明 data scope，并把 `pair_actions`、`pair_qty`、`pair_cost_wavg`、`residual_qty`、`qty_residual_rate`、`net_pnl`、`stress100_actual_pnl`、`worst_residual_net_pnl`、`stress100_worst_pnl`、`positive_stress100_worst_run_count` 等作为核心 scoreboard；promotion gate 必须同时满足合规数据、OOS/walk-forward、larger no-order shadow、残仓尾部和 source-of-truth gates。文档把并行工作拆成五条 disjoint lane：Residual Tail Research、Signal/Selection Research、Shadow Trading Metrics、Compliant Data Adapter、Gate/Status Hygiene，并要求 heartbeat 每次只输出 `EXPERIMENT_DONE`、`BLOCKED` 或 `NOOP`。当前直接结论：不再继续只靠提高 edge，应优先降低 residual_qty / stress100_worst tail，过滤 zero-seed sweep，研究 residual repair、two-sided entry、late cutoff、side-imbalance gating，并在 shared-ingress read-only client 可用时跑更大 no-order shadow report。

174. 已按同事指出的主机边界修正 no-order shadow：本地 Mac 不再尝试读取 EC2 Unix socket，而是在拥有 shared-ingress socket 的 EC2 上以 `pmofi` 同机运行，并显式设置 `PM_SHARED_INGRESS_ROOT=/srv/pm_as_ofi/shared-ingress-main`。远端只读核验确认真实 root 下存在 `broker_manifest.json`、`chainlink.sock`、`local_price.sock`、`market.sock`，broker PID 为 `954720`；旧 worktree runner 不支持 `--pairing-only-when-residual`，因此本轮仅把当前 runner 单文件复制到隔离的 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_residual_control_20260518T114638Z` 下运行，没有改 repo、shared-ingress、broker、service 或 env。实验假设是：same-host explicit root + residual-control pairing 可以降低配对成本尾部并保持正 pair PnL。真实 aggregate `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T114638Z/remote/output/aggregate_report.json` 覆盖 12 个 slug，1800s，offsets 0..11，edge 0.05，target_qty 2，`pairing_only_when_residual=true`，结果为 23 candidates、18 queue-supported fills、10 pair actions、pair_qty `11.2425`、pair_pnl `+0.719825`、ROI on filled cost `4.64%`、net_pair_cost_p90 `0.96`、residual_qty `9.5825`、residual_cost `4.977`、material_residual_lots `0`，orders/cancels/redeems 全 false。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T122122Z/manifest.json` 为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`：成本尾部已从旧 run 的约 1.07 改善到 0.96，但 candidates 仍低于 100，且残仓接近上限。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T122155Z/manifest.json` 继续报告 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。这是 `EXPERIMENT_DONE`，结论是 residual-control 可保持正 PnL 并改善配对成本，但样本过薄且残仓未修复；下一轮应比较更长 shadow window 或放宽 target/entry 的配置，同时继续显式使用 EC2 同机 broker root。

175. 已完成第二轮 EC2 same-host no-order shadow A/B：把上一轮 `target_qty=2` 放宽回 `target_qty=5`，保留 edge 0.05 与 `--pairing-only-when-residual`，检验“样本薄是否主要来自 target 过小”。实验仍在隔离 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_edge005_target5_20260518T125522Z` 下以 `pmofi` 同机连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock`，未发送 order/cancel/redeem，未改 broker/shared-ingress/service/env。真实 aggregate `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T125522Z/remote/output/aggregate_report.json` 覆盖 12 个 slug、1800s、offsets 0..11，结果：candidates 从上一轮 23 提升到 `51`，queue-supported fills `33`，pair actions `15`，pair_qty `18.81`；但 pair_pnl 只有 `+0.45165`、ROI on filled cost `1.88%`，net_pair_cost_p90 恶化为 `1.09`，residual_qty `19.9525`、residual_cost `5.614325`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T132729Z/manifest.json` 仍为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，且同时有 sample-size、pnl-metric 与 residual-risk failures。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T132802Z/manifest.json` 继续保持 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：放宽 target 可以提高样本，但代价是成本尾部和残仓尾部同时失控；下一步不应继续单纯放 target，而应研究 dynamic target、late cutoff、side-imbalance gating 或 two-sided entry 来减少残仓形成。

176. 已完成第三轮 EC2 same-host no-order shadow：静态 side-imbalance gating。先尝试 `imbalance_qty_cap=1.0` 时发现它与 runner `dust_qty=1.0` 冲突，会在 `imbalance_room <= dust_qty` 处阻断所有 seed，因此仅终止自己的 no-order runner 进程，没有触碰 broker/service。随后真实实验使用 `target_qty=5`、edge 0.05、`--pairing-only-when-residual`、`imbalance_qty_cap=1.25`，仍在隔离 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_edge005_target5_imb125_20260518T140502Z` 下以 `pmofi` 同机连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock`，未发送 order/cancel/redeem，未改 shared-ingress/broker/service/env。真实 aggregate `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T140502Z/remote/output/aggregate_report.json`：12 slugs、1800s、89 candidates、73 queue-supported fills、35 pair actions、pair_qty `35.925`、pair_pnl `+0.018225`、ROI on filled cost `0.045%`、net_pair_cost_p90 `1.18`、residual_qty `18.2875`、residual_cost `4.7893`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T143704Z/manifest.json` 仍为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，同时有 sample-size、pnl-metric 和 residual-risk failures；最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T143728Z/manifest.json` 继续为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：静态 imbalance cap 可以把样本推近 100，并在中段短暂控制残仓，但到 late window 仍出现残仓累积且成本尾部失控；下一步应实现 time-decaying target / late cutoff / dynamic side gating，而不是继续调静态 cap。
177. 已完成第四轮 EC2 same-host no-order shadow：default-off late target。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增 `--late-target-after-s` 与 `--late-target-qty`，默认关闭时旧行为不变；candidate event 会记录 `target_qty`、`base_target_qty` 与 `late_target_active`，并显式拒绝 `imbalance_qty_cap <= dust_qty` 或 late target 小于等于 dust 的 zero-seed 配置。本轮实验假设是：`target_qty=5` 保留早段样本，75s 后降到 `late_target_qty=2`，能相对静态 `imbalance_qty_cap=1.25` 降低 late residual tail。实际 runner 在隔离 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_late_target_20260518T151251Z` 下以 `pmofi` 同机连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock`，`PM_DRY_RUN=true`、shared-ingress client，只复制当前 runner 单文件，不 repo sync/build，不改 broker/shared-ingress/service/env，未发送 order/cancel/redeem。SSH 会话中途关闭但远端 runner 继续完成，最终 artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T151251Z/remote/output/aggregate_report.json`：12 slugs、1800s、54 candidates、45 queue-supported fills、20 pair actions、pair_qty `23.4725`、pair_pnl `+1.429875`、ROI on filled cost `6.18%`、net_pair_cost_p90 `1.05`、residual_qty `11.945`、residual_cost `3.30355`、material_residual_lots `0`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T154711Z/manifest.json` 仍为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，失败项为 candidates 54 < 100、net_pair_cost_p90 1.05 > 1.0、residual_qty 11.945 > 10；最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T154726Z/manifest.json` 继续为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：late target 明显恢复 PnL/ROI，并把 residual cost 降到阈值内，但样本量回落、成本 p90 和 residual_qty 仍略超；下一步不应继续静态 cap/target，而应测试 late hard cutoff 或动态 side gating，目标是在保留 early PnL 的同时把 p90 和 residual_qty 压过阈值。
178. 已完成第五轮 EC2 same-host no-order shadow：hard seed cutoff 90s。实验假设是：沿用第三轮的 `target_qty=5`、edge 0.05、`--pairing-only-when-residual`、`imbalance_qty_cap=1.25`，但把 `seed_offset_max_s` 从 120 收到 90，应该能减少 90s 后新开单边 residual，同时比第四轮 late target 保留更多样本。本轮因启动器写 `runner.pid` 的 shell 重定向被拒，直接恢复监督实际已启动的 runner PID，没有重启或重叠窗口；runner 在隔离 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_hard_cutoff90_20260518T155346Z` 下以 `pmofi` 同机连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock`，未发送 order/cancel/redeem，未改 broker/shared-ingress/service/env。最终 artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T155346Z/remote/output/aggregate_report.json`：12 slugs、1800s、59 candidates、45 queue-supported fills、21 pair actions、pair_qty `24.5125`、pair_pnl `+0.914267`、ROI on filled cost `3.63%`、net_pair_cost_p90 `1.17`、residual_qty `12.5`、residual_cost `4.15`、material_residual_lots `0`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T162505Z/manifest.json` 仍为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，失败项为 candidates 59 < 100、net_pair_cost_p90 1.17 > 1.0、residual_qty 12.5 > 10；最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T162516Z/manifest.json` 继续为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：hard cutoff 90s 只比 late target 多 5 个 candidates，却让 p90 和 PnL 明显变差；该假设 kill。下一步应实现动态 side gating：75s 后只允许降低当前较大 residual 的 opposite-side repair，不允许继续扩大同侧 residual。
179. 已完成第六轮 EC2 same-host no-order shadow：dynamic side repair。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增 default-off `--late-repair-after-s`，含义是在阈值后只允许当前较小 exposure 一侧继续 seed，用 opposite-side repair 降低已有 residual；candidate event 同步记录 `late_repair_active`。实验假设是：在不使用 late target 的情况下，`target_qty=5`、edge 0.05、`--pairing-only-when-residual`、`imbalance_qty_cap=1.25`，并在 75s 后启用 repair-only，可以保留第 4 轮的 PnL/ROI，同时把 residual_qty 压回阈值内。本轮 runner 在隔离 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_late_repair_20260518T163201Z` 下以 `pmofi` 同机连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock`，只复制当前 runner 单文件，不 repo sync/build，不改 broker/shared-ingress/service/env，未发送 order/cancel/redeem。真实 aggregate `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T163201Z/remote/output/aggregate_report.json`：12 slugs、1800s、54 candidates、45 queue-supported fills、29 pair actions、pair_qty `28.17`、pair_pnl `+2.701349`、ROI on filled cost `11.14%`、net_pair_cost_p90 `1.04`、residual_qty `5.21`、residual_cost `1.8841`、material_residual_lots `0`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T170352Z/manifest.json` 仍为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，失败项为 candidates 54 < 100 以及 net_pair_cost_p90 1.04 > 1.0；residual-risk 已通过。最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T170352Z/manifest.json` 继续为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：dynamic repair 明确修复了残仓尾部，并把 PnL/ROI 推到当前最好，但样本没有提升且 p90 仍略超；下一步不应回到 hard cutoff，而应围绕 repair-only 族尝试提高样本或降低 p90，例如更早/更晚 repair 阈值、轻微提高 edge、或按 side-cost 选择 seed。
180. 2026-05-19 主线程同步：backtest 数据已改为本地已下载 cache/store，默认根为 `POLY_BT_ROOT=/Users/hot/web3Scientist/poly_backtest_data`。`docs/research/xuan/BACKTEST_DATA_RULES_20260515_ZH.md` 已改为要求从 `CACHE_MANIFEST.json` / `EVENT_STORE_MANIFEST.json` 自动发现可用 label，不再手写日期；strict V2 cache 根为 `$POLY_BT_ROOT/backtest_cache/taker_buy_signal_core_v2_strict_l1`，completion/unwind store v2 根为 `$POLY_BT_ROOT/verification_store/completion_unwind_event_store_v2`，public account audit 为 `$POLY_BT_ROOT/verification_store/public_account_execution_truth_v1/20260502_20260513/event_store.duckdb`。当前可用 label：strict `20260502_20260507`、`20260508`..`20260513`、`20260516`、`20260517`；completion `20260502_20260508`、`20260509`..`20260513`、`20260516`、`20260517`；public audit `20260502_20260513`。`20260514`/`20260515` 仍 blocklist，`20260518` 需等本地 manifest 出现后才能纳入。`scripts/xuan_b27_dplus_compliant_backtest_input_preflight.py` 现在默认从 `POLY_BT_ROOT` 派生本地根，并在未传 label 时按 manifest 自动 discovery；preflight 仍只看 manifest/文件存在性，不扫 collector/raw/replay、不启动网络或交易路径。当前本地数据适合快速搜索和事件层研究；最终 source-of-truth replay 验证仍需 replay archive 下载后单独排队。
181. 新服务器 shared-ingress 已按主机边界重新核验。只读 SSH probe `xuan_research_artifacts/xuan_b27_dplus_remote_shared_ingress_probe_20260518T173946Z/manifest.json` 连接 `ubuntu@ec2-52-209-13-135.eu-west-1.compute.amazonaws.com`，确认远端主机 `ip-172-31-37-43` 上 `/srv/pm_as_ofi/shared-ingress-main/broker_manifest.json` 存在，broker PID `15360` 存活，`chainlink.sock`、`local_price.sock`、`market.sock` 都是 Unix socket，heartbeat age 约 636ms。该 artifact 明确记录 `local_process_can_use_remote_unix_sockets=false`、`requires_same_host_observer_or_shadow=true`，且 service/systemd/broker/order/cancel/redeem side effects 全 false。后续 no-order shadow 只在 EC2 同机以 `pmofi` 身份、显式 `PM_SHARED_INGRESS_ROOT=/srv/pm_as_ofi/shared-ingress-main` 运行；本地 Mac 不再做远端 socket preflight。
182. 已在本地合规 completion store 上跑 completion-only passive/passive 研究 probe，给出了强正的事件层参照，但仍不能解除 promotion/canary blocker。首轮 4-config probe `xuan_research_artifacts/xuan_b27_dplus_local_completion_passive_probe_20260518T174725Z/runner` 自动发现并使用 `POLY_BT_ROOT=/Users/hot/web3Scientist/poly_backtest_data` 下 8 个 completion labels，覆盖 days `20260502..20260513`、`20260516`、`20260517`，合计 `25,111,732` rows，未包含 `20260514/20260515/20260518`。summary `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary_20260518T175012Z/manifest.json` 报告 `dataset_type=local_poly_backtest_completion_unwind_event_store_v2`、`PASS_LOCAL_COMPLETION_RESEARCH_ONLY`，best config 为 `edge=0.055,target=5,imbalance_qty_cap=1.25`：pair_actions `25216`、pair_qty `28144.9425`、net_pair_cost_wavg `0.887225`、net_pnl `+3354.139587`、stress100_worst_pnl `+1406.443612`、qty_residual_rate `5.07%`。扩展 30-config grid `xuan_research_artifacts/xuan_b27_dplus_local_completion_passive_probe_grid_20260518T175654Z/runner` 与 summary `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary_20260518T180751Z/manifest.json` 显示：按 best net PnL，`edge=0.055,target=3,imbalance=1.5` 可到 `+3995.819001`，但 stress100_worst 转负；按 stress-worst 优先，仍是 `edge=0.055,target=3/5,imbalance=1.25` 最稳。结论：本地 completion 事件层支持 repair/cap=1.25/edge=0.055 方向，但它是 completion-only、未 join strict cache、未使用 public truth、也不是 source-of-truth replay，因此 `can_support_strategy_promotion=false`。
183. 已补 public-account audit proxy truth 的只读 schema/coverage probe，供后续 compliant adapter join 使用。`xuan_research_artifacts/xuan_b27_dplus_public_account_truth_schema_probe_20260518T175511Z/manifest.json` 读取 `$POLY_BT_ROOT/verification_store/public_account_execution_truth_v1/20260502_20260513/event_store.duckdb` 的 `public_account_execution_events`，row_count `562087`，覆盖 `20260502..20260513`，`account_label` 分布为 b27bc `551392`、rwo `10695`；event_kind 分布为 fill `556908`、redeem `2510`、settlement `2470`、merge `199`；order_type 中 maker `47686`、taker `46825`、unknown `467576`。该 probe 明确是 `public_account_audit_proxy_truth_not_private_owner_trade_truth`，`includes_public_account_execution_truth_v1=true` 但 `can_support_strategy_promotion=false`，因为最终晋级仍需要私有/回放 source truth 和更大 no-order shadow acceptance。
184. 已完成第七轮新 EC2 same-host no-order shadow：repair-only + edge 0.055 + 3600s。实验假设是：沿用第六轮 dynamic side repair 的 residual 修复逻辑，把 edge 从 0.05 提到 0.055 并把 window 拉长到 3600s，可以在保持正 PnL/ROI 的同时让 candidates 过 100。本轮在新服务器 `ec2-52-209-13-135.eu-west-1.compute.amazonaws.com` 上以 `pmofi` 从隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_late_repair_edge0055_20260518T174308Z` 运行，显式设置 `PM_SHARED_INGRESS_ROOT=/srv/pm_as_ofi/shared-ingress-main`、`PM_SHARED_INGRESS_ROLE=client`、`PM_DRY_RUN=true`，未发送 order/cancel/redeem，未改 broker/shared-ingress/service/env。已拉回小型报告到 `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260518T174308Z/remote/output/aggregate_report.json`，未拉取 `*.events.jsonl`。真实 metrics：12 slugs、3600s、103 candidates、82 queue-supported fills、45 pair actions、pair_qty `50.46`、pair_pnl `+4.15205`、ROI on filled cost `9.52%`、net_pair_cost_p90 `1.02`、residual_qty `21.4825`、residual_cost `7.595037`、material_residual_lots `0`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260518T184612Z/manifest.json` 为 `FAIL_SHADOW_TRADING_PNL_METRICS`：sample-size 已通过，但 p90 成本 `1.02 > 1.0`，residual_qty/residual_cost 仍超阈值；最新 status bundle `xuan_research_artifacts/xuan_b27_dplus_local_status_bundle_20260518T184845Z/manifest.json` 仍为 `CANARY_NOT_READY_SHADOW_TRADING_ACCEPTANCE_FAILED`。结论：提高 edge + 延长窗口解决了样本量，并给出当前最强正 PnL，但 residual tail 明显恶化；下一步应在 repair-only 家族上压 residual_qty/cost 和 p90，而不是继续单纯延长时长或提高 target。
185. 已完成一轮本地 completion-store cost-cap falsification。`tools/xuan_d_branch_passive_passive_redeem.py` 新增 `--imbalance-cost-caps` 多值网格参数，兼容旧的 `--imbalance-cost-cap` 单值参数；该变更仅影响离线研究工具，不触碰 runtime、broker、shared-ingress 或交易路径。实验假设是：第七轮 shadow 的 residual_cost 超线可能来自同侧高成本 exposure 未被 cost cap 限住，因此在本地 `POLY_BT_ROOT` completion store 上扫描 `edge=0.055`、`target_qty=5`、`imbalance_qty_cap=1.25`，并把 `imbalance_cost_cap` 从 `0.25` 扫到无限大。runner artifact 为 `xuan_research_artifacts/xuan_b27_dplus_local_completion_passive_probe_imbcost_sweep_20260519T020607Z/runner`，summary `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary_20260519T021049Z/manifest.json` 使用 8 个本地 completion labels、`25,111,732` rows，排除 `20260514/20260515/20260518`，状态仍是 `PASS_LOCAL_COMPLETION_RESEARCH_ONLY`、`can_support_strategy_promotion=false`。结果：10 个 cost-cap 配置的核心 metrics 完全相同，net_pnl `+3354.139587`、stress100_worst `+1406.443612`、worst_residual `+1999.434587`、residual_qty `3009.2125`、residual_cost `1174.608413`、qty_residual_rate `5.07%`；变化只体现在 block reason 从 `imbalance_qty` 转移到 `imbalance_cost`。结论：当前 completion model 中 cost cap 不是可改善 residual 的有效旋钮，直接上 EC2 跑 cost-cap shadow 不值得；下一步应测试真正改变 seed/repair 结构的机制，例如 side-cost-aware price filter、repair 后 residual budget、或更低 seed size/target 的样本恢复方案。
186. 已重新验证本地 `POLY_BT_ROOT` 合规输入并把 completion schema probe 从旧 `/tmp` scope-limited 模式升级为 manifest-discovered local store 模式。`xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_input_preflight_20260519T022043Z/manifest.json` 现在报告 `PASS_COMPLIANT_BACKTEST_INPUTS_AVAILABLE`：strict V2 cache ready labels `9`，completion store ready labels `8`，public-account audit proxy truth ready，missing_roots 为空，且仍未扫描 raw/replay 或启动网络/交易路径。`xuan_research_artifacts/xuan_b27_dplus_compliant_backtest_run_plan_20260519T022102Z/manifest.json` 因此从“inputs unavailable”推进到 `BLOCKED_COMPLIANT_BACKTEST_ADAPTER_NOT_IMPLEMENTED`，下一 gate 是实现/审查 strict-cache + completion-store adapter，而不是再等待数据。`scripts/xuan_b27_dplus_completion_store_schema_probe.py` 现在默认按 `POLY_BT_ROOT` 自动发现 `EVENT_STORE_MANIFEST.json` labels，并兼容多日 label；最新 schema probe `xuan_research_artifacts/xuan_b27_dplus_completion_store_schema_probe_20260519T022259Z/manifest.json` 为 `PASS_LOCAL_COMPLETION_STORE_SCHEMA_PROBE`，覆盖 labels `20260502_20260508`、`20260509`..`20260513`、`20260516`、`20260517`，days `2026-05-02`..`2026-05-13` 加 `2026-05-16/17`，row_count `25,111,732`，market_count_sum_by_day `4032`。该 probe 仍标记 `includes_public_account_execution_truth_v1=false`、`can_support_strategy_promotion=false`，因为它只是 completion-only schema/coverage；promotion 仍需要 strict-cache join、public audit/proxy-truth cross-check 和最终 source-of-truth replay validation。
187. 已完成第八轮新 EC2 same-host no-order shadow：aggressive repair/seed filter 被快速否决。实验假设来自 subagent artifact review：`edge=0.060`、`seed_l1_cap=1.00`、`late_repair_after_s=60`、`salvage_age_s=15`、`salvage_net_cap=0.95` 可能降低 p90 和 residual tail，同时保留足够样本。本轮在新服务器隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_aggressive_repair_20260519T021321Z` 以 `pmofi` 同机连接 `/srv/pm_as_ofi/shared-ingress-main/market.sock`，`PM_DRY_RUN=true`、shared-ingress client，未发送 order/cancel/redeem，未改 broker/shared-ingress/service/env。运行约 18 分钟后 12 个 summary 仍全部为 0 candidates / 0 fills / 0 pairs，因此只终止该 no-order runner 自身并用已有 summary 生成正式 aggregate；拉回的小型 artifact 为 `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260519T021321Z/remote/output/aggregate_report.json`，未拉取 events JSONL。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260519T023007Z/manifest.json` 为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，metrics：12 slugs、candidates `0`、queue-supported fills `0`、pair_actions `0`、pair_pnl `0`、residual_qty `0`。结论：`seed_l1_cap=1.00` 与 edge 0.060 在当前 live shared-ingress 条件下过严，连 seed candidate 都无法形成；该分支 kill，下一轮不应继续收紧 seed_l1_cap，而应在 repair-only 家族上用更温和的 side-cost-aware price filter 或 explicit residual budget 做结构性修改。
188. 已完成本地 compliant adapter join feasibility probe。新增 `scripts/xuan_b27_dplus_compliant_adapter_join_probe.py`，只读取 `POLY_BT_ROOT` 下 manifest-discovered strict cache、completion store 与 public-account audit DuckDB，不扫 raw/replay，不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_adapter_join_probe_20260519T024125Z/manifest.json` 为 `PASS_STRICT_COMPLETION_JOIN_FEASIBLE_PUBLIC_AUDIT_PARTIAL`：strict rows `169,494`、completion rows `25,111,732`、public audit rows `562,087`；strict market-day keys `4,018` 与 completion overlap `4,018`，overlap rate `1.0`，说明 strict/cache + completion adapter join 在本地数据上可行。关键限制是 public audit 只覆盖 `2026-05-02..2026-05-13`，strict days missing public audit 为 `2026-05-16`、`2026-05-17`，strict-public overlap rate 只有 `0.546292`。结论：下一步 adapter 应优先实现 strict-cache + completion join，并把 public-audit covered 子样本（05-02..13）与 05-16/17 分开报告；不能把 05-16/17 当成 public-truth 覆盖样本。

189. 已完成本地 strict-cache/completion 行级 30s label alignment probe，结论是 join 覆盖够高但标签语义不能直接等价替代。新增 `scripts/xuan_b27_dplus_strict_completion_join_label_probe.py`，只读取 `POLY_BT_ROOT` 下 manifest-discovered strict cache、completion store 与 public-account audit DuckDB，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_strict_completion_join_label_probe_20260519T031711Z/manifest.json` 为 `FAIL_STRICT_COMPLETION_JOIN_LABEL_ALIGNMENT`：strict candidates `169,494`，joined candidates `169,493`，join coverage `0.999994`；但 exact match rate 只有 `0.756598`，within half-cent rate `0.818122`，within one-cent rate `0.894208`，joined pair <=1.00 比 cache 多 `2,556` 条。event-kind variant 中 `public_trade` 最好，但 half-cent 也只有 `0.834760`，one-cent `0.902910`，仍不过阈值；fail reasons 为 `within_half_cent_rate_below_0_90`、`within_one_cent_rate_below_0_95`、`no_event_kind_variant_reaches_half_cent_threshold`。结论：strict/cache 与 completion store 可以按 day/condition/time window 连接，但 completion event row 不能直接重构 strict cache 的 `min_pair_cost_30s` 标签；后续 adapter 需要显式定义新标签语义、分 public-audit-covered 子样本报告，并禁止把该 join 输出当成 source-of-truth replay 或可晋级证据。

190. 已完成本地 completion-store residual cooldown falsification。实验假设是：在 `edge=0.055,target=5,imbalance_qty_cap=1.25` 的当前强正 completion-only 家族上，对 30s 以上 aged residual 设置同侧 seed cooldown cost cap（0.5/1.0/1.5/2.0）应能减少 residual tail。真实 runner artifact `xuan_research_artifacts/xuan_b27_dplus_local_completion_passive_probe_residual_cooldown_20260519T034001Z/runner/combined_results.json` 与 summary `xuan_research_artifacts/xuan_b27_dplus_scope_limited_completion_passive_probe_summary_20260519T034256Z/manifest.json` 仍只读取 `POLY_BT_ROOT` 下 8 个 allowed completion labels，覆盖 `25,111,732` rows，排除 20260514/20260515/20260518，不扫 raw/replay、不启动网络/交易路径。结果：最严格 `residual_cooldown_age_s=30, residual_cooldown_cost_cap=0.5` 只新增 `936` 个 `seed_block_residual_cooldown`，但 seed_actions 仍 `47,768`，pair_qty `28,144.9425`、residual_qty `3,009.2125`、residual_cost `1,174.608413`、net_pnl `+3,354.139587`、stress100_worst_pnl `+1,406.443612` 全部与无 cooldown 基线相同。结论：当前 completion model 中 residual cooldown 不是有效 residual repair knob，和 imbalance-cost cap 一样不值得直接消耗 EC2 shadow 窗口；下一步需要真正改变 seed/repair 结构，例如 side-cost-aware price filter、late repair 的 residual budget，或 strict/completion adapter 上的新标签策略。

191. 已完成第一轮 compliant metrics runner 描述性跑分，结果直接否定“completion-only 强正可以直接迁移到 strict-first 触发模型”。`scripts/xuan_b27_dplus_compliant_metrics_runner.py` 读取本地 `POLY_BT_ROOT` strict cache、completion store 和 public-account audit proxy truth，并要求已通过 input preflight 与 adapter join probe；本轮真实 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_metrics_runner_20260519T041414Z/manifest.json` 状态为 `DISCARD_NO_FEE_POSITIVE_COMPLIANT_METRICS`，输入安全、无 raw/replay/network/order/cancel/redeem/shared-ingress/broker/service side effects。样本是 `max_candidates_per_label_pair=1000` 的 bounded run，9 个 configs（clips 10/25/60，pair caps 0.94/0.96/0.98）全部 nonzero，但 `positive_fee_worst_case_run_count=0`、`low_residual_positive_run_count=0`。最佳配置为 `clip=10,pair_cap=0.98,completion_window=30s,first_max_price=0.60`：candidate_count `6,705`，completion_rate `0.863833`，pair_qty `57,920`，residual_qty `9,130`，qty_residual_rate `0.136167`，net_pair_cost_wavg `0.968252`，pair_gross_pnl `+1,838.836110`，但 residual_cost `5,007.745210` 和 fee 之后 fee_worst_case_pnl `-4,897.725228`，stress100_worst_pnl `-3,779.798191`，worst_day_fee_worst_case_pnl `-956.867592`，positive_daily_count `0/9`。public audit 仍缺 `2026-05-16/17`。结论：当前 D+ 的可用正信号主要来自 completion-only 事件层，不足以支持 strict-first 触发 + future completion 的可推广策略；下一步应在 compliant runner 上搜索更严格的一腿选择/更短 completion window/更低 first price cap 或新标签策略，而不是把 completion-only PnL 当晋级证据。

192. 已完成 compliant strict-first first-price-cap sweep，结果显示有一个很窄的低价首腿正信号，但样本太薄且日级不稳，不能晋级。两组 bounded runner 均读取本地 `POLY_BT_ROOT` strict cache、completion store 和 public-account audit proxy truth，使用通过的 preflight/join probe，不扫 raw/replay、不启动网络或交易路径。`first_max_price=0.45` sweep artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_metrics_runner_firstpx045_paircap_sweep_20260519T044014Z/manifest.json`：6 个 configs 中 `positive_fee_worst_case_run_count=3`，最佳为 `clip=10,pair_cap=0.92`，candidate_count 仅 `132`，completion_rate `0.848485`，qty_residual_rate `0.151515`，net_pair_cost_wavg `0.890800`，fee_worst_case_pnl `+7.527434`，stress100_worst_pnl `+27.325400`，但 worst_day_fee_worst_case_pnl `-13.465085`、positive_daily_count `8/14`，且 `low_residual_positive_run_count=0`。`first_max_price=0.50` sweep artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_metrics_runner_firstpx050_paircap_sweep_20260519T044133Z/manifest.json`：candidate_count 放大到 `3,499..4,813` 后 `positive_fee_worst_case_run_count=0`，最佳 `pair_cap=0.98` 仍为 fee_worst_case_pnl `-3,266.611715`、qty_residual_rate `0.148994`、worst_day `-710.736479`。结论：低价首腿过滤确实改变了 strict-first economics，但有效区间窄且样本量不足；下一步应在 `first_max_price 0.45..0.50` 之间做更细分分层，并加入 day-stability / public-audit-covered 子样本约束，而不是把 `0.45` 的全局小正数当成可部署信号。

193. 已完成 compliant strict-first first-price-cap 细分 sweep，结论是 `0.45` 正信号不是可扩张 plateau，而是样本很薄的尖点。四个新增 bounded artifacts 均只读本地 `POLY_BT_ROOT` strict cache、completion store 和 public-account audit proxy truth，使用通过的 preflight/join probe，不扫 raw/replay、不启动网络或交易路径：`xuan_b27_dplus_compliant_metrics_runner_firstpx046_paircap_sweep_20260519T051020Z`、`firstpx047_20260519T051024Z`、`firstpx048_20260519T051028Z`、`firstpx049_20260519T051032Z`。核心结果：`first_max=0.46` 最佳 `pair_cap=0.94`，candidate_count `176`，fee_worst_case_pnl `-6.589163`，stress100_worst_pnl `+20.756535`，worst_day `-16.096708`；`0.47` 最佳 `pair_cap=0.92`，candidate_count `221`，fee_worst_case_pnl `-12.359849`；`0.48` candidate_count `318`，fee_worst_case_pnl `-56.115266` 且 stress100_worst 已转负；`0.49` candidate_count `649`，qty_residual_rate 降到 `0.055470` 但 fee_worst_case_pnl `-243.939802`、positive_daily_count `0/14`。与上一轮 `0.45` 的 `+7.527434`、132 candidates 对比，一放宽到 0.46 就失去全局 fee-worst positive。结论：低 first price cap 本身不是可扩样策略；后续若继续该方向，必须换成更细的条件组合（例如低价首腿 + day/time/market-state 分层或 public-audit-covered 子样本），而不是把 `first_max` 单旋钮继续推向 EC2 shadow。
194. 已发现并修正 compliant strict-first metrics runner 的 completion window 语义问题；此前 `first_max=0.45` 的小正数应视为 false-positive risk，不再作为研究基准。新增对照 probe `xuan_research_artifacts/xuan_b27_dplus_compliant_metrics_window_semantics_probe_20260519T054442Z/manifest.json` 比较旧的 post-ASOF window filter 与 candidate-stable window semantics：旧逻辑会把最近 completion 超过 30s 的首腿直接过滤掉，而不是计入 residual。结果 `status=FAIL_LEGACY_WINDOW_FILTER_FALSE_POSITIVE_RISK`，18 个 configs 中旧逻辑为正但修正后非正的配置有 `3` 个；关键基准 `first_max=0.45,pair_cap=0.92` 从旧的 fee_worst_case_pnl `+7.527434` 变为 candidate-stable `-120.593985`，candidate_count 从 `132` 变为 `163`，qty_residual_rate 从 `0.151515` 变为 `0.312883`。已把 `scripts/xuan_b27_dplus_compliant_metrics_runner.py` 改为 candidate-stable left join：所有 strict first-leg candidates 都保留，30s 内找不到合格 opposite completion 的候选计入 residual。修正后复跑 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_metrics_runner_candidate_stable_firstpx045_paircap_sweep_20260519T054744Z/manifest.json` 为 `DISCARD_NO_FEE_POSITIVE_COMPLIANT_METRICS`，6 个 pair caps 全部 fee-worst 为负；最佳 `pair_cap=0.98` 仍为 fee_worst_case_pnl `-64.852024`、stress100_worst_pnl `-38.782986`、worst_day `-13.297509`。结论：后续 compliant strict-first 搜索必须基于 candidate-stable runner 重新做，不能再引用旧 first-price positive pocket 作为候选晋级证据。
195. 已完成 candidate-stable trigger-feature 分桶 probe，找到一个弱的可部署前置特征候选，但仍远未到 promotion/shadow 级别。新增 `scripts/xuan_b27_dplus_candidate_stable_feature_probe.py`，只读本地 `POLY_BT_ROOT` strict cache、completion store 与 public-account audit proxy truth，使用 candidate-stable 30s completion semantics，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_stable_feature_probe_20260519T061307Z/manifest.json` 为 `KEEP_DEPLOYABLE_FEATURE_BUCKET_CANDIDATE`：对两个 fixed-run configs 做分桶，整体 `first_max=0.45,pair_cap=0.98` 的 base metrics 是 candidate_count `163`、fee_worst_case_pnl `-64.852024`、qty_residual_rate `0.141104`。其中触发时可见字段 `public_trade_size <= 100` 子桶 candidate_count `69`、completion_rate `0.956522`、qty_residual_rate `0.043478`、fee_worst_case_pnl `+8.920225`、stress100_worst_pnl `+20.507077`，但 worst_day_fee_worst_case_pnl 仍为 `-2.078813`，positive_daily_count `9/14`，样本太薄。forward-looking 诊断字段 `min_pair_cost_30s <= 0.90` 也为正且无 residual，但该字段泄露 30s 未来 completion，不允许作为 deployable selector。结论：`public_trade_size` 可以作为下一轮 bounded runner 参数化假设（例如 `--public-trade-size-max`），但当前结果只是局部分桶候选，不能用于 EC2 shadow 或 canary 晋级。
196. 已把 `public_trade_size` 弱候选参数化进 candidate-stable compliant runner，并完成小网格 falsification。`scripts/xuan_b27_dplus_compliant_metrics_runner.py` 新增默认关闭的 `--public-trade-size-max`，仅使用触发时已知字段，不改变旧调用；本轮 artifacts 为 `xuan_b27_dplus_compliant_metrics_runner_candidate_stable_pubsize050_20260519T064047Z`、`pubsize075_20260519T064049Z`、`pubsize100_20260519T064051Z`、`pubsize125_20260519T064053Z`、`pubsize150_20260519T064056Z`、`pubsize200_20260519T064058Z`。固定 `first_max=0.45`、clip `10`、30s completion window，扫 pair caps `0.92..1.00`。结果：size cap `75/100/125` 有 fee-worst positive，但样本仍薄且 worst-day 为负；最佳 size `100`、pair_cap `0.98`：candidate_count `78`、fee_worst_case_pnl `+5.649992`、stress100_worst_pnl `+18.718752`、qty_residual_rate `0.051282`、worst_day `-2.078813`、positive_daily_count `8/14`。当 size cap 放宽到 `150`，candidate_count 到 `109` 但 fee_worst 转为 `-14.099043`，size `200` 为 `-51.543180`。结论：`public_trade_size_max` 是有用的局部降 residual 特征，但无法在候选数超过 100 时维持正 fee-worst/day stability；不得推进 EC2 shadow，下一步应研究与 offset/time/day-stability 的交互，或换新信号族。
197. 已完成 `public_trade_size` 与 offset/first-price 的 candidate-stable 交互 probe，结论是该一维弱信号无法通过简单交互扩成 shadow 候选。新增 `scripts/xuan_b27_dplus_candidate_stable_interaction_probe.py`，只读本地 `POLY_BT_ROOT` strict cache、completion store 与 public-account audit proxy truth，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_stable_interaction_probe_20260519T071224Z/manifest.json` 为 `DISCARD_NO_QUALIFIED_INTERACTION_BUCKET`：在 `first_max=0.45,pair_cap=0.98` 下，扫 `public_trade_size_max=75/100/125/150`、`offset_max_s=120/180/210/240/300`、`first_price_filter_max=0.42/0.44/0.45`，共 `60` 个交互组合；`positive_interaction_run_count=14`，但 `qualified_interaction_run_count=0`，资格阈值为 candidate_count `>=100`、qty_residual_rate `<=0.06`、worst_day `>=0`。最佳组合仍是 size `100`、offset `240/300`、first `0.45`：candidate_count `78`、fee_worst_case_pnl `+5.649992`、stress100_worst_pnl `+18.718752`、qty_residual_rate `0.051282`、worst_day `-2.078813`。结论：offset/first-price 交互没有修复样本和日级稳定性；不应把这条 public-trade-size 家族推进 EC2 shadow，下一步应转向新 deployable signal family 或更结构化的 residual repair，而不是继续在同一薄口袋微调。
198. 已完成 side-cost-aware trigger filter 的 candidate-stable falsification，结论是 immediate pair cost 过滤不能把 `public_trade_size` 薄口袋扩成 shadow 候选。新增 `scripts/xuan_b27_dplus_candidate_stable_side_cost_probe.py`，只读本地 `POLY_BT_ROOT` strict cache、completion store 与 public-account audit proxy truth，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_stable_side_cost_probe_20260519T074226Z/manifest.json` 为 `DISCARD_NO_QUALIFIED_SIDE_COST_FILTER`：在 `first_max=0.45,pair_cap=0.98` 下，扫 trigger-time `immediate_pair_cap=1.02/1.05/1.10` 与 `public_trade_size_max=0/100/125/150/200`，共 `15` 个组合；`positive_run_count=4`，但 `qualified_run_count=0`，资格阈值仍为 candidate_count `>=100`、qty_residual_rate `<=0.06`、worst_day `>=0`。最佳组合为 immediate_pair_cap `1.10` + public_trade_size_max `100`：candidate_count `70`、fee_worst_case_pnl `+7.939842`、stress100_worst_pnl `+19.722105`、qty_residual_rate `0.042857`、worst_day `-1.147224`。当 public_trade_size_max 放宽到 `150` 使 candidate_count 到 `100` 时，fee_worst 转为 `-7.490333`、residual_rate `0.07`。结论：side-cost-aware seed filter 只是进一步收窄同一薄口袋，不能满足样本/残仓/日级稳定性；不要消耗 EC2 no-order shadow，下一步应换新信号族或做结构性 residual repair。
199. 已完成 public-trade trigger price/size/slippage 的 candidate-stable falsification，结论是 public trade 触发特征仍只产生极薄局部正收益，不能作为 shadow 候选。新增 `scripts/xuan_b27_dplus_candidate_stable_public_trade_probe.py`，只读本地 `POLY_BT_ROOT` strict cache、completion store 与 public-account audit proxy truth，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_stable_public_trade_probe_20260519T081235Z/manifest.json` 为 `DISCARD_NO_QUALIFIED_PUBLIC_TRADE_FILTER`：在 `first_max=0.45,pair_cap=0.98` 下，扫 public_trade_price_max `0.50/0.55/0.60/0.70`、public_trade_size_max `0/100/125/150/200`、slippage_max `disabled/-0.05/-0.02/0`，共 `80` 个组合；`positive_run_count=31`，但 `qualified_run_count=0`，资格阈值仍为 candidate_count `>=100`、qty_residual_rate `<=0.06`、worst_day `>=0`。最佳组合是 public_trade_price_max `0.50` + public_trade_size_max `100`：candidate_count `27`、completion_rate `1.0`、fee_worst_case_pnl `+8.124618`、stress100_worst_pnl `+12.785047`、residual_qty `0`，但样本极薄且 worst_day `-0.105693`。放宽到 public_trade_price_max `0.70` + size `100` 后 candidate_count `77`、fee_worst `+5.658775`、residual_rate `0.051948`、worst_day `-2.078813`，仍不过样本/日级稳定性。结论：public trade 价格/滑点过滤只是把前面 size 薄口袋切得更细；不应推进 EC2 shadow，下一步需要真正不同的信号族或结构化 residual repair。
200. 已完成 strict/cache admission 下 residual-cooldown 结构修复的 compliant 全量复跑，结论是 cooldown brake 不改变 D+ 残仓尾部，不能作为下一轮 shadow 候选。使用合并进来的 `scripts/xuan_b27_dplus_residual_cooldown_compliant_metrics_runner.py`，只读本地 `POLY_BT_ROOT` strict cache、completion store 与 public-account audit proxy truth，排除 20260514/20260515/20260518，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_residual_cooldown_compliant_metrics_runner_20260519T084152Z/manifest.json` 为 `PASS_COMPLIANT_METRICS_RUNNER`，覆盖 strict candidate rows `169494`、5 个 profile。最严格 `residual_cooldown_cost_cap=0.50` 产生 `seed_block_residual_cooldown=18344`，但与无 cooldown baseline 指标完全相同：`seed_actions=12772`、`pair_actions=4597`、`pair_qty=5746.25`、`residual_qty=4472.5`、`qty_residual_rate=0.280144`、`pair_pnl=+102.1125`、`net_pnl=+913.1875`、`worst_residual_net_pnl=-2303.0625`、`stress100_worst_pnl=-2462.7125`。结论：该 cooldown 条件只是统计上触发，并未改变实际 seed/pair path；继续调 cooldown cap 不会推进，应转向真正改变 seed admission/repair pairing 的机制。
201. 已完成第一个 compliant inventory repair 结构机制 probe，结论是 opposite-side repair 明显改善残仓尾部，但仍未达到 promotion/shadow 阈值。新增 `scripts/xuan_b27_dplus_compliant_inventory_repair_probe.py`，复用本地 `POLY_BT_ROOT` strict cache、completion store 与 public-account audit proxy truth，只读 DuckDB，排除 20260514/20260515/20260518，不扫 raw/replay、不启动网络或交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_20260519T091802Z/manifest.json` 为 `KEEP_RESEARCH_ONLY_REPAIR_LEAD`，覆盖 strict candidate rows `169494`、26 个 profiles，明确 public audit 缺失日为 `2026-05-16/17`。最佳 repair profile `repair_t3_a60_q1p25_cap1p02` 相对 baseline_t5：`seed_actions` 从 `12772` 增至 `13701`，`repair_actions=3403`，`pair_actions` 从 `4597` 增至 `7864`，`pair_qty` 从 `5746.25` 增至 `9830.0`，`pair_cost_wavg` 从 `0.982225` 降到 `0.906793`，`residual_qty` 从 `4472.5` 降到 `1720.0`，`qty_residual_rate` 从 `0.280144` 降到 `0.080449`，`pair_pnl` 从 `+102.1375` 增到 `+916.22`，`net_pnl` 从 `+913.2125` 增到 `+1289.9575`，`worst_residual_net_pnl` 从 `-2303.0375` 改到 `-52.5425`，`stress100_worst_pnl` 从 `-2462.6875` 改到 `-266.3425`。scoreboard delta：residual rate 改善 `0.199695`，residual_qty 减少 `2752.5`，stress100_worst 改善 `+2196.345`，但 stress 仍未转正。因此这是 research-only lead；下一步必须按 public-audit-covered 05-02..13 与 uncovered 05-16/17 分拆验证，不能直接进 EC2 shadow 或 G2 canary。
202. 已完成 inventory repair lead 的 public-audit-covered split 验证，结论是 repair lead 不依赖未审计的 2026-05-16/17，但仍是 research-only。新增 `scripts/xuan_b27_dplus_inventory_repair_split_compare.py`，只读取现有 probe manifests，不读 DuckDB、不扫 raw/replay、不启动网络或交易路径。covered split artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_public_covered_20260519T094149Z/manifest.json` 覆盖 2026-05-02..13、strict rows `146984`、public audit missing days 为空；最佳 profile 仍是 `repair_t3_a60_q1p25_cap1p02`，相对 baseline_t5 把 residual rate 从 `0.269741` 降到 `0.079557`，residual_qty 从 `3838.75` 降到 `1498.75`，pair_cost_wavg 到 `0.906341`，pair_pnl 到 `+812.02`，net_pnl 到 `+1165.77625`，worst_residual_net_pnl 从 `-1957.66875` 改到 `-30.47375`，stress100_worst 从 `-2099.98125` 改到 `-218.86125`。uncovered split `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_public_uncovered_20260519T094149Z/manifest.json` 只覆盖 2026-05-16/17，也改善但 public audit missing days 为这两天。compare artifact `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_split_compare_20260519T094541Z/manifest.json` 为 `KEEP_PUBLIC_COVERED_REPAIR_LEAD_RESEARCH_ONLY`，`covered_lead_preserved=true`，covered residual rate 比 full run 还低 `0.000892`，covered stress100_worst 比 full run好 `+47.48125`，但 stress100_worst 仍为负。因此不能进 EC2 shadow 或 G2 canary；下一步应本地测试 surplus-funded repair budget，并用 high-cost `pair_select` 作诊断对照。
203. 已完成 surplus-funded repair budget 的 public-audit-covered bounded probe，结论是预算机制有作用但不足以成为下一轮 EC2 shadow 候选。`scripts/xuan_b27_dplus_compliant_inventory_repair_probe.py` 新增 default-off `--repair-budget-modes`、`--repair-budget-fraction`、`--min-edge-per-pair` 和 `--pair-selects`，用每个 market 已完成 pair 的 spendable surplus 为高成本 repair 提供预算，且 high-cost `pair_select` 作为对照，不扫 raw/replay、不启动网络/交易路径。主 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_surplus_budget_covered_20260519T101219Z/manifest.json` 覆盖 public-audit-covered 2026-05-02..13、strict rows `146984`、50 个 profiles。0.5 surplus fraction 下最佳 surplus profile `repair_t3_a60_q1p25_cap1p06_budgetsurplus`：budget deposited `444.035`、spent `2.621875`、surplus-budget blocks `274`，residual rate `0.078483`，worst_residual_net_pnl `-22.590937`，stress100_worst `-211.026875`；同组无预算 cap=1.06 control stress100_worst 为 `-199.93625`。上界 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_surplus_budget_upper_covered_20260519T101408Z/manifest.json` 用 100% surplus fraction 和 cap 到 1.10，最佳 stress100_worst 仍为 `-201.035105`，residual rate `0.077186`，budget spent `5.3375`，surplus-budget blocks `383`。high-cost pair_select 与 fifo 指标相同，说明当前 residual path 不是 pair selection accounting 能修复。结论：surplus budget 可以约束高成本 repair、略改善旧 cap=1.02 split，但不能把 covered stress 翻正，也不优于简单无预算 cap control；下一步应做 residual failure decomposition（按 day/condition/winner_side/repair opportunity）或引入真实降低 residual settle risk 的机制，而不是把 surplus budget 推到 shadow。
204. 已完成 covered repair lead 的 residual/stress failure decomposition，结论是当前晋级 blocker 已从“大残仓尾部”转成“接近打平后的 stress/turnover drag”。新增 `scripts/xuan_b27_dplus_inventory_repair_failure_decomposition.py`，在 public-audit-covered 2026-05-02..13 上按 market 独立复盘 `repair_t3_a60_q1p25_cap1p02`，输出 top stress markets 和 day/side/opportunity/pair-cost/residual-cost buckets；只读本地 strict/cache、completion store 和 public-account audit proxy truth，不扫 raw/replay、不启动网络/交易路径。真实 artifact `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_failure_decomposition_covered_20260519T104235Z/manifest.json` 为 `KEEP_FEE_DRAG_DOMINATED_FAILURE_LEAD`，覆盖 rows `146984`、markets `3443`。全局 summary：pair_qty `8670.0`、residual_qty `1498.75`、residual_cost `842.49375`、pair_pnl `812.0075`，因此 worst_residual_net_pnl 已接近打平为 `-30.48625`；但 100bps stress fee drag 为 `188.3875`，把 stress100_worst 拉到 `-218.87375`。scoreboard 显示 `fee_drag_share_of_negative_stress=0.860713`，`residual_deficit_share_of_negative_stress=0.139287`。分组上，平仓 residual 为 0 的大组已有正 stress `+556.1825`，但残仓 cost `(0.5,1]` 的 1199 个 market 贡献 worst_residual `-721.94375`；`no_repair_opportunity` 组 stress `-107.52375`，pair-cap blocked 也集中在这些 market。结论：继续单纯放宽 repair/budget 不够；下一步应做 fee-aware/churn-aware repair selector 或更高 per-pair edge threshold，同时针对 no-repair-opportunity/pair-cap-blocked residual buckets 设计可部署机制，未过本地 covered stress 前不消耗 EC2 shadow。
205. 已完成 fee-aware repair selector / stronger edge threshold probe，结论是单纯收紧 repair pair cap 会变差，但 `edge=0.07` 首次让 public-audit-covered split 的 stress100_worst 转正。`scripts/xuan_b27_dplus_compliant_inventory_repair_probe.py` 新增 default-off `--edges`，用于同一 profile family 的 bounded edge-threshold 对照；仍只读本地 strict/cache、completion store 与 public-account audit proxy truth，不扫 raw/replay、不启动网络/交易路径。fee-aware cap artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_feeaware_covered_20260519T111012Z/manifest.json` 显示把 repair_pair_cap 从 `1.02` 收紧到 `0.90..1.00` 只会减少 repair、扩大 residual，best 仍是原 `cap=1.02`，`stress100_worst=-218.86125`；`low_pair_cost/high_cost` 与 FIFO 完全一致。edge-threshold artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_edge_threshold_covered_20260519T111919Z/manifest.json` 扫 edge `0.055/0.06/0.065/0.07`、cap `1.00/1.02`，最佳 `repair_t3_a60_q1p25_cap1p02_e0p07` 覆盖 rows `146984`，pair_qty `8700.0`、pair_cost_wavg `0.883182`、residual_qty `1477.5`、qty_residual_rate `0.078268`、pair_pnl `+1016.31375`、net_pnl `+1401.78875`、worst_residual_net_pnl `+209.28875`、stress100_worst `+20.51375`。edge `0.065` 仍为 `stress100_worst=-59.1925`，说明 break-even threshold 在 6.5% 到 7.0% 被动折价之间。结论：这是第一个 public-audit-covered stress-positive local candidate，但它依赖更高 passive edge 假设，仍不是 EC2 shadow/canary 依据；下一步必须在 full allowed labels、day stability、uncovered 05-16/17 和 no-order shadow p90/residual proxy 上复验，而不是直接升级。
206. 已完成 `edge=0.07` repair lead 的 full-allowed / covered-uncovered 复验，结论是 full allowed labels 仍 stress-positive，但未审计 2026-05-16/17 split 单独为负，因此仍需 source-of-truth/no-order proxy 复验，不能直接进 shadow/canary。`scripts/xuan_b27_dplus_compliant_inventory_repair_probe.py` 进一步新增 default `--exclude-label-fragments=20260514,20260515,20260518`，避免本地新出现的 20260518 manifest 被默认纳入；manifest 中的 `contains_20260518` 也改为由实际 days 计算。full-allowed artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_edge07_fullallowed_20260519T1140Z/manifest.json` 使用 manifest-discovered strict/completion labels，排除 05-14/05-15/05-18，覆盖 2026-05-02..13 与 05-16/17，row_count `169494`，public audit missing days 为 `2026-05-16/17`；最佳 `repair_t3_a60_q1p25_cap1p02_e0p07` 仍为 `KEEP_INVENTORY_REPAIR_CANDIDATE`，pair_qty `10017.5`、pair_pnl `+1146.40125`、net_pnl `+1557.70125`、residual_qty `1692.5`、qty_residual_rate `0.078969`、worst_residual_net_pnl `+220.20125`、stress100_worst `+5.87625`。uncovered-only artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_edge07_uncovered_20260519T1144Z/manifest.json` 覆盖 05-16/17，public audit 缺失这两天，best stress100_worst 为 `-14.625`，但 worst_residual_net_pnl 已为 `+10.925`、pair_pnl `+130.1`。split compare `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_split_compare_edge07_20260519T1144Z/manifest.json` 为 `KEEP_PUBLIC_COVERED_REPAIR_CANDIDATE_NEEDS_SOURCE_TRUTH`：covered stress `+20.51375`、full stress `+5.87625`、uncovered stress `-14.625`。这说明 edge=0.07 lead 不再只是 covered 数据过拟合，但未审计日仍有尾部风险；下一步应做 day-level/market-level stress attribution 与 no-order shadow p90/residual proxy，而不是直接消耗 G2 canary。
207. 已完成 `edge=0.07` full/uncovered 的 day/market stress attribution，结论是 full allowed 全局转正由多数正日覆盖，尾部集中在 2026-05-17 与 no-repair-opportunity/pair-cap-blocked residual bucket。`scripts/xuan_b27_dplus_inventory_repair_failure_decomposition.py` 现在把 edge 写入 profile name，并新增 default `--exclude-label-fragments=20260514,20260515,20260518`，同时按实际 days 计算 `contains_20260518`，防止 manifest 出现后被默认误纳入。full attribution artifact `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_failure_decomposition_edge07_fullallowed_20260519T1210Z/manifest.json` 为 `PASS_FAILURE_DECOMPOSITION_STRESS_NONNEGATIVE`，覆盖 allowed rows `169494`、markets `4018`，全局 stress100_worst `+5.90125`、worst_residual_net_pnl `+220.22625`、pair_cost_wavg_proxy `0.883849`、residual_rate `0.078969`；但 day bucket 中 2026-05-17 最差，stress `-20.775`、worst_residual `-9.05`、residual_rate `0.098081`、pair_cost_wavg_proxy `0.895272`。uncovered attribution artifact `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_failure_decomposition_edge07_uncovered_20260519T1210Z/manifest.json` 为 `KEEP_FEE_DRAG_DOMINATED_FAILURE_LEAD`，覆盖 05-16/17 rows `22510`、markets `575`，全局 stress `-14.675` 但 worst_residual `+10.875`、pair_pnl `+130.05`；其中 05-16 stress 为 `+6.1`，05-17 stress 为 `-20.775`。uncovered 的 `repair_opportunity_seen` bucket 已 stress `+8.25`、worst_residual `+28.0125`，而 `no_repair_opportunity` bucket 为 stress `-22.925`、worst_residual `-17.1375`、residual_rate `0.12311`；pair-cap-blocked bucket 也为 stress `-12.3875`、residual_rate `0.205128`。结论：下一步不应再整体抬 edge 或加宽 repair cap，而应本地测试可部署的 no-repair-opportunity/pair-cap-blocked 预交易风险过滤或 p90/residual proxy，未通过前不花 EC2 no-order shadow 窗口。
208. 已完成第一轮可部署 pre-trade proxy 检验，结论是 `strict_l1_immediate_pair` 收紧代理被 kill，而 Sunday 日历代理是 research-only lead。`scripts/xuan_b27_dplus_compliant_inventory_repair_probe.py` 新增 default-off `--seed-l1-pair-caps`，用于只基于 strict L1 immediate pair 的入场代理；粗网格 artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_seed_l1_proxy_edge07_fullallowed_20260519T1240Z/manifest.json` 与细网格 `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_seed_l1_proxy_fine_edge07_fullallowed_20260519T1244Z/manifest.json` 显示该代理太稀疏：`seed_l1_pair_cap <= 1.00` 只有 `0..70` seeds，`1.015` 虽有 `11981` seeds 但 stress100_worst 变成 `-57.76125`，不如原 `1.02` 的 `+5.87625`。新增 `scripts/xuan_b27_dplus_edge07_calendar_proxy_summary.py` 只读取既有 attribution CSV 做 metadata reducer，不读 raw/replay、不启动网络/交易路径；artifact `xuan_research_artifacts/xuan_b27_dplus_edge07_calendar_proxy_summary_20260519T1248Z/manifest.json` 为 `KEEP_CALENDAR_PROXY_RESEARCH_ONLY`。排除 Sunday 后，strict rows 保留率 `0.822318`、pair_pnl 保留率 `0.824136`，stress100_worst 从 `+5.90125` 提升到 `+53.38875`，residual_rate 从 `0.078969` 降到 `0.075511`，worst_residual_net_pnl 从 `+220.22625` 到 `+227.70125`；Sunday bucket 自身 stress 为 `-47.4875`、residual_rate `0.094033`、no_repair_opportunity_per_seed `0.350644`。结论：Sunday filter 是目前唯一完全 pre-trade、可部署且有显著 tail 改善的本地代理，但样本只有 3 个 Sunday，必须用 source-of-truth/no-order shadow proxy 复验；不能从 completion-store attribution 直接升级。
209. 已完成 Sunday calendar proxy 的 covered/uncovered split stability 复验，结论是 covered 稳定改善，uncovered 排除 Sunday 后也转正但只剩 05-16，样本太薄，因此仍是 research-only mixed lead。新增 `scripts/xuan_b27_dplus_edge07_calendar_proxy_split_compare.py`，只读取已有 summary manifests，不读 DuckDB/raw/replay、不启动网络/交易路径。covered attribution artifact `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_failure_decomposition_edge07_covered_20260519T1310Z/manifest.json` 全局 stress `+20.60125`、worst_residual `+209.37625`；covered calendar summary `xuan_research_artifacts/xuan_b27_dplus_edge07_calendar_proxy_summary_covered_20260519T1310Z/manifest.json` 显示排除 Sunday 后 row retention `0.860373`、pair_pnl retention `0.856121`、stress 从 `+20.60125` 到 `+47.31375`，Sunday bucket 为 `-26.7125`。uncovered summary `xuan_research_artifacts/xuan_b27_dplus_edge07_calendar_proxy_summary_uncovered_20260519T1310Z/manifest.json` 显示排除 Sunday 后 stress 从 `-14.675` 到 `+6.1`，但 row retention 只有 `0.573834`，等于只剩 05-16。split compare `xuan_research_artifacts/xuan_b27_dplus_edge07_calendar_proxy_split_compare_20260519T1312Z/manifest.json` 为 `KEEP_SUNDAY_PROXY_RESEARCH_ONLY_SPLIT_MIXED`：full/covered/uncovered stress delta 分别为 `+47.4875/+26.7125/+20.775`，但 uncovered 太薄，不能 promotion。结论：Sunday proxy 是可部署候选，但下一步必须做 source-of-truth/no-order proxy 验证或找到非日历的 no-repair-opportunity proxy；仍不该消耗 G2 canary。
210. 已完成 non-calendar activation proxy 检验，结论是“15s 内必须先看到对侧触发”的 pre-trade admission gate 明显优于 Sunday 过滤，且 covered/uncovered 都转正。`scripts/xuan_b27_dplus_compliant_inventory_repair_probe.py` 新增默认关闭的 `--activation-modes` / `--activation-windows-s`，当前仅实现 `opp_seen`，在 risk-increasing seed 前要求同一 market 内对侧 first-side trigger 在窗口内出现；repair 逻辑仍先运行，因此它是入场激活代理而不是未来 completion 数据。`scripts/xuan_b27_dplus_inventory_repair_failure_decomposition.py` 同步支持默认关闭的 activation 参数用于同 profile 归因。full artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation_proxy_edge07_fullallowed_20260519T134344Z/manifest.json` 覆盖 allowed rows `169494`，best `repair_t3_a60_q1p25_cap1p02_e0p07_actopp_seen15`：seed_actions `6833`、pair_qty `4835.0`、pair_pnl `+670.6`、net_pnl `+770.7125`、residual_qty `828.75`、qty_residual_rate `0.078938`、worst_residual_net_pnl `+266.9625`、stress100_worst `+161.975`，相对原 edge=0.07 full stress `+5.87625` 大幅改善且 residual_qty 减半。covered split `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation_proxy_edge07_covered_20260519T1347Z/manifest.json` stress `+152.225`、worst_residual `+247.8625`，无 public audit missing days；uncovered split `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation_proxy_edge07_uncovered_20260519T1347Z/manifest.json` 也从原 `-14.675` 改为 `+9.75`，但仍缺 public audit。activation attribution `xuan_research_artifacts/xuan_b27_dplus_inventory_repair_failure_decomposition_edge07_activation15_fullallowed_20260519T1350Z/manifest.json` 为 `PASS_FAILURE_DECOMPOSITION_STRESS_NONNEGATIVE`：05-17 从原 `-20.775` 改为 `+3.9375`，全局 stress `+162.0125`、pair_cost_wavg_proxy `0.861295`。残留负项集中在 pair-cap-blocked bucket（stress `-6.55`）和 no-repair-opportunity bucket（stress `-53.9375`），但 repair_opportunity_seen bucket 已 `+215.95`。结论：activation proxy 是当前最强本地 structural candidate，仍不能 promotion，因为 05-16/17 缺 public-account truth 且尚未做 source-of-truth/no-order shadow p90/residual 验证；下一步应先设计 shadow-safe p90/residual proxy 或把 activation gate 接入 same-host no-order shadow runner 的默认关闭配置后再决定是否消耗 EC2 shadow 窗口。
211. 已把 activation proxy 翻译进 no-order passive/passive shadow runner，但保持默认关闭且只影响虚拟 seed admission。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增 `--activation-mode none|opp_seen` 与 `--activation-window-s`，默认 `none` 不改变旧路径；`opp_seen` 仅在拟议 seed 会增加当前同侧 exposure 风险时，要求对侧 public SELL trigger 在窗口内出现，降低 imbalance 的 repair seed 不被误挡。candidate event 现在记录 `activation_mode`、`activation_window_s`、`activation_required`、`risk_increasing_seed`、`activation_opp_age_ms`；blocked reason 使用 `activation_opp_seen`，aggregate 仍通过既有 `blocked` 汇总上报。新增本地无网络 smoke `scripts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke.sh`，artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260519T141519Z/manifest.json` 为 `PASS`：验证默认关闭保留 seed 路径、`opp_seen` 会挡住首次 risk-increasing seed、10s 内对侧触发会放行、降低 imbalance 的 seed 会绕过 activation。该 smoke 不启动 socket/SSH/network，不发 order/cancel/redeem，不改 shared-ingress/broker/service。结论：activation lead 已具备 shadow-safe runner 配置面，下一步才可以在本地/EC2 no-order shadow 上做 bounded p90/residual 验证；仍不支持 G2 canary 或 live。

212. 已完成新 EC2 same-host no-order shadow 的 activation15 bounded 验证。实验假设是：把本地最强 `edge=0.07 + activation_opp_seen15` 结构接入真实 shared-ingress shadow 后，应在保留正 PnL 的同时把 `net_pair_cost_p90 <= 1.0`、`residual_qty <= 10` 并维持足够样本。本轮只复制当前 runner 单文件到隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_activation15_edge007_20260519T144649Z`，以 `pmofi`、`PM_DRY_RUN=true`、`PM_SHARED_INGRESS_ROLE=client`、`PM_SHARED_INGRESS_ROOT=/srv/pm_as_ofi/shared-ingress-main` 同机运行 1800s，未 repo sync/build，未改 broker/shared-ingress/service/env，未发送 order/cancel/redeem；SSH 中途断开后先核验远端 runner 仍存活和 aggregate 未写出，未重启或重叠窗口，完成后只拉回 manifest、aggregate 和 summary 小文件，未拉回 events JSONL。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260519T144649Z/remote/output/aggregate_report.json` 覆盖 12 个 slug，配置为 edge `0.07`、target_qty `5`、`late_repair_after_s=75`、`imbalance_qty_cap=1.25`、`pairing_only_when_residual=true`、`activation_mode=opp_seen`、`activation_window_s=15`。结果：58 candidates、41 queue-supported fills、24 pair actions、pair_qty `30.0`、pair_pnl `+1.92325`、ROI on filled cost `8.145%`、`net_pair_cost_p90=0.99`、residual_qty `11.25`、residual_cost `4.6125`、material_residual_lots `0`、activation blocks `11`。验收 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260519T151838Z/manifest.json` 为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，失败项是 candidates `58 < 100` 与 residual_qty `11.25 > 10`；PnL、p90、residual_cost 和 no-order safety 均通过。结论：activation15 shadow 证实 p90 成本尾部可被压过阈值，且 residual 从上一条 3600s repair-only best 的 `21.4825` 明显下降，但 1800s 样本不足且 residual_qty 仍略超，因此不能 G2 canary 或 promotion。下一步应做同一结构的样本/残仓机制拆分：优先比较 3600s activation15 或 activation window/late repair target 的小范围 A/B，目标是在不放弃 p90 的前提下把 residual_qty 压到 10 以下并恢复 candidates，而不是回到静态 cap、hard cutoff 或单纯提高 edge。

213. 已完成 activation15 shadow miss 的本地只读 summary/aggregate 归因。新增 `scripts/xuan_b27_dplus_shadow_activation_miss_attribution.py`，只读取 `xuan_research_artifacts` 下已拉回的 no-order shadow `aggregate_report.json` 和 `*.summary.json`，显式不读 events JSONL、不扫 raw/replay、不 SSH、不连 socket、不启动网络/交易路径。artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_activation_miss_attribution_20260519T152757Z/manifest.json` 为 `KEEP_P90_LEAD_RESIDUAL_CONCENTRATED_RESEARCH_ONLY`：activation15 的 3600s-normalized candidates 为 `116`，略高于上一条 3600s repair-only best 的 `103`，pair_pnl rate 为 baseline 的 `92.64%`，residual_qty rate 为 baseline 的 `104.74%`；activation blocks 只有 `11`，每 candidate `0.1897`，占全部 blocked 的 `0.2556%`，所以样本不足不能简单归因为 activation gate。残仓高度集中：3 个 market 贡献全部 `11.25` residual_qty，top2 market 贡献 `10.0`（`88.89%`），且全部 top residual lot 都是 NO side；两个主残仓 market `btc-updown-5m-1779202500` 与 `btc-updown-5m-1779202800` 各有 candidates `8`、fills `4`、pair_actions `0`、residual_qty `5.0`。结论：activation15 的 p90 机制仍成立，但当前 miss 更像“少数 market 的 repair failure/residual concentration + 窗口样本不足”，不是 activation-only sample collapse。由于 summary 没有事件顺序和 decision context，本归因只用于下一步参数选择，不是因果证明或 promotion evidence。下一步不应把 plain 3600s activation15 当作晋级尝试；若花 EC2 窗口，应把它定义成 residual scaling falsification，或同时测试 activation window/late-repair variant 来压 top residual markets。

214. 已完成 activation15 参数拆分的本地 compliant probe，结论是放宽 activation window 不是主修复，提前 repair threshold 才是当前最强候选。所有 artifact 都只读本地 `POLY_BT_ROOT` manifest-discovered strict/cache 与 completion store，默认排除 20260514/20260515/20260518，不扫 raw/replay、不启动网络或交易路径。window compare artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation_window_compare_edge07_fullallowed_20260519T1555Z/manifest.json` 在 target `5`、edge `0.07`、repair_after `75`、repair_qty `1.25`、cap `1.02` 下比较 activation window 15/30/60：15s 仍最好，residual_qty `945.0`、residual rate `9.2105%`、stress100 `+71.7525`；30s 增加 seed/pair/sample（seed `8388`、pair_qty `5700.0`）但 residual_qty 升到 `1078.75` 且 stress 降到 `+23.55`；60s residual_qty `1180.0` 且 stress 转负 `-25.7375`。repair-after compare artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation15_repair_after_compare_edge07_fullallowed_20260519T1558Z/manifest.json` 固定 activation15，扫 repair_after 45/60/75/90：45s 最优，pair_qty `5150.0`、pair_pnl `+748.375`、net_pnl `+790.925`、residual_qty `631.25`、qty_residual_rate `5.7747%`、worst_residual_net_pnl `+440.925`、stress100 `+331.6125`；90s 已 stress 负。split 复验也通过但仍是 research-only：covered artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation15_repair45_edge07_covered_20260519T1602Z/manifest.json` 覆盖 2026-05-02..13、row_count `146984`，stress100 `+305.9125`、residual_qty `562.5`、residual rate `5.6533%`；uncovered artifact `xuan_research_artifacts/xuan_b27_dplus_compliant_inventory_repair_probe_activation15_repair45_edge07_uncovered_20260519T1602Z/manifest.json` 覆盖 2026-05-16/17，stress100 `+25.7125`、residual_qty `68.75`、residual rate `7.0064%`，但 public account truth 仍缺失。结论：下一次 EC2 no-order shadow 如要消耗窗口，应优先验证 `edge=0.07 + activation15 + earlier late_repair_after_s≈45` 对 residual tail 的影响；这仍只是 no-order shadow candidate，不是 G2 canary 或 source-of-truth promotion。

215. 已完成 queued repair45 same-host no-order shadow，结论为 `UNKNOWN_SAMPLE_THIN_REPAIR45_P90_RESIDUAL_LEAD`：repair_after 从 75s 提前到 45s 后，p90/residual/PnL 全部改善并通过对应风险 gate，但样本显著变薄，不能 promotion。运行前确认 EC2 上没有其他 D+ shadow runner；随后在新服务器隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_activation15_repair45_edge007_20260519T193208Z` 以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 跑 1800s，配置为 edge `0.07`、target_qty `5`、`late_repair_after_s=45`、`imbalance_qty_cap=1.25`、`pairing_only_when_residual=true`、`activation_mode=opp_seen`、`activation_window_s=15`、offsets `0..11`。SSH transport 中途关闭后先只读确认远端 runner 仍在运行，未重启或叠加窗口；完成后只拉回 manifest、aggregate、summary 和 stdout/stderr 小文件，未拉 events JSONL，未 repo sync/build/service control，未写 broker/shared-ingress/env/live 路径，未发送 order/cancel/redeem。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_driver_20260519T193208Z/remote/output/aggregate_report.json`：29 candidates、16 queue-supported fills、10 pair actions、pair_qty `10.865`、pair_pnl `+0.673055`、ROI on filled cost `7.907%`、`net_pair_cost_p90=0.9494`、residual_qty `6.25`、residual_cost `1.675`、material_residual_lots `0`。相对上一条 activation15/repair75 1800s shadow（58 candidates、pair_pnl `+1.92325`、p90 `0.99`、residual_qty `11.25`），repair45 成功把 residual 压到 `<=10` 且 p90 更好，但 candidates 降到 29，仅为验收阈值 100 的 29%。acceptance artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_20260519T200546Z/manifest.json` 为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，唯一失败项是 candidates `29 < 100`；PnL metrics、residual risk、market scope、no-order safety、path safety 均通过。下一跳不应重复同一 1800s repair45；应进入 `causal_verifier`/`shadow_observe` 单 lane，解释 sample collapse 并寻找可执行恢复机制，例如只读 summary 对比 activation15 repair75 vs repair45 的 block/candidate delta、target/repair threshold A/B，或在不破坏 p90/residual 的前提下恢复 candidates 的 bounded local proxy。仍不支持 G2 canary、live 或 promotion。

216. 已完成 repair75 vs repair45 shadow miss 的本地 no-network summary comparator，结论为 `UNKNOWN_DIFFERENT_WINDOWS_NO_CAUSAL_ATTRIBUTION`：repair45 的 p90/residual 改善是真实 scoreboard delta，但两条 1800s shadow 没有任何重叠 market slug，不能用 summary 级数据把样本下降因果归咎于 `late_repair_after_s=45`。新增 `scripts/xuan_b27_dplus_shadow_repair_threshold_compare.py`，只读取已拉回的 `aggregate_report.json`、`manifest.json` 与 `*.summary.json`，显式不读 events JSONL、不扫 raw/replay、不 SSH、不连 socket、不启动网络或交易路径。artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_repair_threshold_compare_20260519T201255Z/manifest.json`：repair75 baseline 为 58 candidates、41 fills、24 pair actions、pair_pnl `+1.92325`、p90 `0.99`、residual_qty `11.25`；repair45 为 29 candidates、16 fills、10 pair actions、pair_pnl `+0.673055`、p90 `0.9494`、residual_qty `6.25`。raw candidates 下降 `58 -> 29`，但 sell triggers 也从 `4361 -> 3133`；candidate/sell-trigger 从 `1.33%` 降至 `0.93%`，late_repair_only/sell-trigger 基本相近（`12.73% -> 13.21%`），activation_opp_seen/sell-trigger 上升（`0.25% -> 0.80%`）。blocked raw counts 同时受 regime 影响：cooldown `599 -> 138`、l1_pair_ask_gt_cap `175 -> 23`、pairing_only_when_residual `99 -> 28`、target `49 -> 0`。结论：当前数据支持“repair45 是 p90/residual lead 但样本因果未判明”，不支持重复同一 1800s repair45 或直接花 acceptance-style EC2 shadow。下一跳应实现一个同窗 causal verifier：默认关闭的 multi-profile no-order shadow harness 或 event-lite same-window summary，在同一 market stream 上比较 repair_after `45/60/75`，再决定是否用 EC2 窗口做 bounded A/B；仍不支持 G2 canary、live 或 promotion。

217. 已完成 same-window multi-profile causal verifier 的本地实现和 smoke，结论为 `KEEP_SAME_WINDOW_CAUSAL_VERIFIER_READY_FOR_SHADOW_AB`。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭的 `--profile-late-repair-after-s` CSV 参数；不传该参数时仍走原 single-profile 路径和原 `aggregate_report.json` 输出，传入如 `45,60,75` 时，每个 market socket 消息被 fan-out 到独立 runner state，分别写入 `repair45/`、`repair60/`、`repair75/` 子目录，并生成顶层 `multi_profile_aggregate_report.json`。新增 helper `handle_market_message()` 以保证 single-profile 与 multi-profile 使用同一消息处理逻辑；新增 `aggregate_profiles()` 汇总各 profile 的 candidates/fills/pair_actions/p90/residual/pair_pnl/blocked counts。新增本地无网络 smoke `scripts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke.sh`，artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke_20260519T205130Z/manifest.json` 为 `PASS` / `KEEP`：验证 default single-profile 关键 metrics 与 single-profile 子目录一致，multi-profile 分别输出独立 aggregate，同一 fixture stream 下 repair45 被 late_repair gate 明显区分，profile state 独立，且 side effects 中 network/SSH/order/cancel/redeem/shared-ingress/broker/service 全 false。回归 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260519T205144Z/manifest.json` 也通过。结论：下一跳可以切到 `shadow_observe`，先做 EC2 preflight 确认无重叠 runner，再跑一个 bounded same-host no-order multi-profile A/B：基础配置沿用 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..11`，新增 `--profile-late-repair-after-s 45,60,75`；只拉回 manifest、multi_profile_aggregate_report、各 profile aggregate/summary 小文件，仍不拉 events JSONL、不 promotion、不 G2 canary。

218. 本轮 `shadow_observe` 进入 EC2 preflight 后被安全阻断，结论为 `BLOCKED_NON_XUAN_RESEARCH_RUNNER_ACTIVE`。只读 SSH preflight 检查了 `xuan_dplus_passive_passive_shadow_runner.py` 进程和 shared-ingress socket metadata，未启动 xuan-research runner，未写 shared-ingress/broker/env/live，未发送 order/cancel/redeem，也未 stop/signal/inspect-deep 其它 namespace。artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_observe_preflight_20260519T211120Z/manifest.json` 记录 blocker：新 EC2 上已有 `xuan_frontier` runner PID `80199`，用户 `pmofi`，command rooted at `/srv/pm_as_ofi/xuan_frontier_runs/xuan_frontier_dplus_shadow_repair30_sample_retry_20260519T202247Z/repo/tools/xuan_dplus_passive_passive_shadow_runner.py`，使用同一 `/srv/pm_as_ofi/shared-ingress-main`，preflight 时已运行约 `48:17`。按 namespace 与 shared-ingress 安全规则，本轮不能叠加 xuan-research multi-profile A/B。下一跳保持 `shadow_observe`：下个 heartbeat 重复同一 bounded preflight；若该 runner 已结束，再执行 queued xuan-research no-order multi-profile shadow A/B（`--profile-late-repair-after-s 45,60,75`）；若仍存在任何 runner conflict，继续 `BLOCKED` 并更新 blocker。

219. 已在 blocker 清除后完成 xuan-research same-host no-order multi-profile shadow A/B，结论为 `UNKNOWN_SAME_WINDOW_REPAIR75_RANKS_BEST_SAMPLE_THIN`。本轮先只读 preflight 确认 EC2 上没有 `xuan_dplus_passive_passive_shadow_runner.py` 进程，随后只复制当前 runner 单文件到隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_multprofile_repair456075_edge007_20260519T224043Z`，以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 跑 1800s；未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem，完成后只拉回 manifest、multi-profile aggregate、各 profile aggregate/summary、stdout/stderr/runner.pid 小文件，未拉 events JSONL。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multprofile_driver_20260519T224043Z/audit_manifest.json` 记录同窗对比：`repair45` 为 25 candidates、19 fills、11 pair actions、pair_pnl `+1.1155`、p90 `0.97`、residual_qty `1.25`；`repair60` 为 33 candidates、26 fills、15 pair actions、pair_pnl `+1.473615`、p90 `0.97`、residual_qty `2.5`；`repair75` 为 37 candidates、29 fills、17 pair actions、pair_pnl `+1.751115`、p90 `0.97`、residual_qty `2.5`。三档均通过 p90/residual/PnL/material-residual/no-order safety，且同窗排序明确偏向 `repair75`，说明上一条 repair45 的样本下降不是可直接晋级的优势；但最佳 repair75 的 3600s 等价 candidates 也只有 `74 < 100`，所以仍不是 promotion/G2 canary evidence。下一跳继续 `shadow_observe`：先做 EC2 runner preflight，若无冲突则运行一次样本扩展 no-order A/B，使用 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、duration `3600s`、offsets `0..25`、`--profile-late-repair-after-s 60,75`；只拉回小 summary/aggregate，目标是确认至少一档达到 candidates `>=100` 且保持 p90 `<=1.0`、residual_qty `<=10`、正 PnL 和 no-order safety。

220. 已完成样本扩展 no-order multi-profile shadow A/B，结论为 `DISCARD_SAMPLE_SCALE_REPAIR6075_P90_RESIDUAL_FAIL`。本轮在 preflight 清空后从隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_sample_scale_multprofile_repair6075_edge007_20260519T232703Z` 运行 3600s，配置为 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--profile-late-repair-after-s 60,75`；只复制当前 runner 单文件，使用远端 helper `/srv/pm_as_ofi/repo/scripts/resolve_market_ids.py`，以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client 运行。启动 shell 写 `runner.pid` 曾因 quoting/permission context 失败，但实际 runner PID `84751` 已确认唯一运行并补齐 pid 文件；未重启或叠加第二个窗口。完成后只拉回 manifest、multi-profile aggregate、各 profile aggregate/summary、stdout/stderr/runner.pid 小文件，未拉 events JSONL，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_sample_scale_multprofile_driver_20260519T232703Z/audit_manifest.json`：`repair60` 为 69 candidates、54 fills、25 pair actions、pair_pnl `+2.01225`、ROI `6.744%`、p90 `1.03`、residual_qty `15.0`、material_residual_lots `0`；`repair75` 为 87 candidates、67 fills、29 pair actions、pair_pnl `+2.0875`、ROI `5.785%`、p90 `1.09`、residual_qty `21.25`、material_residual_lots `0`。两档均保持正 PnL 和 no-order safety，但都没有达到 candidates `>=100`，且都失守 p90 `<=1.0` 与 residual_qty `<=10`。相对上一条 1800s 0..11 同窗 lead，扩大 offsets/sample 后 p90 与 residual tail 明显劣化，因此该 sample-scale `repair60/75` shadow 配置应丢弃，不进入 canary。收尾 preflight 发现另一个 namespace 的 `xuan_frontier` runner PID `87799` 已在 shared-ingress 上运行，xuan-research 不触碰它。下一跳切到 `causal_verifier`：本地、无网络、summary-only 比较 1800s 0..11 与 3600s 0..25 的 candidate/p90/residual/PnL/block/offset/slug/price-tail 差异，判断是否有可部署的 price-tail/offset 子集机制；未有新机制前不再花 shadow 窗口。

221. 已完成 sample-scale failure 的本地 summary-only 归因，结论为 `KEEP_PRICE_TAIL_OFFSET_ATTRIBUTION_FOR_LOCAL_PROXY`。本轮只读取两个已拉回 artifact 的 `manifest.json`、`multi_profile_aggregate_report.json`、各 profile `aggregate_report.json` 和 `*.summary.json`，未 SSH、未读 events JSONL、未扫 raw/replay、未连 socket、未启动 shadow。artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_sample_scale_failure_attribution_20260520T0041Z/manifest.json` 对比 1800s offsets `0..11` 与 3600s offsets `0..25` 后显示：`repair75` 的 new-only 23 个 slug 贡献 73 candidates、pair_pnl `+1.27075`，但也贡献 residual_qty `20.0`/residual_cost `6.0375`；overlap 3 个 slug 只有 residual_qty `1.25`。`repair75` 的 residual 主要集中在 YES 侧，top residual side qty 为 YES `18.75`、NO `1.25`；按 top residual price bucket 看，`px_0p30_0p45` 桶贡献 43 candidates、residual_qty `16.25`、pair_pnl 仅 `+0.416`，而 no-residual bucket 贡献 28 candidates、residual_qty `0`、pair_pnl `+1.6215`。`repair60` 也类似：new-only slug 贡献 residual_qty `13.75`，top residual 全部为 YES，`px_0p30_0p45` 与 `px_0p45_0p55` 桶合计 residual_qty `13.75`。结论：sample-scale 失败不是均匀 profile 崩坏，而是扩展窗口引入的新 slug/YES residual/price-tail bucket 把 p90 与 residual tail 拉坏；但 summary-only 还不能证明某个 `seed_px_hi` 或 offset 子集会保留足够 sample。因此下一跳仍留在本地 `causal_verifier`：做 summary-only proxy scorer，模拟 price-tail cap、slug/offset 子集剔除对 candidates、p90 proxy、residual、PnL 的影响；只有 proxy 找到保样本的机制后，才考虑新的 EC2 shadow。

222. 已完成 sample-scale failure 的本地 proxy scorer 与 event-lite verifier 加固，结论为 `UNKNOWN_SUMMARY_PROXY_FIXES_RISK_ONLY_BY_DESTROYING_SAMPLE`，但 `KEEP_EVENT_LITE_CAUSAL_VERIFIER_READY`。proxy scorer artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_price_tail_proxy_scorer_20260520T0041Z/manifest.json` 只读本地 summary/aggregate，不读 events JSONL、不 SSH、不连 socket、不扫 raw/replay；它验证了 removal-style 价格尾部/slug 过滤可以把 residual_qty 降到阈值内，但会破坏样本并且 p90 proxy 仍不过关。例如 repair75 最好过滤 `drop_yes_residual_qty_ge_3p75` 后 residual_qty `21.25 -> 8.75`、pair_pnl 仍 `+1.962875`，但 candidates 只剩 `62`，retained_candidate_ratio `71.26%`，proxy p90 仍 `1.16`；repair60 类似，residual_qty `15.0 -> 7.5` 但 candidates 只剩 `54`，proxy p90 仍 `1.16`。这说明“删掉坏 slug/残仓价桶”不是可部署修复，且 summary-only 无法区分 seed admission price 与最终 residual lot price。为避免继续用粗 summary 猜因果，`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭的 `--event-lite-summary`，只在 summary/aggregate 中输出轻量 bucket：candidate seed/public price、side、offset、fill seed price、block reason by side/price/offset、residual lot side/price、pair/net-pair cost buckets；默认 single-profile 与 multi-profile 行为保持不变。更新后的本地无网络 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke_20260520T012409Z/manifest.json` 为 `PASS`/`KEEP`，确认 event-lite 默认关闭、开启后计数与 metrics 对齐、aggregate 能汇总 event-lite，且无 network/SSH/order/cancel/redeem/shared-ingress/broker/service side effects；activation 回归 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260520T012422Z/manifest.json` 也通过。下一跳仍是 `causal_verifier`：先用 event-lite summary 做本地/fixture 级 seed_px_hi、offset、side 的可部署机制验证；只有它能同时保住 candidates、p90、residual 和 PnL，且 EC2 preflight 无其它 namespace runner 时，才允许带 `--event-lite-summary` 的 bounded no-order shadow。

223. 已完成一次 corrected shared-ingress 并发策略后的 xuan-research same-host no-order event-lite shadow，结论为 `UNKNOWN_EVENT_LITE_REPAIR60_RISK_PASS_SAMPLE_THIN`。本轮先修正自动化策略：shared-ingress 本来支持多个只读 client，因此其它 namespace 的 no-order read-only runner 只作为上下文，不再被当作硬 blocker；真正 blocker 限定为 xuan-research 自身重叠、broker/socket 健康、no-order safety、写路径或 transport/resource failure。随后在隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_eventlite_multprofile_repair6075_edge007_20260520T022345Z` 以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 跑 1800s，配置为 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--profile-late-repair-after-s 60,75`、`--event-lite-summary`。启动时日志重定向/`runner.pid` 写入因 shell 权限上下文失败，但真实 runner PID `90684` 已确认唯一运行并补齐 pid 文件；未重启或叠加窗口。完成后只拉回 manifest、multi-profile aggregate、各 profile aggregate/summary、stdout/stderr/runner.pid 小文件，未拉 events JSONL，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem，收尾确认无 xuan-research runner 残留。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_eventlite_driver_20260520T022345Z/audit_manifest.json`：`repair60` 为 30 candidates、23 fills、11 pair actions、pair_pnl `+1.12975`、ROI `9.379%`、p90 `1.00`、residual_qty `6.25`、material_residual_lots `0`；`repair75` 为 40 candidates、30 fills、13 pair actions、pair_pnl `+1.319`、ROI `8.226%`、p90 `1.01`、residual_qty `9.805`、material_residual_lots `0`。结论：`repair60` 在本窗口通过 p90/residual/PnL/no-order risk gates，但 1800s 只有 30 candidates，按小时外推仍明显低于 100；`repair75` 样本略好但 p90 轻微破线。event-lite 进一步显示 residual 同时出现在 YES/NO、低价/高价 bucket，简单 price/side/offset 删除会继续破坏已经很薄的样本，因此本轮不是 promotion/G2 canary evidence。下一跳切到本地 `causal_verifier`：扩展或评分 residual-source event-lite，让每个 residual lot 关联其 source seed/public price 与 offset，再判断是否存在可部署 admission rule；在该 verifier 给出 bounded mechanism 前，不再花新的 EC2 shadow 窗口。

224. 已接入同事交付的本地 candidate pipeline V1，结论为 `KEEP_CANDIDATE_PIPELINE_V1_DATA_ADAPTER_READY`。本轮只做 `data_adapter` lane 的本地轻量 manifest/schema preflight 与小表聚合，不 SSH、不连 shared-ingress、不扫 raw/replay/collector、不重新扫全量 completion store。新底座位于 `/Users/hot/web3Scientist/poly_backtest_data/derived/completion_candidate_pipeline_v1`：candidate base `local_20260502_20260518_paircap102/candidate_base.duckdb` 的 `candidate_base` 表为 `3,740,411` rows，对应 source rows `27,040,809`，candidate/source ratio `0.138325`；state-machine result `pass_local_completion_residual_cooldown_officialfee_e055_t5_imb125_rc30_050_20260502_20260518_publicfull_v2/state_machine_results.duckdb` 包含 `candidate_registry` `51,618` rows、`residual_lots` `3,082` rows、`summary_by_day` `15` rows。manifest 明确 labels 为 `20260502_20260508,20260509..20260513,20260516,20260517,20260518`，days 覆盖 `2026-05-02..13, 2026-05-16, 2026-05-17, 2026-05-18`，排除 `20260514/20260515/20260519`；strict cache coverage 与 public account audit coverage 都通过，public audit 覆盖 `20260502_20260513` 与 `20260516_20260518`，但仍是 public proxy truth，`public_account_execution_truth_v1_private_truth=false`、`promotion_gate_pass=false`、`deployable=false`、`can_support_strategy_promotion=false`。核心 state-machine 指标：active_markets `4307`、pair_actions `27358`、pair_qty `30426.4325`、weighted_pair_cost `0.887251`、gross_pnl `+3618.707937`、official taker fee `946.907010`、fee_after_pnl `+2671.800927`、stress100_worst_pnl `+1538.959062`、worst_day_fee_after_pnl `+146.462785`、residual_qty_rate `5.0178%`、residual_cost_rate `4.4285%`。新增 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_v1_data_adapter_20260520T031655Z/manifest.json` 还确认了下一步所需的因果 join 字段已经存在：`residual_lots.source_seed_action_id` / `candidate_row_id` 可回连 `candidate_registry.candidate_row_id`，而 registry 暴露 `seed_px`、`public_trade_price`、`offset_s`、`side`、strict-cache/public-audit 覆盖字段。因此下一跳不应再用 shadow summary 猜因果，也不应重扫 completion store；应在本地 `causal_verifier` lane 直接从 `state_machine_results.duckdb` 的 `candidate_registry JOIN residual_lots` 评分 residual-source bucket，寻找能保住 sample、fee_after_pnl、stress100_worst_pnl 和 residual rates 的 bounded admission rule。

225. 已完成 candidate pipeline V1 的本地 residual-source causal verifier，结论为 `KEEP_LATE_OFFSET_RESIDUAL_SOURCE_LEAD_RESEARCH_ONLY`。新增脚本 `scripts/xuan_b27_dplus_candidate_pipeline_residual_source_verifier.py`，只读 `state_machine_results.duckdb` 的 `candidate_registry`、`residual_lots`、`summary_by_day` 以及本地 manifest；不 SSH、不连 shared-ingress、不扫 raw/replay/collector、不读全量 completion store、不启动 shadow。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_residual_source_verifier_20260520T032725Z/manifest.json` 以 residual lot 的 `candidate_row_id/source_seed_action_id` 回连 seed source，按 seed/public price、offset、side 等 deployable pre-trade bucket 做 source-attribution proxy。最强规则是 `drop_offset=off_90_120`：该 bucket 占 candidates `14,111/51,618 = 27.34%`，但贡献 residual_qty `3015.455/3214.7725 = 93.80%` 和 residual_cost `1217.5584`；proxy 移除后 retained candidates `37,507`、retained candidate ratio `72.66%`、proxy residual_qty_rate 从 `5.0178%` 降至 `0.4279%`，strict/public coverage ratio 都为 `1.0`。这不是 deployable 结论，因为 fee_after_pnl、stress100_worst_pnl 和库存路径没有被重算；并且 hard cutoff 族曾在 EC2 shadow 中失败过，不能直接拿去 shadow/canary。新的可执行下一跳是本地 candidate-pipeline state-machine rerun：从 candidate_base 使用同一 official-fee profile，增加等价于 `seed_offset_max_s=90` 的 late-offset admission variant，比较 sample、fee_after_pnl、stress100_worst_pnl、worst_day_fee_after_pnl 与 residual rates；只有本地 rerun 确认 proxy 后，才讨论是否需要后续 no-order shadow。

226. 已完成 candidate pipeline V1 的本地 state-machine rerun，结论为 `DISCARD_OFFSET90_STATE_MACHINE_RESIDUAL_WORSE`，推翻上一轮“直接丢弃 late offset”的 proxy 解读。新增脚本 `scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py`，只读 `candidate_base.duckdb` 与 baseline manifests，不读 raw/replay/collector、不扫全量 completion store、不 SSH、不连 shared-ingress、不启动 shadow。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_offset90_rerun_20260520T041316Z/manifest.json` 同时跑 control `seed_offset_max_s=120` 与 variant `seed_offset_max_s=90`。control 复现质量足够：seed_actions `51618` 与 baseline 完全一致，fee_after_pnl `2671.374496` vs baseline `2671.800927`（相对误差 `-0.016%`），stress100_worst_pnl `1539.941187` vs `1538.959062`（`+0.064%`），residual_qty_rate `5.0175%` vs `5.0178%`。但 offset90 variant 虽保留 `72.66%` seed_actions（`37507`）且 fee_after_pnl 仍正（`+1867.286633`）、stress100 仍正（`+665.413925`）、worst-day 仍正（`+101.516797`），残仓却恶化：residual_qty_rate 从 `5.0175%` 升至 `6.8701%`，residual_cost_rate 从 `4.4247%` 升至 `6.3797%`。解释是：late-offset residual lot 的 source 在 90-120s，不等于 late-offset seed 应该被硬禁；直接禁掉这段也禁掉了大量 pairing/repair 路径，库存状态反而更差。下一跳不再跑 offset hard cutoff，也不花 EC2 shadow；应继续本地 `causal_verifier`，测试 deployable 的 late-offset repair-only / imbalance-reducing admission 机制：90s 后只允许降低当前同侧暴露或改善 pair opportunity 的 seed，而不是完全丢弃 late-offset。

227. 已完成 candidate pipeline V1 的 late-offset repair-only 本地 state-machine rerun，结论为 `KEEP_LATE_REPAIR90_STATE_MACHINE_RESEARCH_ONLY`。同一脚本 `scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py` 现在同时跑三档：control `seed_offset_max_s=120`、已丢弃的 hard `seed_offset_max_s=90`、以及 `late_repair_only_after_90`（`offset_s >= 90` 后只允许当前同侧 qty 低于对侧 qty 的 underweight/repair seed，挡掉 risk-increasing late seed）。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_late_repair90_rerun_20260520T042250Z/manifest.json` 仍只读 candidate_base 与本地 manifests，不扫 raw/replay/collector/full completion store、不 SSH、不连 shared-ingress、不启动 shadow。control 继续高质量复现 baseline；late-repair90 相比 control 保留 seed_actions `41580/51618 = 80.55%`、pair_qty `25510.275/30426.4325 = 83.84%`，fee_after_pnl 仍正 `+2115.230573`、worst-day 仍正 `+119.455984`，且 stress100_worst_pnl 从 `+1539.941187` 提升到 `+2141.110075`；最关键是 residual_qty_rate 从 `5.0175%` 降到 `1.2097%`，residual_cost_rate 从 `4.4247%` 降到 `0.9527%`，对应 residual rate reductions `75.89%/78.47%`。block 机制也符合假设：新增 `seed_block_late_repair_only=85655`，说明它不是硬删 late offset，而是只挡 late risk-increasing seeds，保留 late repair seed。因此下一跳可以切到 `implementability_review`：把该 gate 以默认关闭参数接入 no-order shadow runner，并做本地无网络 smoke；只有 smoke 证明默认路径不变、repair seed 放行、risk-increasing late seed 被挡，才允许后续 bounded EC2 no-order shadow。

228. 已完成 late-repair90 gate 的 no-order shadow runner implementability review，结论为 `KEEP_LATE_REPAIR_ONLY_SHADOW_GATE_READY`。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭参数 `--late-repair-only-after-s`：当 offset 达到阈值后，只有当前同侧虚拟库存 qty 低于对侧 qty 的 underweight/repair seed 才允许继续；同侧 qty 大于等于对侧 qty 的 late risk-increasing seed 以 `late_repair_only` block 计数挡下。该实现不改变既有 `--late-repair-after-s` / `--profile-late-repair-after-s` 语义，flag 未设置时 default single-profile 与 multi-profile 路径保持原样；candidate event 额外记录 `late_repair_only_active` 便于后续 audit。新增本地无网络 smoke `scripts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_smoke.sh`，artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_smoke_20260520T050930Z/manifest.json` 为 `PASS`/`KEEP`：验证默认路径仍允许 late seed、threshold=90 时 late risk-increasing seed 被挡、late underweight repair seed 被放行，summary/aggregate/event-lite 均暴露 `late_repair_only` block count，且无 network/SSH/order/cancel/redeem/shared-ingress/broker/service side effects。回归 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260520T050836Z/manifest.json` 与 `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke_20260520T050836Z/manifest.json` 均 PASS，确认 activation gate 与 same-window multi-profile verifier 未被破坏。下一跳切回 `shadow_observe`：先做 bounded EC2 preflight；若无 xuan-research runner 重叠且 shared-ingress metadata 健康，再运行一个 no-order、dry-run、隔离目录、只拉小 summary/aggregate 的 late-repair-only shadow，候选配置为 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--late-repair-only-after-s 90`、`--event-lite-summary`。这仍是 shadow research queue，不是 G2 canary 或 promotion 证据。

229. 已完成 late-repair-only90 的 bounded same-host no-order shadow，结论为 `UNKNOWN_LATE_REPAIR_ONLY90_RESIDUAL_PASS_P90_SAMPLE_FAIL`。本轮先做 EC2 bounded preflight：没有 active xuan-research runner，`/srv/pm_as_ofi/shared-ingress-main/market.sock` 存在且 broker heartbeat 新鲜；另有一个 xuan_frontier read-only runner 连接同一 shared-ingress，但按并发修正规则只作为上下文，不作为 blocker。随后从隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_late_repair_only90_edge007_20260520T055412Z` 以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client 运行 1800s，配置为 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--late-repair-only-after-s 90`、`--event-lite-summary`。只复制当前 runner 单文件到隔离 run dir，使用远端 helper `/srv/pm_as_ofi/repo/scripts/resolve_market_ids.py`，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem。拉回本地只包含 manifest、aggregate、summary、stdout/stderr/runner.pid 小文件，未拉 events JSONL；post-run sanity 确认无 xuan-research runner 残留。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_driver_20260520T055412Z/audit_manifest.json`：26 markets、56 candidates、44 fills、29 pair actions、pair_qty `25.7675`、pair_pnl `+1.976288`、ROI on filled cost `8.217%`、net_pair_cost_p90 `1.02`、residual_qty `7.4975`、residual_cost `2.31105`、material_residual_lots `0`。Acceptance artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_late_repair_only90_20260520T055412Z/manifest.json` 为 `FAIL_SHADOW_TRADING_SAMPLE_SIZE`，同时记录 pnl metrics 失败项，因为 candidates `56 < 100` 且 p90 `1.02 > 1.0`；residual risk、PnL、market scope 与 no-order safety 通过。event-lite 显示 residual 已全部集中到 NO side，主要是 `px_lt_0p30` residual lot bucket；`late_repair_only` block 共 `90`，全部在 `offset_90_120`；p90 失败来自少量 `pair_cost_1p00_1p05` bucket，而不是 residual tail。结论：late-repair-only90 是 residual-control 进展，但还不是 shadow acceptance 或 canary evidence；不要重复同一 1800s shadow。下一跳切到本地 `causal_verifier`：用 summary/event-lite 对比本轮 late-repair-only90 与 prior repair60/75 event-lite/sample-scale artifacts，定位 `net_pair_cost_1p00_1p05` 的 slug/offset/seed-price/public-price 来源；只有找到一个不破坏 sample/residual/PnL 的 bounded p90-tail 机制，才允许新的 shadow。

230. 已完成 late-repair-only90 p90 tail 的本地 event-lite comparator，结论为 `UNKNOWN_P90_TAIL_SOURCE_FIELDS_INSUFFICIENT`。新增脚本 `scripts/xuan_b27_dplus_shadow_eventlite_p90_tail_comparator.py`，只读三个已拉回 artifact 的本地 `manifest.json`、`aggregate_report.json`、`multi_profile_aggregate_report.json` 和 `*.summary.json`：late-repair-only90 shadow `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_driver_20260520T055412Z`、event-lite repair60/75 shadow `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_eventlite_driver_20260520T022345Z`、sample-scale repair60/75 shadow `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_sample_scale_multprofile_driver_20260519T232703Z`；不读 events JSONL、不 SSH、不连 socket、不扫 raw/replay/collector/full completion store、不启动 shadow。artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_eventlite_p90_tail_comparator_20260520T064229Z/manifest.json` 显示：late-repair-only90 相比 event-lite repair60 candidates `30 -> 56`、pair_pnl `+1.12975 -> +1.976288`，但 p90 `1.00 -> 1.02`，residual `6.25 -> 7.4975`；相比 sample-scale repair60/75，late-repair-only90 residual 明显改善（`15.0/21.25 -> 7.4975`）并保持正 PnL，但 candidates 仍低于 100 且 p90 仍破线。p90 tail 在 late-repair-only90 中只有 `3/29` pair actions 落入 high-cost buckets（约 `10.34%`），但 slug-removal proxy 不可部署：删掉 high-cost slugs 后 p90 proxy 通过、residual 仍低，但 candidates 只剩 `36`；删 residual-heavy 或 NO low-price residual slug 也只剩 `46` 或 `31` candidates。event-lite repair60 是唯一 p90/residual/PnL 均过的 profile，但只有 `30` candidates；repair75/late-repair-only90 都只差一个很窄的 p90 tail。结论：当前 summary/event-lite 能证明“粗暴删坏 slug/bucket 会破坏样本”，但不能把 high-cost pair action 回连到 source seed 的 `seed_px`、`public_trade_price`、`offset`、`side`、`quote_intent/source_order_id`，因此不能推出可部署 p90-tail admission rule。下一跳切到 `implementability_review`：给 no-order runner 增加默认关闭的 pair-source event-lite attribution，本地 smoke 需证明默认路径不变，并在 summary/aggregate 中输出 `pair_cost_by_source_seed_px_bucket`、`pair_cost_by_source_public_px_bucket`、`pair_cost_by_source_offset_bucket`、`pair_cost_by_source_side` 和 source quote id/top high-cost pair context；在该本地 smoke 通过前，不再跑 EC2 shadow/canary。

231. 已完成 pair-source event-lite attribution 的 implementability review，结论为 `KEEP_PAIR_SOURCE_EVENT_LITE_READY_FOR_SHADOW_OBSERVE`。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭参数 `--pair-source-event-lite-summary`，且 CLI 要求它只能和 `--event-lite-summary` 一起使用；flag 未设置时，既有 single-profile、multi-profile、event-lite、late-repair-only 行为保持不变。开启后，每个 internal pair / FAK salvage pair 会把 net pair cost bucket 回连到 source lot 的 seed price bucket、public trade price bucket、offset bucket、side，并在 summary/aggregate 中输出 `pair_cost_by_source_seed_px_bucket`、`pair_cost_by_source_public_px_bucket`、`pair_cost_by_source_offset_bucket`、`pair_cost_by_source_side`、`pair_source_action_count`、`pair_source_record_count` 和 `top_high_cost_pair_sources`（含 source `quote_intent_id`/`source_order_id`/trigger metadata）。本地无网络 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_pair_source_event_lite_smoke_20260520T073443Z/manifest.json` 为 `PASS`/`KEEP`：确认默认 event-lite 不出现 pair-source 字段、开启后 pair_source_action_count 与 pair_actions 对齐、internal pair 的 source record count 为每 pair 两条 lot source、high-cost bucket 能按 source seed/public/offset/side 归因，aggregate 能汇总这些计数，且关闭 event-lite 时 pair-source flag 不产生 summary 字段。回归 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260520T073443Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke_20260520T073443Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_smoke_20260520T073451Z/manifest.json` 均 PASS。下一跳可切回 `shadow_observe`，但仍不是 promotion/canary：先做 bounded EC2 preflight；若无 active xuan-research runner、shared-ingress metadata 健康且无 no-order safety blocker，运行一个 1800s same-host no-order shadow，沿用 late-repair-only90 配置并增加 `--event-lite-summary --pair-source-event-lite-summary`，只拉小 manifest/aggregate/summary/stdout/stderr/runner.pid，不拉 events JSONL。

232. 已完成 pair-source event-lite 的 bounded same-host no-order shadow，结论为 `UNKNOWN_PAIR_SOURCE_PXGE055_TAIL_BUT_SHADOW_GATES_FAIL`。本轮先确认 EC2 根盘与 inode 已恢复（`/dev/root` 145G 中 85G 可用、42% 使用率，inode 1%），没有 active xuan-research runner，shared-ingress sockets 与 broker manifest 存在；随后在隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_pair_source_eventlite_late_repair90_edge007_20260520T080809Z` 以 `pmofi`、`PM_DRY_RUN=true` 运行 1800s no-order shadow。配置沿用 late-repair-only90：edge `0.07`、target_qty `5`、imbalance_qty_cap `1.25`、activation `opp_seen15`、pairing-only residual、offsets `0..25`、`--late-repair-only-after-s 90`，并开启 `--event-lite-summary --pair-source-event-lite-summary`。只复制当前 runner 单文件到隔离目录，使用远端 helper `/srv/pm_as_ofi/repo/scripts/resolve_market_ids.py`，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem；pullback 只包含 manifest、aggregate、summary、stdout/stderr/runner.pid，小文件 artifact 为 `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_pair_source_eventlite_driver_20260520T080809Z`，未拉 events JSONL。acceptance `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_pair_source_eventlite_20260520T080809Z/manifest.json` 失败：candidates `42 < 100`，net_pair_cost_p90 `1.23 > 1.0`，residual_qty `11.0625 > 10`；pair_pnl 仍正 `+0.55`、ROI on filled cost `4.22%`、material_residual_lots `0`，但相对上一轮 late-repair-only90（56 candidates、p90 1.02、residual_qty 7.4975、pair_pnl +1.976288）全面退化。新的有效信息是 source attribution：`pair_cost_gt_1p05` 的高成本 pair 由 source public px `0.55/0.82` 触发，`pair_cost_1p00_1p05` source records 也主要集中在 `source_public_px_ge_0.55`，而 residual 主要在低价残仓 bucket（NO 7.5、YES 3.5625；`px_lt_0p30` 7.3125、`px_0p30_0p45` 3.75）。结论：pair-source attribution 成功解释了 p90 tail 的来源，但本轮 shadow 自身不是 KEEP，也不是 promotion/canary evidence；下一跳应切到本地 `causal_verifier`，从 candidate_base/state-machine 复跑 late-repair90 + bounded `public_trade_px_hi≈0.55` source-admission variant，先验证 sample、fee_after_pnl、stress100、worst-day 和 residual rates，再决定是否值得新的 EC2 shadow。

233. 已完成 candidate pipeline V1 的 source-public-price cap 本地 state-machine rerun，结论为 `DISCARD_PUBLIC_PX_CAP_SAMPLE_OR_STRESS_COLLAPSE`。`scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py` 新增默认关闭的 `public_trade_px_hi` profile 字段，并避免 Python 3.9 下 `zip(strict=True)` 不兼容；本轮 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_public_px_cap_rerun_20260520T085622Z/manifest.json` 只读 `candidate_base.duckdb` 与本地 manifests，不 SSH、不连 shared-ingress、不扫 raw/replay/collector/full completion store、不启动 shadow。实验假设是：pair-source shadow 看到高成本 pair source 集中在 source public px `>=0.55`，因此在 late-repair90 上加 `public_trade_px_hi=0.55` 应能压低 pair-cost/residual risk 并保留样本。结果直接否定：相对 late-repair90 control，public-px cap 的 seed_actions 从 `41580` 降到 `18020`（retention `43.34%`），pair_qty 从 `25510.275` 降到 `9478.9925`，fee_after_pnl 从 `+2115.230573` 降到 `+860.3128`，stress100_worst_pnl 从 `+2141.110075` 降到 `+244.761875`，worst_day 从 `+119.455984` 降到 `+34.120532`；虽然 weighted_pair_cost 从 `0.887289` 降到 `0.829612`，但 residual_qty_rate 从 `1.2097%` 恶化到 `15.4130%`，residual_cost_rate 从 `0.9527%` 恶化到 `12.7215%`，并新增 `seed_block_public_trade_px_hi=156470`。结论：source public px 硬 cap 会同时删除必要 repair seed，和 late-offset hard cutoff 一样会破坏库存修复路径；不要把 `public_trade_px_hi=0.55` 作为 deployable/shadow 机制。下一跳若继续利用 pair-source 线索，必须测试更结构化的 admission：只对 risk-increasing seed 应用 public px cap，underweight/repair seed 仍放行；在该本地 verifier 过关前，不再跑 EC2 shadow。

234. 已完成 candidate pipeline V1 的 risk-increasing-only source-public-price cap 本地 state-machine rerun，结论为 `DISCARD_RISK_PUBLIC_PX_CAP_RESIDUAL_WORSE`。`scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py` 现在新增默认关闭的 `risk_increasing_public_trade_px_hi` profile 字段：当 `public_trade_price > 0.55` 时，仅在 seed 会增加当前同侧库存风险（same-side qty 大于等于 opposite-side qty）时阻断，underweight/repair seed 继续放行。本轮 artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_risk_px_cap_rerun_20260520T092627Z/manifest.json` 仍只读 `candidate_base.duckdb` 与本地 manifests，不 SSH、不连 shared-ingress、不扫 raw/replay/collector/full completion store、不启动 shadow。相对 late-repair90 control，该 variant 保留大部分样本：seed_actions `41580 -> 38537`（retention `92.68%`）、pair_qty `25510.275 -> 23582.335`（retention `92.44%`），fee_after_pnl 仍正 `+1982.084355`、stress100_worst_pnl 仍正 `+1977.138325`、worst_day 仍正 `+107.280007`；weighted_pair_cost 只有轻微改善 `0.887289 -> 0.886060`，但 residual_qty_rate 从 `1.2097%` 恶化到 `1.5258%`，residual_cost_rate 从 `0.9527%` 恶化到 `1.0928%`，新增 `seed_block_risk_increasing_public_trade_px_hi=57216`。结论：即使只挡 risk-increasing 高 public price seed，也仍会削弱后续库存修复，收益只有极小 pair-cost 改善，不满足“不恶化 residual”的门槛；不要把全侧 `public_trade_px_hi=0.55` risk gate 接入 shadow runner。下一跳仍保持本地 `causal_verifier`：只做一个 side-specific verifier，检验 pair-source 里出现的高成本 NO source 是否需要单独限价；若 side-specific 也不能同时改善 pair-cost proxy 与 residual，就冻结 source-public-price cap family，回到 late-repair90 之外的新机制搜索。

235. 已完成 candidate pipeline V1 的 NO-side-only risk-increasing source-public-price cap 本地 state-machine rerun，结论为 `DISCARD_SIDE_RISK_PUBLIC_PX_CAP_RESIDUAL_WORSE`，同时把“机制失败”和“经济净 PnL 失败”区分开。`scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py` 现在支持 `risk_increasing_public_trade_px_hi_side`，本轮只对 `NO` side 且 `public_trade_price > 0.55`、并且会增加当前同侧库存风险的 seed 阻断；`YES` seed 和所有 underweight/repair seed 放行。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_side_risk_px_cap_rerun_20260520T095748Z/manifest.json` 仍只读 `candidate_base.duckdb` 与本地 manifests，不 SSH、不连 shared-ingress、不扫 raw/replay/collector/full completion store、不启动 shadow。相对 late-repair90 control，该 variant 保留 seed_actions `40101/41580 = 96.44%`、pair_qty `24572.5675/25510.275 = 96.32%`，fee_after_pnl 仍正 `+2039.669138`、stress100_worst_pnl 仍正 `+2061.531888`、worst-day 仍正 `+114.857159`；但 weighted_pair_cost 只从 `0.887289` 微降到 `0.886838`，residual_qty_rate 从 `1.2097%` 恶化到 `1.3564%`，residual_cost_rate 从 `0.9527%` 恶化到 `1.0037%`，block count 为 `26624`。因此它不是“亏钱策略”，而是“相对 late-repair90 没有提供足够风险/成本改进的 admission 机制”；不要把 NO-side public px cap 接入 shadow runner。source-public-price cap family 现在应冻结，除非未来有新的 source-of-truth 字段证明它不是在删除必要 repair seed。下一跳切到 `status_hygiene`：把 autoresearch 的评分口径写清楚，区分 research mechanism ranking、shadow/canary hard gate、和最终经济净 PnL，避免以后把 residual 单项恶化误写成策略经济失败。

236. 已补充 D+ scoring contract，结论为 `KEEP_SCORING_CONTRACT_READY`。新增 `docs/research/xuan/DPLUS_SCORING_CONTRACT_ZH.md`，并在 `docs/research/xuan/DPLUS_AUTORESEARCH_LOOP_ZH.md` 中接入。新口径明确三层评价：第一层是 research mechanism ranking，用于判断某个机制相对 control 是否值得继续，正经济但 residual/p90 变差应写成 `TRADEOFF` 或 `MECHANISM_WEAKER`，而不是策略经济失败；第二层是 shadow/canary hard gate，`candidates>=100`、`net_pair_cost_p90<=1.0`、`residual_qty<=10`、`residual_cost<=5`、`material_residual_lots=0`、no-order safety 等是 promotion blockers，不是收益函数；第三层是 final economic objective，即 `pair_pnl + residual_settle_or_mark_pnl - official_fee - stress_or_impact_budget` 必须稳定为正，并通过 OOS / walk-forward / source-of-truth replay。该文档不改变 runner 或 gate 行为，只防止后续 artifact 把 residual 单项恶化误写成策略净 PnL 失败。下一跳回到 `causal_verifier`，选择一个非 source-public-price 的库存路径机制；source-public-price cap family 继续冻结。

237. 已把 scoring contract 接入 shadow acceptance 代码路径，结论为 `KEEP_SCORING_SPLIT_IMPLEMENTED`。`scripts/xuan_b27_dplus_shadow_trading_acceptance.py` 现在同时输出 `research_ranking` 和 `promotion_gate`：前者用于研究排序，正经济但 residual/pair-tail budget 未过时标为 `TRADEOFF_*`，不再写成经济失败；后者继续决定 `acceptance_passed` 和 G2 canary blocker。promotion 风险预算从固定 `residual_qty`/`residual_cost`/action-level `net_pair_cost_p90` 改成归一化 proxy：`residual_qty_share_of_filled`、`residual_cost_share_of_filled_cost`、`pair_tail_loss_share_of_pair_pnl`，旧固定阈值仅作为 `legacy_absolute_reference` 暴露。新增 artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_acceptance_scoring_split_20260520T105144Z/manifest.json` 记录本轮实现；smoke `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_smoke_20260520T105333Z/manifest.json` PASS，证明通过 fixture 会得到 `KEEP_ECONOMIC_RESEARCH_ONLY`，residual budget fixture 会得到 `FAIL_SHADOW_TRADING_PROMOTION_RISK_BUDGET` + `TRADEOFF_ECON_POSITIVE_RISK_BUDGET`，low-sample/high-cost fixture 会保留 sample blocker 同时记录 promotion risk budget blocker。`scripts/xuan_b27_dplus_canary_readiness_plan_smoke.sh` 已允许新的 promotion-risk status，完整 smoke `xuan_research_artifacts/xuan_b27_dplus_canary_readiness_plan_smoke_20260520T105454Z/manifest.json` PASS，避免 readiness hygiene 把新状态误判为未知。该变更不启动 shadow、不连 shared-ingress、不读 raw/replay/collector/full completion store、不改变 runner 或交易路径；下一跳回到 `causal_verifier`，但后续 artifact 必须同时报告 research ranking 与 promotion gate，不得只用 residual 单项恶化来判定经济失败。

238. 已完成 dynamic risk-increasing imbalance target 的本地 candidate-pipeline state-machine verifier，结论为 `UNKNOWN / TRADEOFF_ECON_POSITIVE_RISK_WORSE`。`scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py` 新增 `risk_increasing_imbalance_qty_cap` profile 字段，并在 late_repair90 control 上测试 `variant_late_repair90_dynamic_risk_imbalance_cap_1p10`：仅当 seed 会增加当前同侧库存时，把 post-seed imbalance budget 从 `1.25` 收到 `1.10`；underweight/repair seed 仍按 full target_qty=5 和原 cap 放行。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_dynamic_imbalance_rerun_20260520T110120Z/manifest.json` 只读 `candidate_base.duckdb` 与本地 manifests，不 SSH、不连 shared-ingress、不扫 raw/replay/collector/full completion store、不启动 shadow。相对 late_repair90，dynamic imbalance 提高 seed_actions `41580 -> 46286`、pair_qty `25510.275 -> 27106.765`、fee_after_pnl `+2115.230573 -> +2378.473320`、worst_day `+119.455984 -> +131.999778`，weighted_pair_cost 微降 `0.887289 -> 0.886414`；但 residual_qty_rate 从 `1.2097%` 恶化到 `4.7272%`，residual_cost_rate 从 `0.9527%` 恶化到 `4.3552%`，stress100_worst_pnl 从 `+2141.110075` 降到 `+1415.802325`，`seed_block_dynamic_imbalance_qty=59993`。按新 scoring contract，这不是经济失败，因为 fee/stress/worst-day 仍为正；但它是风险形态明显变差的 TRADEOFF，不支持 shadow/canary。下一跳应本地归因 dynamic imbalance 的新增 residual 来自哪些 day/side/offset/repair-opportunity buckets，再决定是否有更窄的非价格库存路径机制；不要直接把 dynamic cap 接入 EC2 shadow。
239. 已完成 dynamic imbalance tradeoff 的本地 residual-source attribution，结论为 `UNKNOWN_DYNAMIC_IMBALANCE_ATTRIBUTION_MIXED`。新脚本 `scripts/xuan_b27_dplus_dynamic_imbalance_tradeoff_attribution.py` 只读 candidate pipeline V1 的 `candidate_base.duckdb`、dynamic imbalance rerun manifest 和本地 manifests，不 SSH、不连 shared-ingress、不扫 raw/replay/collector/full completion store、不启动 shadow。artifact `xuan_research_artifacts/xuan_b27_dplus_dynamic_imbalance_tradeoff_attribution_20260520T114110Z/manifest.json` 复现 late_repair90 与 dynamic_imbalance_1p10 的 final residual lot source，并把新增 residual 归因到 source day/side/offset/imbalance 状态。结果显示 dynamic variant 的 residual 总量从 `624.7475` 增至 `2689.97`，新增 `2065.2225` qty、`876.381925` cost；新增 residual 几乎都来自 `off_90_120` late repair active bucket（delta qty `2125.205`），且核心集中在 `off_90_120|underweight_0_1`（delta qty `1900.855`）。这说明风险不是来自被 dynamic cap 处理的 risk-increasing seed 本身，而是来自 90s 后仍以 full target_qty=5 放行的小缺口 underweight repair seed；但该风险横跨多数日期和 YES/NO 两侧，不能直接把某个 day/side 过滤成 deployable 规则。下一跳仍走本地 `causal_verifier`：测试 `late_repair90_fill_to_balance_after_90`，即 offset>=90 后继续允许 underweight/repair seed，但当 underweight deficit 小于 target_qty 时，把 seed qty 限到当前 `opposite_qty - same_qty` 缺口；早段 seed 不变，并与 late_repair90 和 dynamic_imbalance_1p10 对比。该方向不是 promotion/canary evidence，dynamic risk-increasing imbalance cap `1.10` 仍不得直接 shadow。
240. 已完成 `late_repair90_fill_to_balance_after_90` 本地 candidate-pipeline state-machine verifier，结论为 `KEEP_ECONOMIC_RESEARCH_ONLY`。`scripts/xuan_b27_dplus_candidate_pipeline_state_machine_rerun.py` 新增默认关闭的 `late_repair_fill_to_balance_after_s` profile 字段；当 offset>=90 且 seed 是 underweight/repair 时，把 seed qty 限到实时 `opposite_qty - same_qty` 缺口，不再让小缺口 repair 以 full target size 过量补仓。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_state_machine_fill_to_balance_rerun_20260520T120527Z/manifest.json` 只读 `candidate_base.duckdb` 与本地 manifests，不 SSH、不连 shared-ingress、不扫 raw/replay/collector/full completion store、不启动 shadow。相对 late_repair90 control，fill-to-balance 保留 seed_actions `39898/41580 = 95.95%`、pair_qty `24632.495/25510.275 = 96.56%`，fee_after_pnl 仍正 `+2015.548633`（少 `99.68194`），worst-day 仍正 `+113.905533`；同时 stress100_worst_pnl 从 `+2141.110075` 提高到 `+2219.103187`，residual_qty_rate 从 `1.2097%` 降到 `0.5219%`，residual_cost_rate 从 `0.9527%` 降到 `0.2714%`，分别降低 `56.86%` 和 `71.51%`。本轮发生 `seed_qty_cap_late_fill_to_balance=5217`、`seed_block_late_fill_to_balance_qty=6805`、削减 late repair qty `4479.1775`。dynamic+fill 对照没有保住 dynamic 的新增样本/PNL，因此 dynamic imbalance cap `1.10` 仍冻结；真正 KEEP 的机制是 plain late_repair90 上的 late fill-to-balance sizing。下一跳切到 `implementability_review`：给 no-order shadow runner 增加默认关闭参数和本地 smoke；在 smoke 通过前不跑 EC2 shadow/canary。
241. 已完成 no-order shadow runner 的 late repair fill-to-balance implementability review，结论为 `KEEP_LATE_REPAIR_FILL_TO_BALANCE_RUNNER_READY`。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭参数 `--late-repair-fill-to-balance-after-s`，CLI 要求它必须和 `--late-repair-after-s` 或 `--late-repair-only-after-s` 搭配；未设置时既有 single-profile、multi-profile、event-lite、pair-source event-lite、late-repair-only 行为保持不变。开启后，offset>=threshold 且 `same_qty < opp_qty` 的 repair seed 会把 base seed qty 限到实时 `opp_qty - same_qty`，若 cap 后不超过 dust 则以 `late_repair_fill_to_balance_qty` block；candidate event 暴露 `base_seed_qty`、`late_repair_fill_to_balance_active`、deficit 和 qty reduction，summary/aggregate 暴露 `late_repair_fill_to_balance_candidates/caps/blocks/qty_reduction`。本地无网络 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_smoke_20260520T123744Z/manifest.json` PASS/KEEP：默认 late-repair-only 仍按 5 下单；开启后 3 手缺口只下 3；0.5 手 dust 缺口被 block；event-lite block bucket、summary 和 aggregate 计数均正确；CLI 单独传 fill-to-balance 参数会拒绝。回归 smoke `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260520T123755Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke_20260520T123755Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_smoke_20260520T123755Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_pair_source_event_lite_smoke_20260520T123800Z/manifest.json` 均 PASS。该实现仍只是 no-order implementability evidence，不是 promotion/canary；下一跳可切回 `shadow_observe`，先做 EC2 bounded preflight，再跑一个 1800s same-host no-order shadow：沿用 late_repair-only90 配置并增加 `--late-repair-fill-to-balance-after-s 90 --event-lite-summary --pair-source-event-lite-summary`，只拉小 manifest/aggregate/summary/stdout/stderr/runner.pid，不拉 events JSONL。

242. 已完成 fill-to-balance90 的 same-host no-order shadow，结论为 `DISCARD_FILL_TO_BALANCE90_SHADOW_ECONOMIC_STRESS_NEGATIVE`。本轮先做 bounded EC2 preflight：root disk `/dev/root` 为 145G total、61G used、85G available、42% use，inode 1%，没有任何 `xuan_dplus_passive_passive_shadow_runner.py` 进程，`market.sock`/`local_price.sock`/`chainlink.sock` 与 `broker_manifest.json` 均存在；随后只复制当前 runner 单文件到隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_fill_to_balance90_edge007_20260520T130047Z`，以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 跑 1800s，配置为 edge `0.07`、target_qty `5`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--late-repair-only-after-s 90 --late-repair-fill-to-balance-after-s 90 --event-lite-summary --pair-source-event-lite-summary`。完成后只拉回 manifest、aggregate、summary、stdout/stderr/runner.pid 和 sanity 小文件，未拉 events JSONL，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem；post-run sanity 确认无 xuan-research runner 残留，root disk 仍 42% used。artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_driver_20260520T130047Z/audit_manifest.json` 与 acceptance artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_fill_to_balance_20260520T130047Z/manifest.json` 显示：candidates `43`、queue_supported_fills `24`、pair_actions `5`、pair_qty `6.25`、pair_pnl `+0.394625`、ROI_on_filled_cost `3.94%`、net_pair_cost_p90 `0.97`、residual_qty `18.75`、residual_cost `4.8375`、material_residual_lots `0`；fill-to-balance 触发 candidates `10`、caps `7`、blocks `7`、qty_reduction `5.51`。相对 prior late-repair-only90 shadow，p90 从 `1.02` 改善到 `0.97`，但 candidates `56 -> 43`、pair_actions `29 -> 5`、pair_pnl `+1.976288 -> +0.394625`、residual_qty `7.4975 -> 18.75`、residual_cost `2.31105 -> 4.8375`。按新的 scoring split，pair PnL 本身为正，但 `pair_pnl - taker_fee - residual_cost` 的 conservative risk-adjusted proxy 为 `-4.48575`，`research_ranking.label=TRADEOFF_ECON_POSITIVE_STRESS_PROXY_NEGATIVE`，promotion gate 因 sample 与 normalized residual budget 失败而不通过。因此这不是 canary/promotion evidence，也不应重复同一 1800s shadow。下一跳切回本地 `causal_verifier`：只读已拉回 summary/aggregate 与 candidate-pipeline fill-to-balance evidence，比较为什么 completion candidate pipeline 中 residual 改善没有转化到同宿主 shadow；在找到新的 KEEP 机制前，不再启动 EC2 shadow。

243. 已完成 fill-to-balance pipeline/shadow mismatch 的本地归因，结论为 `UNKNOWN_FILL_TO_BALANCE_SHADOW_STRESS_NEGATIVE_BUT_SOURCE_FIELDS_INSUFFICIENT`。新增脚本 `scripts/xuan_b27_dplus_fill_to_balance_shadow_mismatch_attribution.py`，只读取本地 candidate-pipeline manifest、已拉回的 fill-to-balance shadow aggregate/summary 和 prior late-repair-only90 shadow aggregate/summary；未 SSH、未连 shared-ingress、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store。artifact `xuan_research_artifacts/xuan_b27_dplus_fill_to_balance_shadow_mismatch_attribution_20260520T141510Z/manifest.json` 发现两个硬差异：candidate-pipeline KEEP 使用 `edge=0.055`、`max_open_cost=250`，而同宿主 shadow 使用 `edge=0.07`、`max_open_cost=80`；因此本地 lead 与 shadow 不是严格同一配置面。shadow 内部归因也显示残仓不是单纯由 fill-to-balance cap 直接产生：有 fill-to-balance 活动的 3 个 slug 合计 candidates `22`、fills `12`、pair_actions `3`、residual_qty `7.5`；无 fill-to-balance 活动但有 residual 的 3 个 slug 合计 candidates `21`、fills `12`、pair_actions `2`、residual_qty `11.25`，占本轮 residual `60%`。当前 summary/event-lite 只暴露 cap/block 总量和 bucket，缺少 per-candidate `deficit/base_seed_qty/capped_seed_qty/qty_reduction` bucket，也不能在不读 events JSONL 的前提下把 residual lot 或 pair action 回连到具体 fill-to-balance seed。因此本轮不能给出可部署窄机制，也不能证明 fill-to-balance 本身必然失败；它明确指出下一跳应该先在本地 candidate pipeline 上做 shadow-aligned rerun：`edge=0.07`、`max_open_cost=80`，比较 late_repair90 control 与 `late_repair90_fill_to_balance_after_90`。在该本地 verifier 返回 KEEP 前，不再启动 EC2 shadow/canary。

244. 已完成 shadow-aligned fill-to-balance 本地 candidate-pipeline state-machine rerun，结论为 `KEEP_SHADOW_ALIGNED_FILL_TO_BALANCE_RESEARCH_ONLY`。新增脚本 `scripts/xuan_b27_dplus_candidate_pipeline_shadow_aligned_fill_rerun.py`，复用 candidate-pipeline state-machine，只跑两个 profile：`edge=0.07/max_open_cost=80` 的 `late_repair90` control 与同配置的 `late_repair90_fill_to_balance_after_90`；只读 `candidate_base.duckdb` 与本地 manifests，不 SSH、不连 shared-ingress、不启动 shadow、不读 events JSONL、不扫 raw/replay/collector/full completion store。artifact `xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_shadow_aligned_fill_rerun_20260520T143629Z/manifest.json` 显示：control seed_actions `41580`、pair_actions `23362`、pair_qty `25510.275`、fee_after_pnl `+2894.880674`、stress100_worst_pnl `+2913.845075`、worst_day_fee_after_pnl `+161.917289`、residual_qty_rate `1.2097%`、residual_cost_rate `0.9470%`；fill-to-balance 保留 seed_actions `39898`（retention `95.95%`）和 pair_qty `24632.495`（retention `96.56%`），fee_after_pnl 仍高为 `+2763.315920`，stress100_worst_pnl 提升到 `+2960.238325`，worst_day 仍为 `+155.145633`，residual_qty_rate 降到 `0.5219%`，residual_cost_rate 降到 `0.2656%`，residual rate reductions `56.86%/71.95%`，并记录 caps `5217`、blocks `6805`、qty_reduction `4479.1775`。这说明本地同配置下 fill-to-balance 机制仍是 KEEP；上一轮 1800s same-host shadow 失败更可能是窗口/样本与缺少 per-candidate source linkage 的问题，而不是本地机制在 shadow-aligned 参数面上失效。该结果仍是 `PASS_LOCAL_COMPLETION_RESEARCH_ONLY`、deployable=false，不是 source-of-truth replay 或 canary evidence。下一跳可以回到 `shadow_observe`，但不能重复 exact 1800s fill-to-balance shadow；若 EC2 preflight 通过，应运行一个更有样本的 bounded no-order fill-to-balance shadow（例如 sample-scale 3600s）并继续只拉小 summary/aggregate。

245. 已完成 shadow-aligned fill-to-balance 的 3600s sample-scale same-host no-order shadow，结论为 `DISCARD_FILL_TO_BALANCE_SAMPLE_SCALE_RISK_ADJUSTED_PROXY_NEGATIVE`。本轮先做 bounded EC2 preflight：root disk 42% used、inode 1%，无 active xuan-research runner，shared-ingress 小元数据与 sockets 正常；随后只复制当前 runner 单文件到隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_fill_to_balance90_sample_scale_edge007_20260520T145943Z`，以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 跑 3600s，配置为 edge `0.07`、target_qty `5`、max_open_cost `80`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--late-repair-only-after-s 90 --late-repair-fill-to-balance-after-s 90 --event-lite-summary --pair-source-event-lite-summary`。完成后只拉回 manifest、aggregate、summary、stdout/stderr/runner.pid 和 sanity 小文件，未拉 events JSONL，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem；post-run sanity 确认无 xuan-research runner 残留，root disk 仍 42% used。artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_fill_to_balance_sample_scale_driver_20260520T145943Z/audit_manifest.json` 与 acceptance artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_fill_to_balance_sample_scale_20260520T145943Z/manifest.json` 显示：candidates `102`、queue_supported_fills `68`、pair_actions `28`、pair_qty `34.765`、pair_pnl `+2.541877`、ROI_on_filled_cost `7.40%`、net_pair_cost_p90 `1.02`、residual_qty `28.705`、residual_cost `8.27985`、material_residual_lots `0`。sample size 已过，但 normalized risk budget 不过：residual_qty_share_of_filled `33.98% > 15%`、residual_cost_share_of_filled_cost `24.12% > 20%`；fee-adjusted pair PnL proxy 为 `+2.197204`，但 conservative risk-adjusted proxy `pair_pnl - fee - residual_cost - pair_tail_loss_proxy` 为 `-6.152176`。event-lite 显示 residual 横跨 YES/NO 两侧（NO qty `16.25`、YES qty `12.455`）和多个 price buckets，fill-to-balance 本轮只触发 candidates `9`、caps `0`、blocks `5`、qty_reduction `0`，说明这个 3600s 窗口不是“cap 有效但样本不足”，而是 residual budget 结构性未通过。按 scoring split，这不是裸 pair PnL 失败，但已经是风险调整后经济代理失败；该 exact shadow 配置不能推进，也不是 canary/promotion evidence。下一跳回本地 `causal_verifier`：只读已拉回 summary/aggregate 与 candidate-pipeline manifests，归因为什么本地 state-machine KEEP 没有转成 no-order shadow risk budget；在新的本地 KEEP 机制前，不再启动 EC2 shadow/canary。

246. 已完成 fill-to-balance transfer-gap 本地 verifier，结论为 `UNKNOWN_FILL_TO_BALANCE_TRANSFER_GAP_SHADOW_CAPS_ABSENT`。新增脚本 `scripts/xuan_b27_dplus_fill_to_balance_transfer_gap_verifier.py`，只读 derived `candidate_base.duckdb`、本地 candidate-pipeline manifest、以及已拉回的 no-order shadow aggregate/summary/audit JSON；未 SSH、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未修改 runner/trading path。artifact `xuan_research_artifacts/xuan_b27_dplus_fill_to_balance_transfer_gap_verifier_20260520T171157Z/manifest.json` 对同配置 state-machine 重新 trace 了 fill-to-balance activation：本地 candidate-pipeline 有 `9196` 个 late fill-to-balance candidates，占 seed_actions `23.05%`，其中 caps `5217`（cap share `56.73%`）、blocks `6805`、qty_reduction `4479.1775`；这些机会全部在 `offset_90_120`，YES/NO 基本对称（NO `4856`、YES `4340`），主要来自 `deficit<=1`（candidates `5350`、caps `4940`、qty_reduction `4446.9375`）和 `deficit_1_2`（candidates `3846`）。同一参数面的 no-order shadow 则只有 `9` 个 fill-to-balance candidates，占 candidates `8.82%`，caps `0`、blocks `5`、qty_reduction `0`，同时 residual_qty `28.705`、risk-adjusted proxy `-6.152176`。这把 transfer gap 定位得更清楚：本地 KEEP 机制依赖大量小 deficit late repair cap；当前 shadow summary 证明这些 cap 在 shared-ingress no-order 路径里没有出现，但它没有暴露 per-candidate `deficit/base_seed_qty/capped_seed_qty/qty_reduction` buckets，也不能把 residual lot 或 pair action 回连到 fill-to-balance-active seed。结论是 UNKNOWN 而不是 KEEP/DISCARD：不能再放大 shadow，也不能靠现有 summary 证明机制天然失效。下一跳是 `implementability_review`：给 shadow runner 增加默认关闭的 fill-to-balance diagnostic event-lite buckets（deficit/base_qty/capped_qty/qty_reduction 按 side/offset/slug 聚合），本地 smoke 后再决定是否有资格重跑 shadow；在这之前不启动 EC2 shadow/canary。

247. 已完成 no-order shadow runner 的 fill-to-balance diagnostic event-lite implementability review，结论为 `KEEP_FILL_TO_BALANCE_DIAGNOSTIC_EVENT_LITE_READY`。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭参数 `--fill-to-balance-diagnostic-event-lite-summary`，CLI 要求它必须和 `--event-lite-summary`、`--late-repair-fill-to-balance-after-s` 搭配；未设置时既有 single-profile、multi-profile、event-lite、pair-source event-lite、late-repair-only、fill-to-balance 行为与 summary 结构保持不变。开启后，每个 late fill-to-balance active seed 会在 summary/aggregate 的 `late_repair_fill_to_balance_diagnostics` 下输出 side/offset 维度的 candidate/status 计数、deficit bucket、base_seed_qty bucket、capped_qty bucket、qty_reduction bucket 和 qty_reduction amount，用于解释 cap/block/uncapped repair 路径而不拉 events JSONL。新增本地无网络 smoke `scripts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_diagnostic_smoke.sh`，artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_diagnostic_smoke_20260520T185654Z/manifest.json` 为 PASS/KEEP：验证默认诊断字段缺省、3 手 tiny-deficit cap、0.5 手 dust block、7 手缺口 uncapped repair seed、aggregate 与 per-summary 合计一致，并验证 CLI 拒绝缺少 event-lite 或缺少 fill-to-balance gate 的诊断配置。回归 smoke 同步通过：fill-to-balance `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_smoke_20260520T185703Z/manifest.json`、activation `..._activation_smoke_20260520T185703Z/manifest.json`、multi-profile `..._multi_profile_smoke_20260520T185703Z/manifest.json`、late-repair-only `..._late_repair_only_smoke_20260520T185703Z/manifest.json`、pair-source event-lite `..._pair_source_event_lite_smoke_20260520T185709Z/manifest.json`。该实现只提供 summary 级诊断能力，不是 promotion/canary evidence。下一跳如果要消耗 EC2 窗口，必须先 bounded preflight，再只跑带新增诊断 flag 的 no-order diagnostic shadow；仍只拉小 manifest/aggregate/summary/stdout/stderr/runner.pid，不拉 events JSONL，不进入 G2 canary。

248. 已完成带 fill-to-balance diagnostic buckets 的 same-host no-order diagnostic shadow，结论为 `DISCARD_FILL_TO_BALANCE_DIAGNOSTIC_CAPS_ABSENT_RISK_BUDGET_FAIL`。本轮先在 EC2 只读 preflight 确认 root disk 42% used、inode 1%、无 active xuan-research runner、shared-ingress 小元数据与 sockets 正常；随后只复制当前 runner 单文件到隔离目录 `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_fill_to_balance90_diagnostic_edge007_20260520T191701Z`，以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 跑 3600s。配置为 edge `0.07`、target_qty `5`、max_open_cost `80`、activation `opp_seen15`、pairing-only residual、imbalance cap `1.25`、offsets `0..25`、`--late-repair-only-after-s 90 --late-repair-fill-to-balance-after-s 90 --event-lite-summary --pair-source-event-lite-summary --fill-to-balance-diagnostic-event-lite-summary`。完成后只拉回 manifest、aggregate、summary、stdout/stderr/runner.pid 与 sanity 小文件，未拉 events JSONL，未 repo sync/build/service control，未写 broker/shared-ingress/env/live，未发送 order/cancel/redeem；post-run sanity 确认无 xuan-research runner 残留，root disk 仍 42% used。audit artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_fill_to_balance_diagnostic_driver_20260520T191701Z/audit_manifest.json` 与 acceptance artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_fill_to_balance_diagnostic_20260520T191701Z/manifest.json` 显示：candidates `116`、queue_supported_fills `89`、pair_actions `54`、pair_qty `59.5625`、pair_pnl `+4.593913`、ROI_on_filled_cost `9.45%`、net_pair_cost_p90 `1.09`、residual_qty `20.0`、residual_cost `6.225`、material_residual_lots `0`。sample 与裸 pair PnL 通过，但 normalized promotion risk budget 失败：residual_qty_share_of_filled `18.06% > 15%`、pair_tail_loss_share_of_pair_pnl `13.79% > 5%`；fee-adjusted pair PnL proxy 为 `+3.888376`，conservative risk-adjusted proxy 为 `-2.872686`，`research_ranking.label=TRADEOFF_ECON_POSITIVE_STRESS_PROXY_NEGATIVE`，`promotion_gate.passed=false`。新增诊断桶解释了 transfer gap：本轮 no-order shadow 只有 `12` 个 fill-to-balance candidates、`1` 个 cap、`6` 个 block、qty_reduction 仅 `0.0425`，全部在 `offset_90_120` 且 deficit bucket 全是 `deficit_1_2`；base_seed_qty 已经落在 `<=2` 手小量区间，因此 fill-to-balance 在 live/no-order path 中几乎没有本地 candidate-pipeline 里的“小缺口但 full-target seed”可削减。对比本地 transfer-gap verifier 的 `5217` 个 caps 与 `4479.1775` qty reduction，这说明本地 state-machine lead 没有按当前 seed sizing surface 转化到 shared-ingress no-order shadow。该 shadow/promotion candidate 应丢弃；underlying local candidate-pipeline lead 保留为 research-only、deployable=false。下一跳进入本地 `causal_verifier`：从 candidate_base/candidate_registry 做 size-aware transfer verifier，把 state-machine seed sizing 对齐到 shadow base seed 约束（public_trade_size/fill haircut、target room、open-cost room、imbalance room 等）后再评估 late_repair90 + fill-to-balance；在该本地 verifier 返回 KEEP 前，不再启动 EC2 shadow/canary。

249. 已完成 size-aware fill-to-balance transfer verifier，结论为 `UNKNOWN_SIZE_AWARE_FILL_TO_BALANCE_MIXED`。新增脚本 `scripts/xuan_b27_dplus_size_aware_fill_to_balance_transfer_verifier.py`，只读 `candidate_base.duckdb`、本地 candidate-pipeline manifests、以及已拉回的 diagnostic no-order shadow aggregate/audit/acceptance JSON；未 SSH、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未修改 runner/trading path、未发送 order/cancel/redeem。artifact `xuan_research_artifacts/xuan_b27_dplus_size_aware_fill_to_balance_transfer_verifier_20260520T205314Z/manifest.json` 复核了一个关键事实：当前 candidate-pipeline state-machine 已经按 runner 可用公式计算 base seed qty，即 `min(max_seed_qty, public_trade_size*fill_haircut, target_room, open_cost_room, imbalance_room)`，所以 transfer gap 不是因为本地完全忽略 trade size/open-cost/imbalance sizing。三档本地重放结果：late_repair90 control 为 seed_actions `41580`、pair_qty `25510.275`、fee_after_pnl `+2894.880674`、stress100 `+2913.845075`、residual_qty_rate `1.2097%`；all-size-aware fill-to-balance 保持本地强 lead，seed_actions `39898`、pair_qty `24632.495`、fee_after_pnl `+2763.315920`、stress100 `+2960.238325`、residual_qty_rate `0.5219%`、caps `5217`、blocks `6805`、qty_reduction `4479.1775`；而只允许 shadow diagnostic 实际出现的 `base_seed_qty<=2 && deficit_1_2` surface 后，仍有 seed_actions `40678`、pair_qty `25089.8725`、fee_after_pnl `+2817.852885`、stress100 `+3008.0135`、residual_qty_rate `0.5488%`，但 caps 只保留 `587/5217=11.25%`，qty_reduction 只保留 `68.82/4479.1775=1.54%`。该结果说明：本地 size-aware bucket 仍然经济为正且 residual/stress 改善，但它需要 candidate_base 里 `4340` 个 shadow-like F2B candidates；真实 diagnostic no-order shadow 只有 `12` 个 F2B candidates、`1` 个 cap、qty_reduction `0.0425`。因此本轮不是 KEEP，也不是 fill-to-balance 裸经济失败；它把差距进一步定位到 no-order gate/trigger parity：candidate-pipeline 本地流没有建模或没有同窗表达 `activation_opp_seen`、`pairing_only_when_residual`、实际 trigger stream/candidate size 分布等 no-order runner gate。下一跳仍应本地 `causal_verifier`，实现/运行 no-order gate parity verifier，在 candidate_base 兼容字段上显式加入 activation `opp_seen15` 与 pairing-only-when-residual 约束，确认 F2B opportunities 是否会收敛到 shadow diagnostic 的低 cap/低 qty_reduction；该 verifier 返回 KEEP 前仍不启动 EC2 shadow/canary。

250. 已完成 no-order gate parity verifier，结论为 `UNKNOWN_NO_ORDER_GATE_PARITY_MIXED_OR_FIELD_GAP`。新增脚本 `scripts/xuan_b27_dplus_no_order_gate_parity_verifier.py`，只读 `candidate_base.duckdb`、本地 manifests、size-aware verifier manifest、以及已拉回的 diagnostic no-order shadow aggregate/audit/acceptance JSON；未 SSH、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未修改 runner/trading path、未发送 order/cancel/redeem。artifact `xuan_research_artifacts/xuan_b27_dplus_no_order_gate_parity_verifier_20260520T212620Z/manifest.json` 在本地 candidate_base 重放中加入 runner-like `activation_mode=opp_seen`、`activation_window_s=15` 与 `pairing_only_when_residual`：gate parity control 相比 no-gate control 产生 activation blocks `16754`、pairing-only blocks `42664`，seed_actions `38277`、pair_qty `23469.155`、fee_after_pnl `+2685.264040`、stress100 `+2683.731550`、residual_qty_rate `1.2688%`。gate parity fill-to-balance 仍没有收敛到 shadow 级别：seed_actions `36583`、pair_qty `22574.0275`、fee_after_pnl `+2555.433252`、stress100 `+2722.451200`、residual_qty_rate `0.5534%`、F2B candidates `8944`、caps `5089`、blocks `6630`、qty_reduction `4293.87`；相对 no-gate F2B，F2B candidate/cap/qty-reduction retention 分别仍有 `97.22%/97.53%/95.85%`。真实 diagnostic no-order shadow 只有 F2B candidates `12`、caps `1`、qty_reduction `0.0425`，shadow/local gate-parity F2B candidate ratio 只有 `0.134%`。结论：activation/pairing gates 本身不足以解释 transfer gap；本地 replay 仍保留大量 F2B 机会，但 live/no-order diagnostic window 没有。这个 UNKNOWN 的确切缺口是同窗/source 字段：diagnostic shadow 是 2026-05-20 live window，而 candidate-pipeline V1 本地允许标签只到 20260518；此外缺少 live book/trade source_sequence_id interleaving、pending-order queue/touch/fill timing、以及 intentionally not pulled 的 per-candidate events JSONL source links。因此 fill-to-balance90 继续保持 research-only，不应再跑任何 fill-to-balance90 shadow；下一步应转向新的本地非价格机制或先做 source-field/status hygiene，不再把 fill-to-balance 当 shadow candidate。

251. 已完成 fill-to-balance90 状态卫生与机制冻结，结论为 `KEEP_FILL_TO_BALANCE_FREEZE_AND_SOURCE_FIELD_READINESS_SELECTED`。新增 status artifact `xuan_research_artifacts/xuan_b27_dplus_status_hygiene_fill_to_balance_freeze_20260520T214753Z/manifest.json`，不改 runner/trading path、不 SSH、不启动 shadow、不读 events JSONL、不扫 raw/replay/collector/full completion store、不发送 order/cancel/redeem。本轮把 `late_repair90_fill_to_balance_after_90` / `--late-repair-fill-to-balance-after-s 90` shadow family 明确冻结：本地历史 candidate-pipeline 证据仍为正（`xuan_research_artifacts/xuan_b27_dplus_candidate_pipeline_shadow_aligned_fill_rerun_20260520T143629Z/manifest.json`），但 sample-scale shadow 与 diagnostic shadow 均未通过 live/no-order promotion risk budget（`xuan_research_artifacts/xuan_b27_dplus_shadow_fill_to_balance_sample_scale_driver_20260520T145943Z/audit_manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_shadow_fill_to_balance_diagnostic_driver_20260520T191701Z/audit_manifest.json`），后续 size-aware 与 no-order gate parity verifier 又证明本地 replay 无法用已允许字段解释 2026-05-20 live window 的 F2B candidate/cap 缺失（`xuan_research_artifacts/xuan_b27_dplus_size_aware_fill_to_balance_transfer_verifier_20260520T205314Z/manifest.json`、`xuan_research_artifacts/xuan_b27_dplus_no_order_gate_parity_verifier_20260520T212620Z/manifest.json`）。冻结条件：在同窗 source fields 可用并且本地 verifier 返回 KEEP 前，不得再跑 exact `fill-to-balance90` 1800s、3600s、diagnostic 或 canary；也不得把本地历史 completion/candidate-pipeline positive 当成 live/no-order transfer evidence。缺失字段清单固定为：live book/trade `source_sequence_id` 全序、pending-order queue/touch/fill timing、shared-ingress 同窗 book/trade interleaving、2026-05-20 same-window candidate_base/candidate_registry、以及 per-candidate source links（events JSONL 不应 retroactively 拉取）。下一跳选定为本地 `same_window_source_field_readiness_verifier`：只检查 candidate_base/candidate_registry schema、已拉回的 summary/aggregate/audit schema 与现有 runner diagnostic 字段，产出是否能在不碰 raw/replay/full store/events JSONL/EC2 的前提下构造同窗 no-order parity adapter；KEEP 条件是生成可实现的 default-off source-field adapter/spec 并确认不需要禁用数据源，UNKNOWN 条件是只能由同窗 live source/event fields 解决且列出 exact missing fields，DISCARD 条件是允许数据无法支持下一步 verifier。该下一跳不是 promotion/canary evidence，也不是新的 shadow 许可。

252. 已完成 same-window source-field readiness verifier，结论为 `UNKNOWN_SAME_WINDOW_SOURCE_FIELDS_INSUFFICIENT`。新增脚本 `scripts/xuan_b27_dplus_same_window_source_field_readiness_verifier.py` 与 artifact `xuan_research_artifacts/xuan_b27_dplus_same_window_source_field_readiness_verifier_20260520T222107Z/manifest.json`，只读 allowed candidate_base/candidate_registry schema、本地 manifests、已拉回的 diagnostic shadow summary/aggregate/audit/acceptance JSON；未 SSH、未连接 shared-ingress、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未改 runner/trading path、未发送 order/cancel/redeem。verifier 输出 field availability matrix：`condition_id`、`slug`、历史 `ts_ms/public_trade_row_id/strict_l1_row_id` 等字段足够做历史 candidate-pipeline adapter，但不足以构造 2026-05-20 same-window no-order parity adapter；candidate_base days 只有 2026-05-02..13 与 2026-05-16..18，缺 2026-05-20 same-window rows。pulled summary 有 `late_repair_fill_to_balance_diagnostics`、pair-source event-lite 和 aggregate risk metrics，但没有 summary-wide per-candidate `source_sequence_id`、`quote_intent_id/source_order_id` 到 fills/pairs/residual 的 source links，也没有 per-candidate `queue_credit`、`first_bid_touch_ms`、`first_trade_touch_ms` timing buckets。结论：允许数据可以解释“历史 replay 正”，但不能证明 live/no-order transfer；fill-to-balance90 继续 frozen/research-only。下一跳不能跑 EC2 shadow/canary；若继续本地工程，应只做默认关闭 source-link diagnostic/spec 的 local smoke，或者切到主线程已显式提出的 public Polymarket profile review，且仅用 public web/API 与 proxy truth。

253. 已完成用户指定 Polymarket public profile 的公开策略 review，结论为 `KEEP_PUBLIC_PROFILE_TWO_SIDED_PAIRING_HYPOTHESIS`。新增脚本 `scripts/xuan_b27_dplus_public_profile_strategy_review.py` 与 artifact `xuan_research_artifacts/xuan_b27_dplus_public_profile_strategy_review_20260520T225109Z/manifest.json`，只用公开 profile URL 与 `data-api.polymarket.com/activity`、`data-api.polymarket.com/positions`；未用 private owner-trade truth，未 SSH、未连接 shared-ingress、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未改 runner/trading path、未发送 order/cancel/redeem。公开样本抓取 1500 条 recent TRADE activity 与 24 条 current positions，覆盖 2026-05-20T17:51:40Z 到 22:50:38Z；activity 全部是 BUY，集中在 BTC/ETH/SOL/XRP 短周期 Up/Down，101 个 slugs，notional_sum 约 `29095.845461`，p50 trade interarrival 约 2s。positions 显示 15 个 current conditions 中 8 个同时持有 Up/Down，both-side condition share `53.33%`；current positions 的 paired-inventory proxy 为 pair_qty `5324.5112`、pair_pnl_proxy_before_fees `+288.556287`、residual_qty `2070.5944`，例如 BTC 6PM ET Up/Down 的 avg_pair_cost `0.9502`、pair_qty `3379.9767`、pair_pnl_proxy `+168.32284`。解释：这个账户有参考价值，但参考点不是“方向预测”或已冻结的 fill-to-balance tiny-deficit cap，而是公开可观察的“双边持续买入 + 仓位级配对成本/残仓 mark 风险账本”。下一跳应本地无网络验证 `public_profile_two_sided_inventory_pairing`：在 candidate_base/candidate_registry 上比较 late_repair90 control 与 portfolio-level paired-inventory risk-adjusted ranking rule，看是否用 account-level `avg_pair_cost < 1`、pair_qty、residual mark/stress budget 改善 ranking；不得复用 fill-to-balance90、public/source price caps、static target/cap sweeps、cooldown/admission caps、EC2 或 raw/replay scan。该 public review 不是 promotion/canary evidence。

254. 已完成 public-profile 派生的本地 paired-inventory verifier，结论为 `KEEP_PUBLIC_PROFILE_PAIRED_INVENTORY_LOCAL_RANKER`。新增脚本 `scripts/xuan_b27_dplus_public_profile_pairing_verifier.py` 与 artifact `xuan_research_artifacts/xuan_b27_dplus_public_profile_pairing_verifier_20260520T232251Z/manifest.json`，只读 `candidate_base.duckdb`、local manifests 与 public profile review artifact；未 SSH、未连接 shared-ingress、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未改 runner/trading path、未发送 order/cancel/redeem。verifier 重放 shadow-aligned `late_repair90`（edge `0.07`、target `5`、max_open_cost `80`、late_repair_only_after_s `90`，不启用 fill-to-balance），并额外构造 condition-level paired inventory ledger。control ledger 指标：active_markets `4303`、seed_actions `41580`、pair_qty `25510.275`、weighted_pair_cost `0.857336`、fee_after_pnl `+2894.880674`、stress100_worst_pnl `+2913.845075`、worst_day `+161.917289`、residual_qty_rate `1.2097%`、residual_cost_rate `0.9470%`。最佳规则为 `portfolio_risk_adjusted_nonnegative`（condition-level conservative `pair_pnl - fee - residual_cost - stress >= 0`）：保留 seed_actions `39580`（95.19%）、pair_qty `24374.375`（95.55%）、active_markets `4079`（94.79%），fee_after_pnl 仍为 `+2803.932813`、worst_day `+153.357139`，stress100 提升到 `+2924.185425`，residual_qty_rate 降到 `0.8780%`、residual_cost_rate 降到 `0.5054%`。解释：public profile 的“仓位级双边配对账本”在本地 candidate-pipeline 上有可检验价值；它不是价格 cap、不是 fill-to-balance，也不是静态 target/cap sweep。限制：该 ranker 目前是 condition-ledger research scoring，不是 runner/trading behavior，不是 promotion/canary evidence；下一跳只能进入 implementability_review，先做默认关闭的 portfolio-ledger diagnostic/summary spec 与本地 smoke，不能直接跑 EC2 shadow。

255. 已完成 public-profile paired-inventory lead 的 no-order runner implementability review，结论为 `KEEP_PORTFOLIO_LEDGER_DIAGNOSTIC_READY`。`tools/xuan_dplus_passive_passive_shadow_runner.py` 新增默认关闭参数 `--portfolio-ledger-event-lite-summary`，且必须与 `--event-lite-summary` 配套使用；flag 未设置时，既有 single-profile、multi-profile、event-lite、pair-source event-lite、late-repair-only、fill-to-balance 与 fill-to-balance diagnostics 行为和 summary shape 均保持不变。开启后，summary/aggregate 的 `event_lite.portfolio_ledger_diagnostics` 暴露 condition-level/slug-level 账本：seed_actions、queue_supported_fills、pair_qty、pair_cost_sum、avg_pair_cost、pair_pnl、official_taker_fee_proxy、residual_qty/cost by side、stress_cost_proxy、conservative_risk_adjusted_proxy，以及 `portfolio_risk_adjusted_nonnegative` 的 pass/fail bucket。新增本地无网络 smoke `scripts/xuan_b27_dplus_passive_passive_shadow_runner_portfolio_ledger_smoke.sh`，artifact `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_portfolio_ledger_smoke_20260520T235932Z/manifest.json` 为 `PASS`/`KEEP`：验证默认 diagnostics absent、配对 condition 账本为 risk-adjusted nonnegative、residual-heavy condition 为 negative、aggregate 正确合并两类 bucket，并且 CLI 拒绝未开启 event-lite 的 portfolio ledger diagnostics。验证结果：`py_compile` PASS、`git diff --check` PASS、json.tool PASS；回归 smoke PASS：activation `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_activation_smoke_20260520T235946Z/manifest.json`、multi-profile `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_multi_profile_smoke_20260520T235946Z/manifest.json`、late-repair-only `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_late_repair_only_smoke_20260520T235946Z/manifest.json`、fill-to-balance `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_smoke_20260520T235946Z/manifest.json`、fill-to-balance diagnostic `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_fill_to_balance_diagnostic_smoke_20260520T235946Z/manifest.json`、pair-source event-lite `xuan_research_artifacts/xuan_b27_dplus_passive_passive_shadow_runner_pair_source_event_lite_smoke_20260520T235953Z/manifest.json`。本轮未 SSH、未连接 shared-ingress、未启动 shadow、未读 events JSONL、未扫 raw/replay/collector/full completion store、未发送 order/cancel/redeem。该结果只是默认关闭的 summary-only 可观测性准备，不是 promotion/canary evidence；下一跳若继续，应先做 bounded EC2 preflight，再运行一个诊断-only no-order shadow 加 `--portfolio-ledger-event-lite-summary` 拉回小 summary/aggregate，用于观察 public-profile paired-inventory rule 是否在 live/no-order window 中保持 risk-adjusted quality。

256. 已完成 portfolio-ledger diagnostic 的 bounded same-host no-order shadow，结论为 `DISCARD_PORTFOLIO_LEDGER_DIAGNOSTIC_RISK_BUDGET_FAIL`。本轮延续 preflight 后的 active run `/srv/pm_as_ofi/xuan_research_runs/xuan_research_dplus_shadow_portfolio_ledger_diagnostic_edge007_20260521T001843Z`，以 `pmofi`、`PM_DRY_RUN=true`、shared-ingress client root `/srv/pm_as_ofi/shared-ingress-main` 运行；配置为 edge `0.07`、target_qty `5`、max_open_cost `80`、imbalance cap `1.25`、activation `opp_seen15`、pairing-only residual、offsets `0..25`、duration `3600s`、`--late-repair-only-after-s 90`、`--event-lite-summary`、`--pair-source-event-lite-summary`、`--portfolio-ledger-event-lite-summary`，且明确未启用 fill-to-balance。完成后只拉回 manifest、aggregate、`*.summary.json`、stdout/stderr/runner.pid 与小型 sanity 文件，未拉 events JSONL；post-run sanity 确认无 xuan-research runner 残留，root disk `43%`、inode `1%`。audit artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_portfolio_ledger_diagnostic_driver_20260521T001843Z/audit_manifest.json` 与 acceptance artifact `xuan_research_artifacts/xuan_b27_dplus_shadow_trading_acceptance_portfolio_ledger_diagnostic_20260521T001843Z/manifest.json` 显示：sample 达标（candidates `101`、queue_supported_fills `76`、pair_actions `42`、pair_qty `40.29`），bare pair PnL 为正（pair_pnl `+3.179453`、ROI on filled cost `8.134%`、fee-adjusted proxy `+2.698231`），material_residual_lots 为 `0`；但 normalized promotion risk budget 失败，residual_qty `28.735`、residual_cost `9.84115`、residual_qty_share_of_filled `30.74% > 15%`、residual_cost_share_of_filled_cost `25.18% > 20%`，conservative risk_adjusted_pnl_proxy `-7.183209`。portfolio ledger diagnostics 也未复现本地 ranker：aggregate `conservative_risk_adjusted_proxy=-8.236069`，13 个活跃 condition 中 8 个为 risk-adjusted negative，残仓成本分布在 YES/NO 两边（NO `5.228525`、YES `4.612625`）与多个 price bucket；这说明 live/no-order window 的 portfolio-ledger negative 不是一个窄的可部署 source bucket。结论：public-profile paired-inventory 本地 lead 仍保留为 research-only，但这个 exact diagnostic shadow/promotion candidate 应丢弃，不能 promotion/canary。下一跳回到本地 `causal_verifier`：只用已拉回 summary/aggregate/audit 与 candidate-pipeline manifests 做 portfolio-ledger transfer attribution，确认是否存在不依赖 fill-to-balance、价格 cap、静态 cap/target sweep 或 cooldown/admission cap 的 bounded non-price follow-up；在新的本地 verifier 返回 KEEP 前不再启动 EC2 shadow/canary。

257. 已完成 portfolio-ledger transfer attribution，结论为 `DISCARD_PORTFOLIO_LEDGER_TRANSFER_BROAD_LIVE_RISK_SAMPLE_DESTROYED`。artifact `xuan_research_artifacts/xuan_b27_dplus_portfolio_ledger_transfer_attribution_20260521T031327Z/manifest.json` 只读 public-profile local verifier `xuan_research_artifacts/xuan_b27_dplus_public_profile_pairing_verifier_20260520T232251Z/manifest.json`、portfolio-ledger diagnostic shadow 的已拉回 summary/aggregate/audit/acceptance JSON；未 SSH、未启动 shadow、未读 events JSONL、未连接 shared-ingress、未扫 raw/replay/collector/full completion store、未修改 runner/trading path、未发送 order/cancel/redeem。结论清晰：本地 historical candidate-pipeline ranker 能保留约 `95%` seed/pair_qty 并改善 residual/stress，但 live/no-order diagnostic window 里同一 condition-ledger score 没有转移。若在 live summary 上做 retrospective `risk_adjusted_nonnegative` positive-only proxy，只保留 active conditions `5/13=38.46%`、seed_actions `25.74%`、queue_supported_fills `30.26%`、pair_actions `47.62%`、pair_qty `47.96%`、pair_pnl `61.42%`；虽然 residual 会下降，但这是 summary-only removal proxy，且直接破坏样本，不是 deployable mechanism。negative residual risk 也不是窄桶：negative conditions 占 active majority，residual cost 跨 YES/NO 两边，并跨多个 residual price buckets。因此 public-profile paired-inventory 仍可作为研究灵感，但 exact portfolio-ledger live/no-order transfer 与 exact 3600s diagnostic shadow 均应冻结为 promotion candidate。下一跳切到本地 status/mechanism selection：选择一个不同的非价格 inventory-path verifier，例如动态 condition-ledger risk-state gate，只在某 condition 已转为 risk-adjusted negative 后阻止新的 risk-increasing seed，同时保留 repair/pairing 路径；该 verifier 必须只用 candidate_base/candidate_registry-compatible 本地数据，在返回 KEEP 前不得启动 EC2 shadow/canary。

258. 已完成 portfolio-ledger 状态卫生与下一机制选择，结论为 `KEEP_PORTFOLIO_LEDGER_FREEZE_AND_DYNAMIC_RISK_STATE_GATE_SELECTED`。status artifact `xuan_research_artifacts/xuan_b27_dplus_status_hygiene_portfolio_ledger_freeze_20260521T034715Z/manifest.json` 只读本地 manifests 与 schema，不 SSH、不启动 shadow、不读 events JSONL、不连接 shared-ingress、不扫 raw/replay/collector/full completion store、不修改 runner/trading path、不发送 order/cancel/redeem。本轮将 exact portfolio-ledger diagnostic 3600s shadow 冻结为 promotion candidate：它达到 sample 且 bare pair PnL 为正，但 prior acceptance/audit 风险预算失败，随后 transfer attribution 证明 live positive-only ledger 只能靠 summary removal 保留 `25.74%` seed_actions 与 `47.96%` pair_qty，属于 sample-destroying proxy。public-profile paired-inventory 仍保留为 research inspiration，不作为 canary/promotion evidence。下一机制选定为 `dynamic_condition_ledger_risk_state_gate`：从 candidate_base/candidate_registry-compatible 本地数据重放 shadow-aligned late_repair90；当某 condition 的在线 conservative ledger（pair_pnl - fee - residual_cost_proxy - stress_cost_proxy）进入 risk_adjusted_negative 后，只阻止该 condition 的新增 risk-increasing seed，同时继续允许 underweight repair、imbalance-reducing 或 pairing-improving seed。该机制不使用 fill-to-balance90、不做 public/source price cap、不扫静态 target/cap、不用 residual cooldown/admission cap、不扩大 activation window、也不是 completion 后 summary bucket removal。schema readiness 已确认 required fields 存在：`condition_id/slug/day/ts_ms/offset_s`、`side/opposite_side/public_trade_size/seed_px/seed_qty/seed_cost/official_taker_fee`、`pair_qty_after_seed/pair_actions_after_seed/inventory_yes_qty_after/inventory_no_qty_after/inventory_yes_cost_after/inventory_no_cost_after`、strict/public coverage 字段；限制是 verifier 可能仍需本地 state-machine rerun 才能拿到 exact pre-seed same/opposite qty 与 immediate pairing effect。下一跳切到本地 `causal_verifier`：实现/运行 dynamic condition-ledger risk-state gate，对比 late_repair90 control 与 variant 的 seed_actions、active_markets、pair_actions、pair_qty、weighted_pair_cost、fee_after_pnl、stress100、worst-day、residual rates 与 risk-state block counts；该 verifier 返回 KEEP 前不得启动 EC2 shadow/canary。

## 当前结论

D+ 现在已经有真实 no-order passive/passive shadow runner 报告。1800s same-window multi-profile shadow 曾显示 `repair75` 在同窗样本、pair_qty 和 PnL 上优于 `repair45/60`，且三档均通过 p90/residual/PnL/no-order safety；但随后 3600s、offsets `0..25` 的 sample-scale `repair60/75` A/B 证明该方向放大后 p90 与 residual tail 失守，不能作为可部署或 canary-ready 证据。summary-only 归因把失败定位到新窗口 slug、YES residual 和 price-tail bucket；proxy scorer 又证明简单 removal filter 只能靠牺牲样本降低 residual，不能作为部署机制。event-lite shadow 进一步显示：`repair60` 可在 1800s 窗口通过 p90/residual/PnL gate，但样本只有 30 candidates；`repair75` 样本略好但 p90 轻微破线。当前本地 candidate pipeline V1 已可用：state-machine rerun 先推翻了“直接丢弃 offset_90_120”的 proxy，但新的 late-repair90 gate 保留 80.55% seed_actions、把 residual_qty_rate 从 5.0175% 降到 1.2097%，并把 stress100_worst_pnl 提到 +2141.110075；该 gate 已以默认关闭参数接入 no-order shadow runner，并通过本地 no-network smoke。same-host no-order shadow 证明 late-repair-only90 可以把 residual 控到阈值内，但仍只有 56 candidates 且 net_pair_cost_p90 为 1.02，未过 sample/p90 gate；新增 pair-source event-lite 后的同类 shadow 成功把 p90 高成本 pair 回连到 source public px `>=0.55`，但该窗口自身退化到 42 candidates、p90 1.23、residual_qty 11.0625。随后本地 candidate-pipeline rerun 连续推翻了 `public_trade_px_hi=0.55` 硬 cap、全侧 risk-increasing cap、以及 NO-side-only risk-increasing cap：这些 cap 均保持或改善部分经济指标，但会删除必要 repair seed 或只带来极小 pair-cost proxy 改善，同时让 residual rate 相对 late-repair90 恶化。因此 source-public-price cap family 暂时冻结；这不是策略净 PnL 失败，而是相对机制排名失败。`late_repair90_fill_to_balance_after_90` 在本地 candidate pipeline 里是强研究 lead；shadow-aligned 本地 rerun（edge `0.07`、max_open_cost `80`）也保持 KEEP，但同宿主 no-order shadow 没有转化：1800s 窗口 p90 改善但样本和 residual 失败，3600s sample-scale 虽达到 102 candidates 且 pair_pnl `+2.541877`，但 residual_qty `28.705`、residual_cost `8.27985`，risk-adjusted proxy `-6.152176`，promotion risk budget 失败；diagnostic shadow 达到 116 candidates 和 pair_pnl `+4.593913`，但 risk-adjusted proxy 仍为 `-2.872686`，且 F2B caps 只有 1 个、qty_reduction 仅 `0.0425`。后续 transfer-gap、size-aware、no-order gate parity verifiers 证明：本地 KEEP 依赖 `offset_90_120` 的小 deficit repair caps，但该机会面在 2026-05-20 live/no-order diagnostic window 中基本不存在；允许的本地字段又没有同窗 source ordering、pending-order timing 和 source-link 信息来解释差异。same-window source-field readiness verifier 已确认：当前 allowed local schema 可以构造历史 candidate-pipeline adapter，但不能构造 2026-05-20 no-order parity adapter。因此 fill-to-balance90 shadow family 现在冻结：本地历史 positive 保留为 research-only，不再作为 live/no-order transfer 或 promotion/canary 证据。用户指定 public profile review 给出的“仓位级双边配对账本/风险调整排名”已经通过本地 verifier 初筛：在 shadow-aligned late_repair90 control 上，condition-level `portfolio_risk_adjusted_nonnegative` ranker 保留约 95% seed/pair_qty，同时把 residual_qty_rate 从 `1.2097%` 降到 `0.8780%`、residual_cost_rate 从 `0.9470%` 降到 `0.5054%`、stress100 从 `+2913.845075` 提到 `+2924.185425`；这是新的 research-only lead，下一步只能做默认关闭 portfolio-ledger diagnostics/spec 的 implementability review，不能直接 shadow/canary。2026-05-20 起本机已有本地 strict/cache、completion candidate pipeline、state-machine registry 和 public-account audit proxy truth，可用于快速搜索、事件层研究和 adapter join；但最终 source-of-truth replay 验证仍需 replay archive 下载后单独排队，且当前 D+ 仍需要更大 no-order shadow runner 交出足够样本并通过验收。生产级别仍差两类证据：

1. Rust runtime 中的 D+ observer/canary 实现。
2. 我们自己的 authenticated order/fill/queue/wallet/redeem/cashflow truth。

项目模块化基础是够的。当前主要问题不是授权或基础组件，而是策略证据没有过 trading/backtest gate；接下来应集中在策略参数/训练和真实 shadow/backtest 指标，而不是继续堆离线 micro-gate。

评分口径已单独固化在 `docs/research/xuan/DPLUS_SCORING_CONTRACT_ZH.md`：后续必须区分 research mechanism weaker、promotion risk-budget blocker 和 economic negative，不能再把 residual 单项恶化直接等同于策略经济失败。

后续架构升级设计见：

- `docs/research/xuan/DPLUS_ARCHITECTURE_UPGRADE_ZH.md`
